---
phase: 01-foundation-data-model
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - TARIQ__career_radar/conftest.py
  - TARIQ__career_radar/tests/__init__.py
  - TARIQ__career_radar/tests/test_opportunity_schema.py
  - TARIQ__career_radar/tests/test_config.py
  - TARIQ__career_radar/tests/test_dedup_engine.py
  - TARIQ__career_radar/tests/test_structure.py
  - TARIQ__career_radar/tests/test_registration.py
  - TARIQ__career_radar/tests/test_privacy.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05

must_haves:
  truths:
    - "Every requirement has at least one automated test that will turn RED immediately (before implementation)"
    - "pytest TARIQ__career_radar/tests/ -v runs without import errors and reports all tests as FAILED (red phase)"
    - "Shared fixtures (temp db, sample profile, sample opportunity) are usable by all test files"
  artifacts:
    - path: "TARIQ__career_radar/conftest.py"
      provides: "Shared pytest fixtures: tmp_db_path, sample_profile, sample_opportunity, schema_path"
    - path: "TARIQ__career_radar/tests/test_opportunity_schema.py"
      provides: "DATA-01 schema validation tests"
    - path: "TARIQ__career_radar/tests/test_config.py"
      provides: "DATA-02 profile seed load + key validation tests"
    - path: "TARIQ__career_radar/tests/test_dedup_engine.py"
      provides: "DATA-03 SQLite round-trip, normalization, persistence tests"
    - path: "TARIQ__career_radar/tests/test_structure.py"
      provides: "DATA-04 folder layout tests"
    - path: "TARIQ__career_radar/tests/test_registration.py"
      provides: "DATA-04/05 _index.json, KNOWN_LEDGERS, PRIVACY_CLASSIFICATION tests"
    - path: "TARIQ__career_radar/tests/test_privacy.py"
      provides: "DATA-02/05 profile egress block and privacy-rule tests"
  key_links:
    - from: "TARIQ__career_radar/conftest.py"
      to: "TARIQ__career_radar/tests/*.py"
      via: "pytest fixture injection"
      pattern: "def sample_opportunity|def tmp_db_path|def sample_profile"
    - from: "TARIQ__career_radar/tests/test_registration.py"
      to: "NIZAM__system/governor/ledger_writer.py"
      via: "imports KNOWN_LEDGERS set"
      pattern: "from NIZAM__system.governor.ledger_writer import KNOWN_LEDGERS"
    - from: "TARIQ__career_radar/tests/test_privacy.py"
      to: "NIZAM__system/policies/PRIVACY_CLASSIFICATION.json"
      via: "loads and inspects path_glob rules"
      pattern: "PRIVACY_CLASSIFICATION.json"
---

<objective>
Create all Wave 0 test scaffolding for Phase 1 before any implementation. Every test must be
written against the expected behavior and must FAIL in this plan (RED phase). Implementation
plans will make them GREEN.

Purpose: Enforce TDD discipline. Tests define the contract; code must satisfy it. This also
ensures no task in Phase 1 lacks an automated verify command.

Output: 7 test files + conftest + __init__.py, all importable, all failing with
NotImplementedError or ImportError (not syntax errors). Full suite runs in < 30 s.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-data-model/01-VALIDATION.md
@.planning/phases/01-foundation-data-model/01-RESEARCH.md

<interfaces>
<!-- Python code style from NIZAM conventions -->
<!-- Every test file must start with: -->
```python
from __future__ import annotations
import sys
from pathlib import Path

_REPO = Path(__file__).resolve().parents[3]
if str(_REPO) not in sys.path:
    sys.path.insert(0, str(_REPO))
```

<!-- Ledger writer KNOWN_LEDGERS (current, from ledger_writer.py lines 29-40): -->
KNOWN_LEDGERS = {
    "EVENT_LEDGER", "DECISION_LEDGER", "LEARNING_LEDGER", "DEAD_LETTER",
    "STRATEGY_LEDGER", "BATTLE_LEDGER", "FINANCE_LEDGER", "BODY_LEDGER",
    "PULSATION_LEDGER", "COUNCIL_LEDGER",
    # "CAREER_RADAR_LEDGER" NOT YET present — test must assert it IS present after Plan 01-06
}

<!-- PRIVACY_CLASSIFICATION.json structure (current rules end at line 44 of file): -->
<!-- rules array — test must assert 3 new TARIQ rules exist after Plan 01-06 -->
<!-- Expected new rules (not present yet): -->
{
  "path_glob": "TARIQ__career_radar/data/profile_cache.json",  "classification": "strict_local_maximum"
  "path_glob": "TARIQ__career_radar/data/**",                  "classification": "strict_local"
  "path_glob": "NIZAM__system/ledgers/CAREER_RADAR_LEDGER.jsonl", "classification": "review_before_commit"
}

<!-- opportunity schema required fields (from research): -->
required = [
    "opportunity_id", "title", "company", "location", "remote_status",
    "source", "source_type", "source_url", "access_date",
    "fit_score", "growth_score", "confidence", "tags",
    "salary_usd_low", "salary_usd_high", "salary_evidence_type", "salary_confidence",
    "observed_at", "lane", "data_quality"
]
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create conftest.py with shared fixtures</name>
  <files>TARIQ__career_radar/conftest.py, TARIQ__career_radar/tests/__init__.py</files>
  <behavior>
    - Fixture `tmp_db_path`: yields a tmp Path to a .sqlite file, cleaned up after test
    - Fixture `sample_opportunity`: returns a minimal valid opportunity dict with all required fields set to valid values (fit_score=50, growth_score=40, confidence="LOW", tags=[], salary_usd_low=null, salary_usd_high=null, salary_evidence_type="not_disclosed", salary_confidence="LOW", data_quality="partial", lane="Remote USD", remote_status="fully_remote", source_type="manual")
    - Fixture `sample_profile`: returns a minimal profile dict with keys role_keywords, target_roles, constraints
    - Fixture `schema_path`: returns Path to NIZAM__system/schemas/career_opportunity_record.schema.json (may not exist yet — fixture just returns the path)
    - Fixture `repo_root`: returns the repo root Path (D:/NIZAM)
  </behavior>
  <action>
    Create TARIQ__career_radar/conftest.py with the five fixtures above. Use `from __future__ import annotations`
    as mandatory first import. Use `tmp_path` built-in pytest fixture for tmp_db_path.

    Create TARIQ__career_radar/tests/__init__.py as an empty file (required for pytest discovery with dotted imports).

    The sample_opportunity fixture must produce a dict that satisfies all 20 required fields from the schema.
    Use uuid4 strings for opportunity_id and run_id. Use ISO 8601 UTC strings for access_date and observed_at
    (e.g., "2026-06-14T12:00:00Z"). The fixture does NOT validate against the schema — that is the test's job.

    ADDITIVE ONLY: these are new files; no existing file is touched.
  </action>
  <verify>
    <automated>cd /d/NIZAM && python -m pytest TARIQ__career_radar/conftest.py --collect-only -q 2>&1 | head -5</automated>
  </verify>
  <done>conftest.py exists; `pytest --collect-only` does not error; fixtures are importable</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write failing test suite — all 12 test behaviors</name>
  <files>
    TARIQ__career_radar/tests/test_opportunity_schema.py,
    TARIQ__career_radar/tests/test_config.py,
    TARIQ__career_radar/tests/test_dedup_engine.py,
    TARIQ__career_radar/tests/test_structure.py,
    TARIQ__career_radar/tests/test_registration.py,
    TARIQ__career_radar/tests/test_privacy.py
  </files>
  <behavior>
    test_opportunity_schema.py (DATA-01):
    - test_schema_validate: Load schema JSON from schema_path; validate sample_opportunity against it using jsonschema.validate(); assert validates without error
    - test_schema_required_fields: Load schema; assert all 20 required fields are present in schema["required"]
    - test_schema_rejects_missing_title: sample_opportunity without "title" must fail jsonschema.validate()

    test_config.py (DATA-02):
    - test_profile_seed_load: Import load_profile_seed from TARIQ__career_radar.radar.config; call it; assert result has keys {"role_keywords", "target_roles", "constraints"}
    - test_profile_seed_missing_raises: If profile file doesn't exist, load_profile_seed() must raise ValueError

    test_dedup_engine.py (DATA-03):
    - test_sqlite_roundtrip: Import DedupeEngine from TARIQ__career_radar.radar.dedup_engine; create instance with tmp_db_path; call check_or_add(sample_opportunity); assert is_duplicate=False; call again; assert is_duplicate=True
    - test_normalization_deterministic: Call compute_dedup_key("AI Ops Manager", "Acme, Inc.", "Remote") twice; assert results are equal
    - test_persistence_across_restarts: Insert via DedupeEngine(tmp_db_path); create NEW DedupeEngine(same tmp_db_path); assert check_or_add returns is_duplicate=True

    test_structure.py (DATA-04):
    - test_module_layout: Assert these paths exist under repo_root: TARIQ__career_radar/radar/__init__.py, TARIQ__career_radar/radar/config.py, TARIQ__career_radar/radar/dedup_engine.py, TARIQ__career_radar/data/ (as a dir), TARIQ__career_radar/tests/__init__.py

    test_registration.py (DATA-04/05):
    - test_index_json_valid: Load TARIQ__career_radar/_index.json; assert it has keys "module", "privacy_level", "phase"
    - test_ledger_registered: Import KNOWN_LEDGERS from NIZAM__system.governor.ledger_writer; assert "CAREER_RADAR_LEDGER" in KNOWN_LEDGERS

    test_privacy.py (DATA-02/05):
    - test_privacy_rules_defined: Load NIZAM__system/policies/PRIVACY_CLASSIFICATION.json; assert a rule with path_glob "TARIQ__career_radar/data/profile_cache.json" and classification "strict_local_maximum" exists in rules array
    - test_profile_not_in_egress: Import load_profile_seed; call it (skip if file absent); serialize result to JSON string; assert none of the privacy-sensitive keys ("work_authorization", "minimum_salary_usd", "visa_sponsorship_needed") appear in a mock Telegram payload (empty string represents no profile data sent)
  </behavior>
  <action>
    Write all 6 test files. Each file begins with the mandatory sys.path injection block (see interfaces).

    These tests MUST FAIL NOW because the implementation does not yet exist. Acceptable failure modes:
    - ImportError (modules not yet written)
    - FileNotFoundError (schema/profile files not yet created)
    - AssertionError (KNOWN_LEDGERS doesn't contain CAREER_RADAR_LEDGER yet)

    Do NOT import modules conditionally to avoid errors — let them fail on import.
    Use `pytest.importorskip` only for jsonschema (external dep check).

    test_privacy.py::test_profile_not_in_egress: The test simulates egress by serializing the
    profile to JSON and asserting it does NOT appear in a mocked Telegram text payload. The test
    passes vacuously if profile file is absent (DATA-02 not yet created); use `pytest.skip` if
    TARIQ__career_radar/data/profile_cache.json doesn't exist (implementation plans create it).

    ADDITIVE ONLY: all new files; no existing files touched.
  </action>
  <verify>
    <automated>cd /d/NIZAM && python -m pytest TARIQ__career_radar/tests/ -v --tb=line 2>&1 | tail -25</automated>
  </verify>
  <done>
    All 12 tests are collected and FAIL (red phase). No syntax errors, no collection errors.
    Output shows "12 failed" or similar — ImportError/AssertionError counts as passing the Wave 0 gate.
    The exact failure reasons should match the missing implementation (ImportError for missing modules,
    FileNotFoundError for missing files, AssertionError for missing registrations).
  </done>
</task>

</tasks>

<verification>
Run full test collection:
`cd /d/NIZAM && python -m pytest TARIQ__career_radar/tests/ --collect-only -q`
Expected: 12 tests collected, 0 errors.

Run full suite to confirm all fail:
`cd /d/NIZAM && python -m pytest TARIQ__career_radar/tests/ -v --tb=short 2>&1 | grep -E "PASSED|FAILED|ERROR|collected"`
Expected: 0 PASSED, 12 FAILED or ERROR, 0 collection errors.
</verification>

<success_criteria>
- 7 test files exist (conftest.py + 6 test_*.py)
- tests/__init__.py exists
- pytest collects all 12 tests without collection errors
- All 12 tests FAIL (red phase — acceptable failure: ImportError, FileNotFoundError, AssertionError)
- No test PASSES (that would indicate a pre-existing implementation collision)
- Runtime for full suite: less than 10 seconds
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-model/01-01-SUMMARY.md`
</output>
