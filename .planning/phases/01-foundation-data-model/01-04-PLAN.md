---
phase: 01-foundation-data-model
plan: "04"
type: execute
wave: 2
depends_on:
  - "01-03"
files_modified:
  - TARIQ__career_radar/data/profile_cache.json
autonomous: true
requirements:
  - DATA-02

must_haves:
  truths:
    - "Profile seed file exists at TARIQ__career_radar/data/profile_cache.json"
    - "Profile loads via load_profile_seed() without error"
    - "Profile contains role_keywords, target_roles, and constraints keys"
    - "File is NOT tracked by git (verified by .gitignore check)"
    - "profile_cache.json is not serialized into any egress payload (Telegram/Drive mock test passes)"
  artifacts:
    - path: "TARIQ__career_radar/data/profile_cache.json"
      provides: "Seif's role keyword groups + target-role taxonomy + constraints"
      contains: "role_keywords, target_roles, constraints"
  key_links:
    - from: "TARIQ__career_radar/data/profile_cache.json"
      to: "TARIQ__career_radar/radar/config.py:load_profile_seed"
      via: "PROFILE_CACHE_PATH constant points here"
      pattern: "profile_cache.json"
    - from: "TARIQ__career_radar/data/profile_cache.json"
      to: "NIZAM__system/policies/PRIVACY_CLASSIFICATION.json"
      via: "path_glob strict_local_maximum rule (added in Plan 01-06)"
      pattern: "TARIQ__career_radar/data/profile_cache.json"
---

<objective>
Create the profile seed file (strict_local_maximum) at TARIQ__career_radar/data/profile_cache.json
with Seif's role keyword groups, target-role taxonomy, and constraints. This file is the LOCAL-ONLY
personal intelligence that the matching engine uses — it must never leave disk.

Purpose: Without the profile seed, the matching engine (Phase 7) has no basis for fit scoring.
The seed is also needed for keyword filtering in Phase 3 (role-targeted sourcing).

Output: TARIQ__career_radar/data/profile_cache.json — a single well-structured JSON file classified
as strict_local_maximum. No new Python modules. No edits to existing files.

CRITICAL SAFETY: This file is strict_local_maximum. It must not be committed to git.
The .gitignore from Plan 01-03 covers data/ entirely. Verify git status does NOT show this file
as tracked after creation.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-model/01-RESEARCH.md
@.planning/phases/01-foundation-data-model/01-03-SUMMARY.md

<interfaces>
<!-- Profile seed shape (from RESEARCH.md DATA-02 section): -->
{
  "version": "1.0",
  "profile_owner": "Seif Elsherbiny",
  "privacy_class": "strict_local_maximum",
  "created_at": "<ISO 8601 UTC>",
  "last_updated": "<ISO 8601 UTC>",
  "role_keywords": {
    "AI_OPERATIONS": ["ai operations manager", "ai ops engineer", "operations lead", "program manager", "coordination"],
    "DATA_SCIENCE": ["data scientist", "machine learning engineer", "analytics engineer"],
    "AI_RESEARCH": ["ai researcher", "research scientist", "llm researcher"],
    "LLM_EVALUATION": ["llm evaluator", "ai evaluation specialist", "prompt engineer"],
    "DATA_ANNOTATION": ["data annotator", "annotation specialist", "rlhf trainer"],
    "GROWTH_ANALYST": ["growth analyst", "growth engineer", "product analyst"],
    "BUSINESS_ANALYST": ["business analyst", "data analyst", "bi analyst"],
    "PROJECT_COORDINATOR": ["project coordinator", "program coordinator", "operations coordinator"]
  },
  "target_roles": [
    {
      "category": "AI_OPERATIONS",
      "title_patterns": ["AI Operations", "AI Ops", "Operations Manager", "Program Manager"],
      "required_skills": ["project management", "communication", "cross-functional"],
      "nice_to_have": ["python", "data analysis", "llm tooling"],
      "avoid_flags": ["sales", "non-technical", "cold calling"]
    },
    {
      "category": "LLM_EVALUATION",
      "title_patterns": ["LLM Evaluator", "AI Evaluation", "Prompt Engineer", "RLHF"],
      "required_skills": ["critical thinking", "writing quality assessment", "instruction following"],
      "nice_to_have": ["python", "ml basics"],
      "avoid_flags": ["unpaid", "below minimum wage equivalent"]
    }
  ],
  "experience_summary": {
    "years_total": 2,
    "specializations": ["AI operations", "coordination", "data analysis", "LLM evaluation"],
    "technical_skills": ["Python", "SQL", "data analysis", "prompt engineering"],
    "soft_skills": ["communication", "project management", "strategic thinking", "Arabic-English bilingual"],
    "languages": ["Arabic (native)", "English (fluent)"]
  },
  "constraints": {
    "location_preference": "remote",
    "visa_sponsorship_needed": true,
    "minimum_salary_usd": 60000,
    "currency_preference": "USD",
    "work_authorization": "Egyptian, seeking sponsorship"
  },
  "red_flags": [
    "unpaid work",
    "exploitative gig platforms",
    "unclear pay with no provenance",
    "no visa sponsorship without relocation offer"
  ],
  "notes": "Phase 1 bootstrap version — enriched during Phase 7 (TAG-02) with operator review"
}

<!-- load_profile_seed() signature (from Plan 01-03 config.py): -->
def load_profile_seed() -> dict:
    # raises ValueError if file missing or required keys absent
    # required keys: {"role_keywords", "target_roles", "constraints"}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create profile_cache.json and verify DATA-02 tests pass</name>
  <files>TARIQ__career_radar/data/profile_cache.json</files>
  <action>
    Create TARIQ__career_radar/data/profile_cache.json using the exact JSON shape from the
    interfaces block above. Use today's date (2026-06-14T12:00:00Z) for created_at and last_updated.

    PRIVACY CONSTRAINTS — verify all before finishing:
    1. File is created at TARIQ__career_radar/data/profile_cache.json (inside data/ which is gitignored)
    2. Verify git does NOT track it: `git status TARIQ__career_radar/data/profile_cache.json` must show
       "nothing to commit" or the file must be listed under "Ignored files" — not under "Untracked files"
       (untracked is OK; tracked would be a privacy breach)
    3. DO NOT add any personal salary amounts beyond the minimum_salary_usd already in the research
       (60000 is the placeholder from RESEARCH.md; do not add real bank account info or exact current salary)

    The file MUST validate that load_profile_seed() can load it:
    `cd /d/NIZAM && python -c "from TARIQ__career_radar.radar.config import load_profile_seed; p=load_profile_seed(); print('loaded OK, keys:', list(p.keys()))"`

    ADDITIVE ONLY: new file in data/ which is an already-gitignored directory.
    Do not modify any existing file.
  </action>
  <verify>
    <automated>cd /d/NIZAM && python -m pytest TARIQ__career_radar/tests/test_config.py -v 2>&1 | tail -15</automated>
  </verify>
  <done>
    Both DATA-02 tests PASS:
    - test_profile_seed_load: PASSED (loads file, returns dict with role_keywords/target_roles/constraints)
    - test_profile_seed_missing_raises: PASSED (still passes — tests it in a tmp env)
    File is NOT tracked by git.
  </done>
</task>

</tasks>

<verification>
File loads correctly:
`cd /d/NIZAM && python -c "from TARIQ__career_radar.radar.config import load_profile_seed; p=load_profile_seed(); assert 'role_keywords' in p and 'constraints' in p; print('profile OK')"`

Git does not track the file:
`cd /d/NIZAM && git check-ignore -v TARIQ__career_radar/data/profile_cache.json`
Expected: output shows a matching .gitignore rule (e.g. "TARIQ__career_radar/.gitignore:1:data/")

DATA-02 tests:
`cd /d/NIZAM && python -m pytest TARIQ__career_radar/tests/test_config.py -v`
Expected: 2 passed.
</verification>

<success_criteria>
- TARIQ__career_radar/data/profile_cache.json exists and is valid JSON
- File has keys: version, profile_owner, privacy_class, role_keywords, target_roles, constraints, red_flags
- privacy_class field is "strict_local_maximum"
- load_profile_seed() returns the profile without error
- git check-ignore confirms file is gitignored (not tracked)
- pytest TARIQ__career_radar/tests/test_config.py: 2 passed
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-model/01-04-SUMMARY.md`
</output>
