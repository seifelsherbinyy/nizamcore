---
phase: 01-foundation-data-model
plan: "06"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
  - "01-04"
  - "01-05"
files_modified:
  - NIZAM_TEMPLE.json
  - NIZAM__system/governor/ledger_writer.py
  - NIZAM__system/policies/PRIVACY_CLASSIFICATION.json
  - NIZAM__system/ledgers/CAREER_RADAR_LEDGER.jsonl
autonomous: true
requirements:
  - DATA-05

must_haves:
  truths:
    - "CAREER_RADAR_LEDGER appears in KNOWN_LEDGERS set in ledger_writer.py"
    - "NIZAM_TEMPLE.json ledgers section contains CAREER_RADAR_LEDGER entry"
    - "PRIVACY_CLASSIFICATION.json has 3 new TARIQ path rules (profile strict_local_maximum, data strict_local, ledger review_before_commit)"
    - "CAREER_RADAR_LEDGER.jsonl exists as an empty (genesis-ready) file"
    - "ledger_writer.append('CAREER_RADAR_LEDGER', {...}) succeeds without ValueError"
    - "Pre-commit hook would block TARIQ__career_radar/data/profile_cache.json from git commit"
  artifacts:
    - path: "NIZAM__system/ledgers/CAREER_RADAR_LEDGER.jsonl"
      provides: "Empty append-only ledger file for career radar runs"
    - path: "NIZAM_TEMPLE.json"
      provides: "CAREER_RADAR_LEDGER registered in ledgers section"
      contains: "CAREER_RADAR_LEDGER"
    - path: "NIZAM__system/governor/ledger_writer.py"
      provides: "CAREER_RADAR_LEDGER in KNOWN_LEDGERS set"
      contains: "CAREER_RADAR_LEDGER"
    - path: "NIZAM__system/policies/PRIVACY_CLASSIFICATION.json"
      provides: "3 new path_glob rules for TARIQ module"
      contains: "TARIQ__career_radar"
  key_links:
    - from: "NIZAM__system/governor/ledger_writer.py:KNOWN_LEDGERS"
      to: "NIZAM__system/ledgers/CAREER_RADAR_LEDGER.jsonl"
      via: "_ledger_path() validates name is in KNOWN_LEDGERS before returning path"
      pattern: "CAREER_RADAR_LEDGER"
    - from: "NIZAM__system/policies/PRIVACY_CLASSIFICATION.json"
      to: "NIZAM__system/governor/classifier.py"
      via: "classifier._load_rules() reads PRIVACY_CLASSIFICATION.json on first call"
      pattern: "TARIQ__career_radar"
---

<objective>
Complete the ledger registration ceremony: 3 minimal additive edits to existing NIZAM files +
create the empty ledger file. This is the final DATA-05 requirement. After this plan, all
Phase 1 tests should be GREEN.

Purpose: Without ledger registration, any downstream plan that calls
`ledger_writer.append("CAREER_RADAR_LEDGER", ...)` will raise ValueError. Without the
PRIVACY_CLASSIFICATION rules, the pre-commit hook won't block strict_local files from git.

CRITICAL SAFETY: This plan touches 3 LIVE NIZAM files. Every edit is ADDITIVE ONLY:
- NIZAM_TEMPLE.json: add one key to "ledgers" object — do not touch any other key
- ledger_writer.py: add one string to KNOWN_LEDGERS set literal — do not touch any other line
- PRIVACY_CLASSIFICATION.json: append 3 entries to "rules" array — before the closing bracket

If you accidentally delete or overwrite existing content in any of these files, the live
NIZAM system (Telegram relay, existing ledgers, privacy enforcement) will break.
READ EACH FILE FIRST. MAKE THE SMALLEST POSSIBLE TARGETED EDIT.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-data-model/01-RESEARCH.md
@.planning/phases/01-foundation-data-model/01-02-SUMMARY.md
@.planning/phases/01-foundation-data-model/01-03-SUMMARY.md

<interfaces>
<!-- NIZAM_TEMPLE.json ledgers section — add exactly this key inside the "ledgers" object: -->
"CAREER_RADAR_LEDGER": {
  "path": "NIZAM__system/ledgers/CAREER_RADAR_LEDGER.jsonl",
  "phase": 1,
  "privacy": "review_before_commit",
  "owner": "Tariq",
  "purpose": "Career radar run events, opportunity counts, delivery status, error tracking"
}
<!-- Add after the last existing ledger entry (currently "BODY_LEDGER"), before the closing } of "ledgers" -->

<!-- ledger_writer.py KNOWN_LEDGERS — current set (lines 29-40): -->
KNOWN_LEDGERS = {
    "EVENT_LEDGER",
    "DECISION_LEDGER",
    "LEARNING_LEDGER",
    "DEAD_LETTER",
    "STRATEGY_LEDGER",
    "BATTLE_LEDGER",
    "FINANCE_LEDGER",
    "BODY_LEDGER",
    "PULSATION_LEDGER",
    "COUNCIL_LEDGER",
}
<!-- Add ONE line inside the set, after "COUNCIL_LEDGER": -->
    "CAREER_RADAR_LEDGER",  # TARIQ Career Radar run log

<!-- PRIVACY_CLASSIFICATION.json rules array — add 3 entries before the closing ] of "rules": -->
{ "path_glob": "TARIQ__career_radar/data/profile_cache.json", "classification": "strict_local_maximum" },
{ "path_glob": "TARIQ__career_radar/data/**",                 "classification": "strict_local" },
{ "path_glob": "NIZAM__system/ledgers/CAREER_RADAR_LEDGER.jsonl", "classification": "review_before_commit" }
<!-- Note: insert these BEFORE the closing bracket ] of the rules array -->
<!-- The last line should NOT have a trailing comma (or the file won't be valid JSON) -->
<!-- Insert after the last existing rule (currently "log.md" rule at line 43) -->

<!-- Existing governor tests to verify after edits (DO NOT modify): -->
NIZAM__system/governor/tests/test_ledger_writer.py  (existing — must still pass)
NIZAM__system/governor/tests/test_classifier_fixture.py  (existing — must still pass)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit the 3 shared NIZAM files (additive-only registration ceremony)</name>
  <files>
    NIZAM_TEMPLE.json,
    NIZAM__system/governor/ledger_writer.py,
    NIZAM__system/policies/PRIVACY_CLASSIFICATION.json
  </files>
  <action>
    Execute the 3 additive edits described in the interfaces block. STRICT RULES:

    Step A — NIZAM_TEMPLE.json:
    1. Read the FULL file first (it is ~280 lines — read it all)
    2. Find the "ledgers" object (currently ends with "BODY_LEDGER" entry at ~line 220)
    3. Add the CAREER_RADAR_LEDGER key-value pair after "BODY_LEDGER" entry, inside the "ledgers" object
    4. Verify the resulting JSON is valid: `python -c "import json; json.load(open('NIZAM_TEMPLE.json', encoding='utf-8')); print('valid')"`
    5. Verify no existing key was removed: `python -c "import json; t=json.load(open('NIZAM_TEMPLE.json',encoding='utf-8')); assert 'BODY_LEDGER' in t['ledgers'] and 'EVENT_LEDGER' in t['ledgers']; print('existing keys intact')"`

    Step B — NIZAM__system/governor/ledger_writer.py:
    1. Read the file (it is ~120 lines — read it all)
    2. Find the KNOWN_LEDGERS set literal (lines 29-40)
    3. Add the single line `    "CAREER_RADAR_LEDGER",  # TARIQ Career Radar run log`
       AFTER the "COUNCIL_LEDGER" line, BEFORE the closing `}`
    4. Verify: `python -c "from NIZAM__system.governor.ledger_writer import KNOWN_LEDGERS; assert 'CAREER_RADAR_LEDGER' in KNOWN_LEDGERS; assert 'COUNCIL_LEDGER' in KNOWN_LEDGERS; print('KNOWN_LEDGERS OK, count:', len(KNOWN_LEDGERS))"`

    Step C — NIZAM__system/policies/PRIVACY_CLASSIFICATION.json:
    1. Read the full file (currently ~45 lines)
    2. Find the end of the "rules" array (currently the last entry is the "log.md" rule at line 43)
    3. Add a trailing comma after the "log.md" entry, then append the 3 new rules, then close the array
    4. Verify valid JSON: `python -c "import json; d=json.load(open('NIZAM__system/policies/PRIVACY_CLASSIFICATION.json',encoding='utf-8')); globs=[r['path_glob'] for r in d['rules']]; assert 'TARIQ__career_radar/data/profile_cache.json' in globs; assert 'TARIQ__career_radar/data/**' in globs; print('rules OK, total rules:', len(d['rules']))"`

    NEVER use string replacement on these files — always read-full, edit precisely, write back.
    If any step fails validation, STOP and report the error; do not proceed.
  </action>
  <verify>
    <automated>cd /d/NIZAM && python -c "
import json, sys
# Verify NIZAM_TEMPLE.json
t = json.load(open('NIZAM_TEMPLE.json', encoding='utf-8'))
assert 'CAREER_RADAR_LEDGER' in t['ledgers'], 'missing in TEMPLE'
assert 'BODY_LEDGER' in t['ledgers'], 'BODY_LEDGER removed — CRITICAL'
# Verify ledger_writer.py
from NIZAM__system.governor.ledger_writer import KNOWN_LEDGERS
assert 'CAREER_RADAR_LEDGER' in KNOWN_LEDGERS, 'missing in KNOWN_LEDGERS'
assert 'COUNCIL_LEDGER' in KNOWN_LEDGERS, 'COUNCIL_LEDGER removed — CRITICAL'
# Verify PRIVACY_CLASSIFICATION.json
pc = json.load(open('NIZAM__system/policies/PRIVACY_CLASSIFICATION.json', encoding='utf-8'))
globs = [r['path_glob'] for r in pc['rules']]
assert 'TARIQ__career_radar/data/profile_cache.json' in globs
assert 'TARIQ__career_radar/data/**' in globs
assert 'NIZAM__system/ledgers/CAREER_RADAR_LEDGER.jsonl' in globs
print('ALL THREE EDITS VERIFIED OK')
"</automated>
  </verify>
  <done>
    All 3 files edited correctly. CAREER_RADAR_LEDGER appears in TEMPLE.json, KNOWN_LEDGERS,
    and PRIVACY_CLASSIFICATION.json. All existing content preserved.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create empty ledger file + run full DATA-05 test suite</name>
  <files>NIZAM__system/ledgers/CAREER_RADAR_LEDGER.jsonl</files>
  <action>
    Create NIZAM__system/ledgers/CAREER_RADAR_LEDGER.jsonl as an EMPTY file (zero bytes).
    The ledger_writer.append() function handles genesis row creation on first real write.
    Do NOT pre-populate it with any rows.

    Note: NIZAM__system/ledgers/ has a .gitignore that blocks all .jsonl files. Verify:
    `git check-ignore -v NIZAM__system/ledgers/CAREER_RADAR_LEDGER.jsonl`
    Expected: matching rule from NIZAM__system/ledgers/.gitignore or root .gitignore.

    Then run the full DATA-05 test battery:
    `cd /d/NIZAM && python -m pytest TARIQ__career_radar/tests/test_registration.py TARIQ__career_radar/tests/test_privacy.py -v`

    Also verify existing NIZAM governor tests still pass:
    `cd /d/NIZAM && python -m pytest NIZAM__system/governor/tests/ -v --tb=short`
    This confirms the 3 live-file edits did not break existing behavior.
  </action>
  <verify>
    <automated>cd /d/NIZAM && python -m pytest TARIQ__career_radar/tests/test_registration.py TARIQ__career_radar/tests/test_privacy.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>
    CAREER_RADAR_LEDGER.jsonl exists (empty).
    test_registration.py::test_ledger_registered: PASSED.
    test_privacy.py::test_privacy_rules_defined: PASSED.
    test_privacy.py::test_profile_not_in_egress: PASSED.
    Existing NIZAM governor tests still pass (no regressions from shared-file edits).
  </done>
</task>

</tasks>

<verification>
Full Phase 1 test suite — all tests green:
`cd /d/NIZAM && python -m pytest TARIQ__career_radar/tests/ -v 2>&1 | tail -20`
Expected: 12 passed, 0 failed.

Ledger write smoke test:
`cd /d/NIZAM && python -c "
from NIZAM__system.governor.ledger_writer import append
row = append('CAREER_RADAR_LEDGER', {'note': 'Phase 1 registration test', 'module': 'TARIQ__career_radar', 'event_type': 'phase_1_registration_smoke_test'}, actor='Tariq', action='phase_test')
print('ledger append OK:', row['row_id'])
"`
Expected: Prints a row_id UUID, no ValueError.

Existing governor tests still pass:
`cd /d/NIZAM && python -m pytest NIZAM__system/governor/tests/ -v --tb=short 2>&1 | tail -10`
Expected: All previously-passing tests still pass.

Privacy classification check:
`cd /d/NIZAM && python -c "from NIZAM__system.governor.classifier import classify; assert classify('TARIQ__career_radar/data/profile_cache.json') == 'strict_local_maximum'; print('privacy OK')"`
</verification>

<success_criteria>
- NIZAM_TEMPLE.json: CAREER_RADAR_LEDGER in ledgers, all existing ledgers preserved
- ledger_writer.py: CAREER_RADAR_LEDGER in KNOWN_LEDGERS, all existing ledgers preserved
- PRIVACY_CLASSIFICATION.json: 3 new TARIQ rules added, all existing rules preserved
- CAREER_RADAR_LEDGER.jsonl: exists as empty file, gitignored
- ledger_writer.append("CAREER_RADAR_LEDGER", {...}) succeeds without error
- pytest TARIQ__career_radar/tests/: 12 passed, 0 failed
- pytest NIZAM__system/governor/tests/: all previously-passing tests still pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-model/01-06-SUMMARY.md`
</output>
