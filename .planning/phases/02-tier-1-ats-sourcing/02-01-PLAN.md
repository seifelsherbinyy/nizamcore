---
phase: 02-tier-1-ats-sourcing
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - TARIQ__career_radar/tests/fixtures/greenhouse_sample_response.json
  - TARIQ__career_radar/tests/fixtures/lever_sample_response.json
  - TARIQ__career_radar/tests/fixtures/ashby_sample_response.json
  - TARIQ__career_radar/tests/fixtures/workable_sample_response.json
  - TARIQ__career_radar/conftest.py
  - TARIQ__career_radar/tests/test_sources.py
autonomous: true
requirements:
  - SRC-01
  - SRC-04
  - SRC-05
must_haves:
  truths:
    - "pytest TARIQ__career_radar/tests/test_sources.py collects 11 tests and all fail RED (not collection errors)"
    - "No network calls are made during the test run — all HTTP is mocked via monkeypatch/fake transport"
    - "Fixture JSON files exist at tests/fixtures/ for all four ATS platforms"
    - "conftest.py provides mock_requests_* fixtures that inject fake HTTP without touching production code"
  artifacts:
    - path: "TARIQ__career_radar/tests/fixtures/greenhouse_sample_response.json"
      provides: "Recorded Greenhouse API response shape"
    - path: "TARIQ__career_radar/tests/fixtures/lever_sample_response.json"
      provides: "Recorded Lever API response shape"
    - path: "TARIQ__career_radar/tests/fixtures/ashby_sample_response.json"
      provides: "Recorded Ashby API response shape"
    - path: "TARIQ__career_radar/tests/fixtures/workable_sample_response.json"
      provides: "Recorded Workable API response shape"
    - path: "TARIQ__career_radar/tests/test_sources.py"
      provides: "11 failing TDD tests for SRC-01/04/05"
      contains: "test_greenhouse_fetch_mocked, test_lever_fetch_mocked, test_ashby_fetch_mocked, test_workable_fetch_mocked"
  key_links:
    - from: "tests/test_sources.py"
      to: "tests/fixtures/*.json"
      via: "conftest.py fixture loading via Path(__file__).parent / 'fixtures'"
    - from: "conftest.py mock_requests_greenhouse"
      to: "radar.sources.greenhouse_source.requests.get"
      via: "monkeypatch.setattr"
---

<objective>
Wave 0 TDD scaffold for Phase 2: create all fixture JSON files, extend conftest.py with fake-HTTP transport fixtures, and write test_sources.py with all 11 tests in RED state. No connector code yet — tests must fail with ImportError (collected, not crashed).

Purpose: Establish the test contract before any production code so all 11 ATS source behaviors are verifiable offline.
Output: 6 files. 11 collectible failing tests. Zero network calls.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tier-1-ats-sourcing/02-RESEARCH.md
@.planning/phases/02-tier-1-ats-sourcing/02-VALIDATION.md

<interfaces>
<!-- Phase 1 contracts the executor must NOT break -->

From TARIQ__career_radar/conftest.py (existing — EXTEND, do not replace):
```python
# _REPO = Path(__file__).resolve().parents[1]  (repo root)
# Fixtures: repo_root, schema_path, tmp_db_path, sample_opportunity, sample_profile
# sample_opportunity has keys: opportunity_id, title, company, location, remote_status,
#   source, source_type, source_url, access_date, fit_score, growth_score, confidence,
#   tags, salary_usd_low, salary_usd_high, salary_evidence_type, salary_confidence,
#   observed_at, lane, data_quality
```

From .planning/phases/02-tier-1-ats-sourcing/02-VALIDATION.md (11 test names):
```
test_greenhouse_fetch_mocked        # SRC-01
test_lever_fetch_mocked             # SRC-01
test_ashby_fetch_mocked             # SRC-01
test_workable_fetch_mocked          # SRC-01
test_normalization_to_schema        # SRC-04
test_required_fields_present        # SRC-04
test_salary_confidence_tagging      # SRC-04
test_fetch_network_error_graceful   # SRC-05
test_429_rate_limit_handled         # SRC-05
test_blocked_sources_manifest       # SRC-05
test_zero_results_graceful          # SRC-05
```
</interfaces>
</context>

<feature>
  <name>Wave 0: ATS source test scaffold (fixtures + conftest + failing tests)</name>
  <files>
    TARIQ__career_radar/tests/fixtures/greenhouse_sample_response.json,
    TARIQ__career_radar/tests/fixtures/lever_sample_response.json,
    TARIQ__career_radar/tests/fixtures/ashby_sample_response.json,
    TARIQ__career_radar/tests/fixtures/workable_sample_response.json,
    TARIQ__career_radar/conftest.py,
    TARIQ__career_radar/tests/test_sources.py
  </files>
  <behavior>
    Wave 0 TDD — all tests must be COLLECTIBLE but FAILING (RED). Use the same
    _require_module() / try-except ImportError pattern from Phase 1 Plan 01-01
    (importable but fails on missing module, not collection error).

    Fixture JSON files — use these exact shapes (from 02-RESEARCH.md):

    greenhouse_sample_response.json:
    {
      "jobs": [
        {
          "id": 123456,
          "title": "AI Operations Manager",
          "location": {"name": "Remote"},
          "company_name": "Acme Corp",
          "absolute_url": "https://boards.greenhouse.io/acme/jobs/123456",
          "content": "<p>Description</p>",
          "salary_min": 90000,
          "salary_max": 110000,
          "salary_currency": "USD"
        }
      ]
    }

    lever_sample_response.json:
    [
      {
        "id": "abc-lever-123",
        "text": "Data Analyst",
        "url": "https://jobs.acme.lever.co/apply/abc-lever-123",
        "categories": {
          "location": "Remote",
          "commitment": "Full-time",
          "team": "Data",
          "level": "Mid-level"
        },
        "createdAt": 1623052800000,
        "updatedAt": 1686398400000
      }
    ]

    ashby_sample_response.json:
    {
      "jobPostings": [
        {
          "id": "uuid-ashby-001",
          "title": "ML Platform Engineer",
          "country": "United States",
          "state": "CA",
          "location": "Remote",
          "remotePolicy": "fully_remote",
          "url": "https://jobs.ashbyhq.com/acme/uuid-ashby-001",
          "compensation": {
            "salary": {"min": 120000, "max": 160000, "currency": "USD"}
          },
          "updatedAt": "2026-06-14T10:00:00Z",
          "department": "Engineering"
        }
      ]
    }

    workable_sample_response.json:
    {
      "name": "Acme Corp",
      "url": "https://acme.workable.com",
      "jobs": [
        {
          "id": "job-wk-001",
          "title": "AI Ops Coordinator",
          "location": {
            "country": "United States",
            "region": "Remote",
            "city": null
          },
          "job_url": "https://acme.workable.com/jobs/wk-001",
          "published_on": "2026-06-14",
          "updated_on": "2026-06-14"
        }
      ]
    }

    conftest.py additions (append after existing fixtures — do NOT touch existing ones):
    - Fixture `fixtures_dir` returning Path(__file__).parent / "tests" / "fixtures"
    - Fixture `mock_greenhouse_response(fixtures_dir)` loading greenhouse_sample_response.json
    - Fixture `mock_lever_response(fixtures_dir)` loading lever_sample_response.json
    - Fixture `mock_ashby_response(fixtures_dir)` loading ashby_sample_response.json
    - Fixture `mock_workable_response(fixtures_dir)` loading workable_sample_response.json
    - Fixture `fake_requests_get` returning a factory function:
        def make_fake_get(status_code=200, json_data=None, raise_exc=None):
            def fake_get(*args, **kwargs):
                if raise_exc:
                    raise raise_exc
                resp = unittest.mock.MagicMock()
                resp.status_code = status_code
                resp.json.return_value = json_data or {}
                resp.raise_for_status.side_effect = (
                    None if status_code < 400 else
                    requests.exceptions.HTTPError(f"{status_code}")
                )
                return resp
            return fake_get
      (imports needed at top of conftest additions: import json, unittest.mock, requests)

    test_sources.py structure:
    - Use _require_module() wrapper (try/except ImportError → pytest.fail with
      "MISSING — implement in Wave 1/2") for all imports of:
        radar.sources.base (SourceResult, OpportunityRaw, BaseSource)
        radar.sources.greenhouse_source (GreenhouseSource)
        radar.sources.lever_source (LeverSource)
        radar.sources.ashby_source (AshbySource)
        radar.sources.workable_source (WorkableSource)
        radar.stages.fetch (run_fetch)

    All 11 tests follow this pattern:
    1. Call _require_module() at top of each test → fails RED with ImportError message
    2. No actual assertions run yet (unreachable code after _require_module fails)

    Exact test names matching 02-VALIDATION.md:
    - test_greenhouse_fetch_mocked(mock_greenhouse_response, monkeypatch)
    - test_lever_fetch_mocked(mock_lever_response, monkeypatch)
    - test_ashby_fetch_mocked(mock_ashby_response, monkeypatch)
    - test_workable_fetch_mocked(mock_workable_response, monkeypatch)
    - test_normalization_to_schema(mock_greenhouse_response, monkeypatch)
    - test_required_fields_present(mock_greenhouse_response, monkeypatch)
    - test_salary_confidence_tagging(mock_greenhouse_response, mock_ashby_response, monkeypatch)
    - test_fetch_network_error_graceful(monkeypatch)
    - test_429_rate_limit_handled(monkeypatch)
    - test_blocked_sources_manifest(monkeypatch)
    - test_zero_results_graceful(monkeypatch)

    Each test body after the _require_module guard should contain the FULL assertion
    logic (the GREEN contract), written as dead code now, so Wave 1/2 implementors
    know exactly what to satisfy. Document with a comment:
        # --- RED: imports fail above; assertions below are the GREEN contract ---

    Example for test_greenhouse_fetch_mocked:
    ```python
    def test_greenhouse_fetch_mocked(mock_greenhouse_response, monkeypatch):
        GreenhouseSource = _require_module("radar.sources.greenhouse_source", "GreenhouseSource")
        import requests
        monkeypatch.setattr(requests, "get", make_fake_get(200, mock_greenhouse_response))
        src = GreenhouseSource({"board_token": "acme", "enabled": True})
        result = src.fetch({})
        assert result.source_name == "greenhouse"
        assert len(result.opportunities) == 1
        assert result.opportunities[0].title == "AI Operations Manager"
        assert result.errors == []
        assert result.rate_limited is False
    ```

    Assertions inside each test (the GREEN contract to write now):
    - test_greenhouse_fetch_mocked: result.source_name=="greenhouse", len(opps)==1,
        opps[0].title=="AI Operations Manager", errors==[], rate_limited==False
    - test_lever_fetch_mocked: result.source_name=="lever", len(opps)==1,
        opps[0].title=="Data Analyst", errors==[], rate_limited==False
    - test_ashby_fetch_mocked: result.source_name=="ashby", len(opps)==1,
        opps[0].title=="ML Platform Engineer", errors==[], rate_limited==False
    - test_workable_fetch_mocked: result.source_name=="workable", len(opps)==1,
        opps[0].title=="AI Ops Coordinator", errors==[], rate_limited==False
    - test_normalization_to_schema: normalized dict has all required keys:
        opportunity_id, title, company, location, remote_status, source, source_type,
        source_url, access_date, salary_usd_low, salary_usd_high, salary_evidence_type,
        salary_confidence, fit_score, growth_score, confidence, tags, lane, observed_at
    - test_required_fields_present: normalized["source"]=="greenhouse",
        normalized["source_type"]=="ats", normalized["source_url"] starts with "https://",
        normalized["access_date"] ends with "Z"
    - test_salary_confidence_tagging: greenhouse opp salary_confidence=="HIGH" (has salary),
        ashby opp salary_confidence=="HIGH", lever opp salary_confidence=="LOW"
    - test_fetch_network_error_graceful: after injecting Timeout, result.errors non-empty,
        "timeout" in result.errors[0].lower(), result.opportunities==[]
    - test_429_rate_limit_handled: after injecting 429 response, result.rate_limited==True,
        result.opportunities==[], result.errors non-empty
    - test_blocked_sources_manifest: run_fetch({}, "test-run-id") returns dict with
        "blocked_sources" key; if source fails, it appears in blocked_sources list
        with keys "source", "errors"; other sources continue
    - test_zero_results_graceful: run_fetch with all sources disabled/empty returns
        {"opportunities": [], "blocked_sources": [...], "fetch_summary": {...}} without raising
  </behavior>
  <implementation>
    Create tests/fixtures/ directory and four JSON files.
    Append HTTP mock fixtures to conftest.py (keep existing fixtures intact).
    Create tests/test_sources.py with _require_module helper + 11 failing tests.
    Run pytest to confirm exactly 11 tests collected, all FAILED (not ERROR).
  </implementation>
</feature>

<verification>
<automated>cd D:\NIZAM && python -m pytest TARIQ__career_radar/tests/test_sources.py -v 2>&1 | tail -20</automated>
Expected: "11 failed" in summary line. No "ERROR" lines (collection errors would mean the scaffold is broken). No network calls (grep test output for "requests.get" being called with real URLs).
</verification>

<success_criteria>
- `pytest TARIQ__career_radar/tests/test_sources.py -v` collects and FAILS exactly 11 tests
- No collection errors (all 11 show "FAILED", none "ERROR")
- `pytest TARIQ__career_radar/tests/ -v` still shows Phase 1's 13 tests GREEN (not broken by this plan)
- tests/fixtures/ directory contains exactly 4 JSON files matching the ATS response shapes
- conftest.py has fake_requests_get factory and four mock_*_response fixtures
</success_criteria>

<output>
After completion, create `.planning/phases/02-tier-1-ats-sourcing/02-01-SUMMARY.md`
</output>
