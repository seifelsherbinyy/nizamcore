---
phase: 04-deduplication-engine
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - TARIQ__career_radar/tests/fixtures/dedup_test_data.jsonl
  - TARIQ__career_radar/conftest.py
  - TARIQ__career_radar/tests/test_dedup_engine.py
autonomous: true
requirements:
  - DEDUP-01
  - DEDUP-02
  - DEDUP-03

must_haves:
  truths:
    - "All 6 new Phase-4 tests are collectible by pytest and fail RED (not collection error)"
    - "dedup_test_data.jsonl provides 50+ role pairs with title variants (same role, different wording)"
    - "conftest.py augmentation adds Phase-4 dedup fixtures WITHOUT breaking existing Phase 1-3 fixtures"
    - "No real network calls are made during pytest collection or execution"
    - "Existing test_dedup_engine.py tests (3 Phase-1 tests) remain passing GREEN after augmentation"
  artifacts:
    - path: "TARIQ__career_radar/tests/fixtures/dedup_test_data.jsonl"
      provides: "50+ synthetic opportunity records: title variants, same-company pairs, cross-source duplicates"
    - path: "TARIQ__career_radar/conftest.py"
      provides: "Phase-4 fixtures: dedup_opp_pairs, dedup_fresh_record, cross_source_batch"
    - path: "TARIQ__career_radar/tests/test_dedup_engine.py"
      provides: "6 failing RED tests for DEDUP-01 (key), DEDUP-02 (fuzzy), DEDUP-03 (rerun + freshness)"
  key_links:
    - from: "TARIQ__career_radar/tests/test_dedup_engine.py"
      to: "TARIQ__career_radar.radar.dedup_engine (fuzzy_match_opportunities, is_fresh_repost, run_dedup_pass)"
      via: "_require_module() import guard — fails RED until Wave 1/2 implementation"
      pattern: "fuzzy_match_opportunities|is_fresh_repost|run_dedup_pass"
    - from: "TARIQ__career_radar/conftest.py"
      to: "TARIQ__career_radar/tests/fixtures/dedup_test_data.jsonl"
      via: "dedup_opp_pairs fixture loads JSONL and returns list[dict]"
      pattern: "dedup_test_data\\.jsonl"
---

<objective>
Wave 0 TDD scaffold for Phase 4: record the dedup fixture dataset and write 6 failing tests that define the acceptance contracts for fuzzy matching (DEDUP-02), seen-store rerun prevention (DEDUP-03), and freshness rule (DEDUP-03). Tests must be collectible but fail RED until Wave 1/2 adds `fuzzy_match_opportunities()`, `is_fresh_repost()`, and `run_dedup_pass()` to dedup_engine.py.

Purpose: Establish RED baseline so Wave 1 implementation turns tests GREEN with precise measurable contracts. Avoids any architectural ambiguity in what counts as a duplicate.

Output: 1 fixture JSONL file, augmented conftest.py (Phase-4 section), and 6 new test functions appended to test_dedup_engine.py — all failing RED.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-deduplication-engine/04-RESEARCH.md
@.planning/phases/04-deduplication-engine/04-VALIDATION.md

<interfaces>
<!-- Existing Phase 1 dedup_engine.py — executor must NOT modify these -->
<!-- Only the NEW functions (fuzzy_match_opportunities, is_fresh_repost, run_dedup_pass) are absent -->

From TARIQ__career_radar/radar/dedup_engine.py:
```python
def normalize_title(title: str) -> str: ...
def normalize_company(company: str) -> str: ...
def normalize_location(location: str) -> str: ...
def compute_dedup_key(title: str, company: str, location: str) -> tuple[str, str, str]: ...

class DedupeEngine:
    def __init__(self, db_path: Path) -> None: ...
    def check_or_add(self, opportunity: dict[str, Any]) -> dict[str, Any]:
        # Returns: {"is_duplicate": bool, "key": str, "hit_count": int}
```

From TARIQ__career_radar/conftest.py (existing fixtures — do NOT replace):
```python
@pytest.fixture def tmp_db_path(tmp_path: Path) -> Path: ...
@pytest.fixture def sample_opportunity() -> dict: ...
# Also: repo_root, schema_path, sample_profile, fixtures_dir,
#        mock_greenhouse_response, mock_lever_response, mock_ashby_response,
#        mock_workable_response, fake_requests_get,
#        mock_remotive_rss, mock_weworkremotely_rss, mock_remoteok_response,
#        manual_import_fixture, fake_rss_bytes_get, synthetic_profile_seed
```

From TARIQ__career_radar/tests/test_dedup_engine.py (existing Phase-1 tests — keep all 3):
```python
def test_sqlite_roundtrip(tmp_db_path, sample_opportunity): ...      # DATA-03 ✓ GREEN
def test_normalization_deterministic(): ...                           # DATA-03 ✓ GREEN
def test_persistence_across_restarts(tmp_db_path, sample_opportunity): ...  # DATA-03 ✓ GREEN
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create dedup_test_data.jsonl fixture and Phase-4 conftest fixtures</name>
  <files>TARIQ__career_radar/tests/fixtures/dedup_test_data.jsonl, TARIQ__career_radar/conftest.py</files>
  <behavior>
    - dedup_test_data.jsonl must contain >=50 synthetic opportunity records
    - Records come in PAIRS: same underlying role, different title wording (e.g. "AI Ops Manager" / "AI Operations Manager", "Data Scientist" / "Data Science Lead", "LLM Evaluator" / "LLM Trainer Evaluator")
    - Each record has: title, company, location, source, access_date fields
    - Include at least 5 cross-source pairs (same role appearing in "greenhouse" and "remotive")
    - Include at least 3 records with access_date >30 days apart (for freshness rule tests)
    - No real personal data; all companies are synthetic (e.g. "Acme Corp", "Beta Inc", "Gamma LLC")
    - conftest.py Phase-4 section adds three new fixtures APPENDED after the Phase-3 section:
      * dedup_opp_pairs(fixtures_dir) -> list[dict]: loads dedup_test_data.jsonl, returns all records
      * dedup_fresh_record() -> dict: returns a dict mimicking a SQLite seen_roles row with first_seen_date 45 days ago and last_seen_date today (for freshness rule tests); use datetime.datetime.utcnow() - timedelta(days=45) for first_seen_date
      * cross_source_batch() -> list[dict]: returns a hardcoded list of 4 opportunities where opportunities[0] and opportunities[2] are the same role from different sources ("AI Ops Manager" at "Acme Corp" from "greenhouse" and "remotive" respectively), and opportunities[1] and opportunities[3] are distinct roles
  </behavior>
  <action>
    1. Create TARIQ__career_radar/tests/fixtures/dedup_test_data.jsonl with 50+ JSONL records.
       Each line is a JSON object: {"title": "...", "company": "...", "location": "Remote", "source": "greenhouse|remotive|weworkremotely", "access_date": "2026-06-15T10:00:00Z"}.
       Organize as 25 pairs; first item in each pair uses concise title, second uses expanded variant.
       Include 5 pairs where source differs between items (cross-source duplicate scenario).
       Include 3 records where access_date is "2026-01-10T10:00:00Z" (>30 days before 2026-06-15).

    2. Append a "Phase 4 additions" section to TARIQ__career_radar/conftest.py (after the Phase-3 section comment block). Add:
       - import datetime at the top if not present (add to existing imports)
       - dedup_opp_pairs fixture: reads fixtures_dir / "dedup_test_data.jsonl", returns list of dicts
       - dedup_fresh_record fixture: returns {"first_seen_date": (utcnow - 45 days).isoformat()+"Z", "last_seen_date": utcnow.isoformat()+"Z", "hit_count": 1}
       - cross_source_batch fixture: hardcoded list[dict] of 4 opps as described in behavior

    Do NOT remove or modify any existing Phase 1/2/3 fixtures in conftest.py.
  </action>
  <verify>
    <automated>cd D:/NIZAM && python -c "import json; lines=open('TARIQ__career_radar/tests/fixtures/dedup_test_data.jsonl').readlines(); assert len(lines) >= 50, f'Need 50+ records, got {len(lines)}'; [json.loads(l) for l in lines]; print('fixture OK')"</automated>
  </verify>
  <done>dedup_test_data.jsonl exists with >=50 valid JSONL records; conftest.py loads cleanly with 3 new Phase-4 fixtures without breaking existing fixtures</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Append 6 failing RED tests to test_dedup_engine.py</name>
  <files>TARIQ__career_radar/tests/test_dedup_engine.py</files>
  <behavior>
    - All 6 tests must be collectible by pytest (no ImportError at collection time)
    - All 6 tests must FAIL with a descriptive message (not error) — use _require_module() pattern from existing file
    - The 3 existing Phase-1 tests (test_sqlite_roundtrip, test_normalization_deterministic, test_persistence_across_restarts) must remain unchanged and GREEN

    Test contracts (these become the GREEN acceptance criteria for Wave 1/2):

    test_fuzzy_match_title_variants (DEDUP-02):
      - Imports fuzzy_match_opportunities from dedup_engine (via _require_module guard)
      - Calls fuzzy_match_opportunities({"title": "AI Operations Manager"}, [{"title": "AI Ops Manager"}])
      - Asserts (is_match, score): is_match is True AND score >= 0.88

    test_fuzzy_match_no_false_positive (DEDUP-02):
      - Calls fuzzy_match_opportunities({"title": "Finance Manager"}, [{"title": "AI Ops Manager"}])
      - Asserts is_match is False (score < 0.88 for clearly different roles)

    test_fuzzy_match_same_company_exact_location (DEDUP-02):
      - Uses cross_source_batch fixture (opps[0] and opps[2] are same role, different source)
      - Calls fuzzy_match_opportunities(opps[2], [opps[0]])
      - Asserts is_match is True (cross-source duplicate detected)

    test_is_fresh_repost_old_role_surfaces (DEDUP-03):
      - Imports is_fresh_repost from dedup_engine
      - Uses dedup_fresh_record fixture (first_seen 45 days ago)
      - Calls is_fresh_repost(record["first_seen_date"], record["last_seen_date"])
      - Asserts result is True (45 days >= 30 day threshold → treat as new)

    test_is_fresh_repost_recent_stays_hidden (DEDUP-03):
      - Calls is_fresh_repost with first_seen=10 days ago, last_seen=today
      - Asserts result is False (10 days < 30 → duplicate, stays hidden)

    test_run_dedup_pass_removes_within_run_dups (DEDUP-02, DEDUP-03):
      - Imports run_dedup_pass from dedup_engine
      - Uses tmp_db_path + cross_source_batch fixture (4 opps, 2 pairs of cross-source dups)
      - Calls run_dedup_pass(cross_source_batch, db_path=tmp_db_path)
      - Asserts len(result) == 2 (only 2 unique roles; 2 cross-source duplicates removed)
      - Asserts result is a list[dict] with "title" keys
  </behavior>
  <action>
    Append the following to TARIQ__career_radar/tests/test_dedup_engine.py (after the existing 3 tests):

    1. Add to the top-level import guard block: also try importing
       fuzzy_match_opportunities, is_fresh_repost, run_dedup_pass from dedup_engine.
       Use the same try/except ImportError pattern, storing each in a module-level variable
       (e.g. _fuzzy_match_opportunities, _is_fresh_repost, _run_dedup_pass).
       Add a combined _require_phase4() helper that raises ImportError if any Phase-4
       symbol is missing.

    2. Append the 6 test functions described in behavior. Each calls _require_phase4()
       as its first line to fail RED cleanly. Each function has a docstring indicating
       which requirement it covers (DEDUP-02 or DEDUP-03).

    3. Do NOT modify the existing 3 Phase-1 tests or the existing _require_module() function.

    Note: run_dedup_pass accepts (opportunities: list[dict], db_path: Path) signature
    per the RESEARCH.md architecture pattern (engine is instantiated internally).
  </action>
  <verify>
    <automated>cd D:/NIZAM && python -m pytest TARIQ__career_radar/tests/test_dedup_engine.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>pytest collects 9 tests total (3 GREEN + 6 RED/FAILED). No collection errors. Existing 3 tests still GREEN. 6 new tests fail with "MISSING — implement in Wave 1" style message, not AttributeError or TypeError.</done>
</task>

</tasks>

<verification>
pytest TARIQ__career_radar/tests/test_dedup_engine.py -v shows:
- 3 PASSED (existing Phase-1 tests)
- 6 FAILED with descriptive "not yet implemented" messages
- 0 ERROR (all 9 tests collected cleanly)
- dedup_test_data.jsonl parseable as JSONL with >=50 records
- conftest.py imports cleanly: python -c "import TARIQ__career_radar.conftest" exits 0
</verification>

<success_criteria>
- pytest TARIQ__career_radar/tests/test_dedup_engine.py -v reports 3 passed, 6 failed, 0 errors
- All 6 new tests clearly map to DEDUP-01/02/03 requirements in their docstrings
- dedup_test_data.jsonl has >=50 records covering: title variants, cross-source pairs, >30 day access_date gaps
- conftest.py adds 3 fixtures (dedup_opp_pairs, dedup_fresh_record, cross_source_batch) without touching existing fixtures
</success_criteria>

<output>
After completion, create .planning/phases/04-deduplication-engine/04-01-SUMMARY.md
</output>
