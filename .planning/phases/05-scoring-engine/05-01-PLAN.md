---
phase: 05-scoring-engine
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - TARIQ__career_radar/tests/test_scoring_engine.py
  - TARIQ__career_radar/tests/fixtures/scoring_test_data.jsonl
  - TARIQ__career_radar/conftest.py
autonomous: true
requirements:
  - SCORE-01
  - SCORE-02

must_haves:
  truths:
    - "17 tests in test_scoring_engine.py are collectable (no import errors) and ALL fail RED before implementation"
    - "scoring_test_data.jsonl exists with ≥8 distinct opportunity records covering: normal ATS opp, scam signal opp, unpaid work opp, unclear pay opp, side-income platform opp, stale opp, high-salary opp, missing-salary opp"
    - "conftest.py exposes scoring_profile, scored_opportunity, scam_opportunity, unpaid_opportunity, now_fixture as named fixtures"
    - "Test names directly map to SCORE-01 and SCORE-02 requirement behaviors from RESEARCH.md"
  artifacts:
    - path: "TARIQ__career_radar/tests/test_scoring_engine.py"
      provides: "17 failing unit/integration tests for ScoringEngine + run_scoring_pass"
      contains: "def test_score_output_range_0_100"
    - path: "TARIQ__career_radar/tests/fixtures/scoring_test_data.jsonl"
      provides: "8+ fixture opportunity records for calibration"
    - path: "TARIQ__career_radar/conftest.py"
      provides: "Phase 5 fixtures: scoring_profile, now_fixture, scored_opportunity"
  key_links:
    - from: "test_scoring_engine.py"
      to: "radar.scoring_engine.ScoringEngine"
      via: "_require_module() import guard"
      pattern: "ScoringEngine|scoring_engine"
    - from: "test_scoring_engine.py"
      to: "radar.stages.score.run_scoring_pass"
      via: "_require_module() import guard"
      pattern: "run_scoring_pass"
---

<objective>
Establish the RED TDD scaffold for Phase 5: 17 failing tests covering all SCORE-01 and SCORE-02 behaviors, fixture data for 8 scenario types, and conftest fixtures for scoring context.

Purpose: Tests are the executable contract — they define exactly what scoring_engine.py and stages/score.py must produce before any implementation line is written.
Output: 17 failing tests, scoring_test_data.jsonl, 5 new conftest fixtures.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-scoring-engine/05-RESEARCH.md

@TARIQ__career_radar/conftest.py
@TARIQ__career_radar/tests/fixtures/dedup_test_data.jsonl
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From radar/stages/dedup.py (Phase 4 output that Phase 5 consumes):
```python
def run_dedup_pass(
    opportunities: list[dict],
    db_path: Path,
) -> list[dict]:
    """Returns deduplicated opportunities — Phase 5 input."""
```

From radar/config.py:
```python
def load_profile_seed() -> dict:
    """Returns dict with keys: role_keywords, target_roles, constraints.
    role_keywords is dict[group_name, list[keyword]] per existing conftest.py."""
```

Phase 5 target module signatures (NOT YET IMPLEMENTED — these are the contracts tests import against):
```python
# TARIQ__career_radar/radar/scoring_engine.py (to be created in 05-02)
class ScoreBreakdown:
    fit: int
    salary_upside: int
    growth: int
    visa_feasibility: int
    company_strength: int
    referral_leverage: int
    freshness: int
    side_income: int
    penalties: dict[str, int]
    def total_penalty(self) -> int: ...

class ScoringEngine:
    WEIGHTS: dict  # fit:0.25, salary_upside:0.20, growth:0.15, visa:0.10, company:0.10, referral:0.10, freshness:0.05, side_income:0.05
    def __init__(self, profile: dict | None = None): ...
    def score(self, opportunity: dict, now: datetime | None = None) -> tuple[int, ScoreBreakdown]: ...

# TARIQ__career_radar/radar/stages/score.py (to be created in 05-03)
def run_scoring_pass(
    opportunities: list[dict],
    profile: dict | None = None,
    now: datetime | None = None,
) -> list[dict]:
    """Returns opportunities sorted by final_score descending, each with final_score + score_breakdown."""
```

Existing conftest.py fixture pattern (try/except ImportError guard — DO NOT import directly at module level):
```python
def _require_module(module_path: str):
    try:
        import importlib
        return importlib.import_module(module_path)
    except ImportError:
        pytest.skip(f"Module {module_path} not yet implemented")
```
</interfaces>

<feature>
  <name>Phase 5 TDD Scaffold — ScoringEngine + run_scoring_pass</name>
  <files>
    TARIQ__career_radar/tests/test_scoring_engine.py,
    TARIQ__career_radar/tests/fixtures/scoring_test_data.jsonl,
    TARIQ__career_radar/conftest.py
  </files>
  <behavior>
    RED tests must collect and fail. The existing pattern in conftest.py uses try/except ImportError
    guards (NOT direct top-level imports) so tests are collectible even before implementation files exist.
    Mirror the pattern from conftest.py existing fixtures exactly.

    SCORE-01 tests (11 tests):
    - test_score_output_range_0_100: ScoringEngine.score() returns int in [0, 100] inclusive
    - test_scoring_deterministic: same opportunity + same now + same profile → identical score twice
    - test_fit_weight_25_percent: opportunity with only fit dimension non-zero contributes 25% of 100
    - test_salary_weight_20_percent: opportunity with only salary_upside non-zero contributes 20% of 100
    - test_growth_weight_15_percent: opportunity with only growth non-zero contributes 15% of 100
    - test_visa_weight_10_percent: opportunity with only visa_feasibility non-zero contributes 10% of 100
    - test_company_weight_10_percent: opportunity with only company_strength non-zero contributes 10% of 100
    - test_referral_weight_10_percent: opportunity with only referral_leverage non-zero contributes 10% of 100
    - test_freshness_weight_5_percent: opportunity with only freshness non-zero contributes 5% of 100
    - test_side_income_weight_5_percent: opportunity with only side_income non-zero contributes 5% of 100
    - test_breakdown_includes_all_dimensions: ScoreBreakdown has fit, salary_upside, growth, visa_feasibility, company_strength, referral_leverage, freshness, side_income, penalties attrs

    SCORE-02 tests (6 tests):
    - test_penalty_scam_risk: scam keyword in title → breakdown.penalties["scam_risk"] == 20
    - test_penalty_unclear_pay: salary_confidence==LOW + unclear keyword in description → breakdown.penalties["unclear_pay"] == 15
    - test_penalty_severe_skill_mismatch: fit < 30 + role in avoid_flags → breakdown.penalties["severe_skill_mismatch"] == 10
    - test_penalty_unpaid_work: salary_usd_high == 0 → breakdown.penalties["exploitative_unpaid"] == 20
    - test_multiple_penalties_cumulative: scam + unpaid both present → total penalty == 40 (both applied, not one)
    - test_score_capped_0_100: penalties exceed base score → final_score == 0 (not negative)

    Integration test (1 test):
    - test_run_scoring_pass_batch: run_scoring_pass([3 opps]) returns list sorted by final_score descending, each opp has "final_score" (int) and "score_breakdown" (dict) keys

    Total: 17 tests.
  </behavior>
  <implementation>
    ## Task 1: Create scoring_test_data.jsonl (8 fixture records)

    Write TARIQ__career_radar/tests/fixtures/scoring_test_data.jsonl with exactly 8 JSONL records.
    Each record must have all required fields from DATA-01 schema. Records must cover all 8 scoring scenarios:

    Record 1 (normal_ats): Normal ATS-sourced opportunity. title="AI Operations Manager", company="Acme Corp",
    source="greenhouse", source_type="ats", salary_usd_high=120000, salary_confidence="HIGH",
    visa_feasibility="visa_sponsored_likely", role_category="AI_OPERATIONS",
    access_date="2026-06-15T10:00:00Z", description="AI ops and stakeholder management role"

    Record 2 (scam_signal): Scam keywords in title. title="Quick Cash - Work From Home - No Experience Required",
    company="Easy Money Inc", source="manual", source_type="manual",
    description="guaranteed usd income", access_date="2026-06-15T10:00:00Z",
    salary_usd_high=null, salary_confidence="LOW"

    Record 3 (unpaid_work): salary_usd_high=0. title="AI Research Assistant (Unpaid)",
    company="Research Lab", description="unpaid internship no salary",
    source="manual", source_type="manual", access_date="2026-06-15T10:00:00Z"

    Record 4 (unclear_pay): salary_confidence="LOW" + description contains "stipend".
    title="Data Science Contractor", description="project-based stipend commission",
    company="Gig Corp", source="remotive", source_type="rss_feed",
    access_date="2026-06-15T10:00:00Z", salary_confidence="LOW"

    Record 5 (side_income_platform): source="outlier". title="AI Evaluator",
    company="Outlier AI", source="outlier", source_type="manual",
    description="evaluate AI outputs", access_date="2026-06-15T10:00:00Z",
    salary_usd_high=50000, salary_confidence="MEDIUM"

    Record 6 (stale_opp): access_date 45 days ago (use "2026-05-01T10:00:00Z").
    title="Data Analyst", company="Legacy Corp", source="greenhouse",
    source_type="ats", access_date="2026-05-01T10:00:00Z",
    salary_usd_high=80000, salary_confidence="HIGH"

    Record 7 (high_salary): salary_usd_high=180000, salary_confidence="HIGH".
    title="Staff ML Engineer", company="OpenAI", source="lever", source_type="ats",
    description="ml platform engineering", access_date="2026-06-15T10:00:00Z",
    visa_feasibility="visa_sponsored_likely"

    Record 8 (missing_salary): salary_usd_high=null, salary_usd_low=null, salary_confidence="LOW".
    title="Program Manager", company="Startup Co", source="weworkremotely",
    source_type="rss_feed", description="coordination program management",
    access_date="2026-06-15T10:00:00Z", visa_feasibility="visa_sponsored_unclear"

    Every record must also include: opportunity_id (uuid4 string), location="Remote",
    remote_status="fully_remote", fit_score=0, growth_score=0, confidence="LOW",
    tags=[], salary_usd_low (number or null), observed_at (same as access_date),
    lane="Remote USD", data_quality="partial", run_id="test-run-001".
    salary_evidence_type: "employer_posted" if salary present + ats source, else "not_disclosed".

    ## Task 2: Augment conftest.py with Phase 5 fixtures

    Append 5 new fixtures to TARIQ__career_radar/conftest.py at the bottom.
    Add a section header comment: "Phase 5 additions — scoring fixtures (SCORE-01, SCORE-02)".

    Fixture 1 — scoring_profile:
    Returns a synthetic profile dict for scoring tests. Must have:
    - role_keywords: dict with "AI_OPERATIONS": ["ai operations", "ai ops", "operations", "ml ops", "llm ops"]
      and "DATA_SCIENCE": ["data science", "data scientist", "ml engineer", "machine learning"]
    - target_roles: ["AI Ops Manager", "ML Engineering Manager"]
    - constraints: {"min_salary_usd": 60000, "remote_only": True}
    - avoid_flags: ["SALES", "RECRUITING"]  ← used by severe_skill_mismatch penalty

    Fixture 2 — now_fixture:
    Returns datetime.datetime(2026, 6, 15, 12, 0, 0, tzinfo=datetime.timezone.utc)
    This is a fixed datetime for deterministic freshness testing. Named `now_fixture` to
    avoid shadowing pytest's `now` if any plugin uses that name.

    Fixture 3 — scored_opportunity:
    Returns a fully populated opportunity dict for normal scoring tests.
    title="AI Operations Manager", company="Acme Corp", source="greenhouse",
    source_type="ats", access_date="2026-06-15T10:00:00Z",
    salary_usd_high=120000, salary_usd_low=90000, salary_confidence="HIGH",
    salary_evidence_type="employer_posted", visa_feasibility="visa_sponsored_likely",
    role_category="AI_OPERATIONS", description="ai operations and stakeholder management",
    location="Remote", remote_status="fully_remote", tags=[], fit_score=0, growth_score=0,
    confidence="LOW", observed_at="2026-06-15T10:00:00Z", lane="Remote USD",
    data_quality="confirmed", run_id="test-run-001", opportunity_id=str(uuid.uuid4())

    Fixture 4 — scam_opportunity:
    Returns opportunity dict with scam keywords in title and description.
    title="Quick Cash - No Experience Required - Make Money Fast",
    company="Easy Money Inc", description="guaranteed usd income work from home 100%",
    source="manual", source_type="manual", access_date="2026-06-15T10:00:00Z",
    salary_usd_high=None, salary_usd_low=None, salary_confidence="LOW",
    salary_evidence_type="not_disclosed", visa_feasibility="visa_sponsored_unclear",
    location="Remote", remote_status="fully_remote", tags=[], fit_score=0, growth_score=0,
    confidence="LOW", observed_at="2026-06-15T10:00:00Z", lane="Remote USD",
    data_quality="partial", run_id="test-run-001", opportunity_id=str(uuid.uuid4())

    Fixture 5 — unpaid_opportunity:
    Returns opportunity dict with salary_usd_high=0 and unpaid keywords.
    title="AI Research Assistant (Unpaid Trial)", company="Research Lab",
    description="unpaid trial no salary volunteer",
    source="manual", source_type="manual", access_date="2026-06-15T10:00:00Z",
    salary_usd_high=0, salary_usd_low=0, salary_confidence="LOW",
    salary_evidence_type="not_disclosed", visa_feasibility="visa_not_sponsored",
    location="Remote", remote_status="fully_remote", tags=[], fit_score=0, growth_score=0,
    confidence="LOW", observed_at="2026-06-15T10:00:00Z", lane="Remote USD",
    data_quality="partial", run_id="test-run-001", opportunity_id=str(uuid.uuid4())

    ## Task 3: Write test_scoring_engine.py (17 RED tests)

    Create TARIQ__career_radar/tests/test_scoring_engine.py.

    IMPORTANT: Use try/except ImportError guard pattern at module top (do NOT use direct top-level
    imports — that causes collection errors, not test failures, breaking the RED → GREEN cycle).

    Use this guard at the top of the file (before test functions):
    ```python
    try:
        from radar.scoring_engine import ScoringEngine, ScoreBreakdown
        _SCORING_ENGINE_AVAILABLE = True
    except ImportError:
        _SCORING_ENGINE_AVAILABLE = False
        ScoringEngine = None
        ScoreBreakdown = None

    try:
        from radar.stages.score import run_scoring_pass
        _SCORE_STAGE_AVAILABLE = True
    except ImportError:
        _SCORE_STAGE_AVAILABLE = False
        run_scoring_pass = None
    ```

    Then at the start of each test function that uses ScoringEngine:
    ```python
    if not _SCORING_ENGINE_AVAILABLE:
        pytest.fail("radar.scoring_engine not yet implemented (RED phase)")
    ```
    And for run_scoring_pass tests:
    ```python
    if not _SCORE_STAGE_AVAILABLE:
        pytest.fail("radar.stages.score not yet implemented (RED phase)")
    ```

    This ensures: pytest collects all 17 tests, all 17 FAIL in RED phase, all 17 PASS after implementation.

    Test functions (all take fixtures from conftest.py where needed):

    SCORE-01 weight tests — pattern: craft an opportunity where ONLY the dimension under test
    is non-zero. Score it. Assert final_score equals that dimension's weight × 100 (as int).
    Use now_fixture to freeze time. Use scoring_profile from conftest.

    For weight tests, use these "isolation" opportunity patterns:
    - fit only: set all scoring fields to neutral/zero EXCEPT title contains all profile keywords.
      To isolate fit: visa_feasibility="visa_not_sponsored" (10 pts), role not AI_OPERATIONS (40 growth base),
      source_type="manual" (50 referral), freshness=60 (7-30 days), salary=None.
      NOTE: True isolation is hard because growth/company have defaults. Instead test that fit CONTRIBUTES
      at least 25 points when all profile keywords match AND other dimensions are minimal.
      More practical approach: score an opportunity where fit_score can be predicted exactly,
      then assert breakdown.fit == expected_fit_score AND final_score includes that contribution.
      
    Simpler weight test approach (use for 05-02 implementation guidance too):
    - test_fit_weight_25_percent: Create opportunity with 0 profile keyword matches (fit=0).
      Then create one with exact matches (fit should be >0). Assert that increasing fit changes
      final_score by approximately weight × Δfit.
      OR: Assert ScoringEngine.WEIGHTS["fit"] == 0.25 (directly test the constant).
      
    Use WEIGHTS constant assertion for the 7 weight-value tests (simpler, unambiguous):
    - test_fit_weight_25_percent: assert ScoringEngine.WEIGHTS["fit"] == 0.25
    - test_salary_weight_20_percent: assert ScoringEngine.WEIGHTS["salary_upside"] == 0.20
    - test_growth_weight_15_percent: assert ScoringEngine.WEIGHTS["growth"] == 0.15
    - test_visa_weight_10_percent: assert ScoringEngine.WEIGHTS["visa_feasibility"] == 0.10
    - test_company_weight_10_percent: assert ScoringEngine.WEIGHTS["company_strength"] == 0.10
    - test_referral_weight_10_percent: assert ScoringEngine.WEIGHTS["referral_leverage"] == 0.10
    - test_freshness_weight_5_percent: assert ScoringEngine.WEIGHTS["freshness"] == 0.05
    - test_side_income_weight_5_percent: assert ScoringEngine.WEIGHTS["side_income"] == 0.05

    (This is 8 tests, not 7 — count is fine, 17 total still holds)

    Other SCORE-01 tests:
    - test_score_output_range_0_100(scored_opportunity, scoring_profile, now_fixture):
      score, breakdown = ScoringEngine(scoring_profile).score(scored_opportunity, now_fixture)
      assert isinstance(score, int)
      assert 0 <= score <= 100
    
    - test_scoring_deterministic(scored_opportunity, scoring_profile, now_fixture):
      engine = ScoringEngine(scoring_profile)
      s1, b1 = engine.score(scored_opportunity, now_fixture)
      s2, b2 = engine.score(scored_opportunity, now_fixture)
      assert s1 == s2
      assert b1.fit == b2.fit and b1.salary_upside == b2.salary_upside
    
    - test_breakdown_includes_all_dimensions(scored_opportunity, scoring_profile, now_fixture):
      score, breakdown = ScoringEngine(scoring_profile).score(scored_opportunity, now_fixture)
      assert hasattr(breakdown, "fit")
      assert hasattr(breakdown, "salary_upside")
      assert hasattr(breakdown, "growth")
      assert hasattr(breakdown, "visa_feasibility")
      assert hasattr(breakdown, "company_strength")
      assert hasattr(breakdown, "referral_leverage")
      assert hasattr(breakdown, "freshness")
      assert hasattr(breakdown, "side_income")
      assert hasattr(breakdown, "penalties")
      assert isinstance(breakdown.penalties, dict)

    SCORE-02 penalty tests:
    - test_penalty_scam_risk(scam_opportunity, scoring_profile, now_fixture):
      _, breakdown = ScoringEngine(scoring_profile).score(scam_opportunity, now_fixture)
      assert "scam_risk" in breakdown.penalties
      assert breakdown.penalties["scam_risk"] == 20
    
    - test_penalty_unclear_pay(scoring_profile, now_fixture):
      opp = {... base fields ..., "description": "project-based stipend commission",
             "salary_confidence": "LOW", "salary_usd_high": 50000}
      _, breakdown = ScoringEngine(scoring_profile).score(opp, now_fixture)
      assert "unclear_pay" in breakdown.penalties
      assert breakdown.penalties["unclear_pay"] == 15
    
    - test_penalty_severe_skill_mismatch(scoring_profile, now_fixture):
      # role_category in avoid_flags AND fit < 30
      opp = {... base fields ..., "role_category": "SALES",
             "title": "Sales Executive xyz-no-match",
             "description": "B2B enterprise sales revenue quota"}
      _, breakdown = ScoringEngine(scoring_profile).score(opp, now_fixture)
      assert "severe_skill_mismatch" in breakdown.penalties
      assert breakdown.penalties["severe_skill_mismatch"] == 10
    
    - test_penalty_unpaid_work(unpaid_opportunity, scoring_profile, now_fixture):
      _, breakdown = ScoringEngine(scoring_profile).score(unpaid_opportunity, now_fixture)
      assert "exploitative_unpaid" in breakdown.penalties
      assert breakdown.penalties["exploitative_unpaid"] == 20
    
    - test_multiple_penalties_cumulative(scam_opportunity, scoring_profile, now_fixture):
      # scam_opportunity also has unpaid keywords — add salary_usd_high=0 to trigger both
      opp = dict(scam_opportunity)
      opp["salary_usd_high"] = 0
      opp["description"] = "guaranteed usd income work from home 100% unpaid no salary"
      _, breakdown = ScoringEngine(scoring_profile).score(opp, now_fixture)
      assert "scam_risk" in breakdown.penalties
      assert "exploitative_unpaid" in breakdown.penalties
      total = sum(breakdown.penalties.values())
      assert total >= 40, f"Expected cumulative penalties >= 40, got {total}"
    
    - test_score_capped_0_100(unpaid_opportunity, scoring_profile, now_fixture):
      # Force worst-case: no positive dimension signals
      opp = dict(unpaid_opportunity)
      opp["description"] = "unpaid no salary guaranteed usd quick cash no experience required"
      opp["title"] = "Unpaid Quick Cash Scam Role"
      score, _ = ScoringEngine(scoring_profile).score(opp, now_fixture)
      assert score >= 0, "Score must not go negative"
      assert score <= 100, "Score must not exceed 100"

    Integration test:
    - test_run_scoring_pass_batch(scoring_profile, now_fixture):
      from radar.stages.score import run_scoring_pass (guarded)
      opps = [scored_opportunity_inline_dict, scam_opp_inline_dict, unpaid_opp_inline_dict]
      result = run_scoring_pass(opps, profile=scoring_profile, now=now_fixture)
      assert len(result) == 3
      for opp in result:
          assert "final_score" in opp
          assert "score_breakdown" in opp
          assert isinstance(opp["final_score"], int)
      # Sorted descending
      scores = [o["final_score"] for o in result]
      assert scores == sorted(scores, reverse=True)

    All 17 tests MUST FAIL when run before 05-02 and 05-03 are implemented.
    Verify RED with: pytest TARIQ__career_radar/tests/test_scoring_engine.py -v
    Expected output: 17 FAILED (all with "radar.scoring_engine not yet implemented" or similar)
  </implementation>
</feature>

<verification>
```
pytest TARIQ__career_radar/tests/test_scoring_engine.py -v
```
Expected: 17 tests collected, 17 FAILED (RED phase confirmed).

```
pytest TARIQ__career_radar/tests/ -v --collect-only 2>&1 | grep "test_scoring"
```
Expected: 17 test items listed (confirms collectibility — no import errors at collection time).

```
python -c "import json; lines=open('TARIQ__career_radar/tests/fixtures/scoring_test_data.jsonl').readlines(); [json.loads(l) for l in lines]; print(f'{len(lines)} records')"
```
Expected: "8 records" (or more).

Full suite must still pass existing tests:
```
pytest TARIQ__career_radar/tests/ -v --ignore=TARIQ__career_radar/tests/test_scoring_engine.py
```
Expected: All prior tests GREEN (no regressions from conftest augmentation).
</verification>

<success_criteria>
- 17 tests in test_scoring_engine.py, all collectible (no ImportError at collection), all FAIL
- scoring_test_data.jsonl with ≥8 records covering all 8 scenario types
- conftest.py has 5 new fixtures: scoring_profile, now_fixture, scored_opportunity, scam_opportunity, unpaid_opportunity
- No regression: full test suite minus test_scoring_engine.py remains GREEN
- RED phase committed: `test(05-01): add 17 failing Phase-5 scoring tests (SCORE-01, SCORE-02)`
</success_criteria>

<output>
After completion, create `.planning/phases/05-scoring-engine/05-01-SUMMARY.md`
</output>
