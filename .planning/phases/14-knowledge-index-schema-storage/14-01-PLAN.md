---
phase: 14-knowledge-index-schema-storage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HIKMAH__knowledge_index/index/schema.py
  - HIKMAH__knowledge_index/index/__init__.py
autonomous: true
requirements:
  - INDEX-01
must_haves:
  truths:
    - "Index schema defines topics with id, name, status, timestamps, context_tags, confidence, accomplishments, blockers, notes"
    - "Index schema defines completions array with closed topic records"
    - "Index schema defines activity_history as append-only log of all index mutations"
    - "Index schema defines stalled_work tracking blockers and days_stalled per topic"
    - "Index schema defines context_snapshots with timestamp and aggregated metrics"
    - "Schema includes version field supporting semantic versioning (1.0, 1.1, etc.)"
    - "Context tags are validated against whitelist to prevent PII leakage"
    - "Schema validation function validates structure and rejects invalid data"
  artifacts:
    - path: "HIKMAH__knowledge_index/index/schema.py"
      provides: "IndexSchema TypedDict definitions + validate_index_schema() function"
      min_lines: 150
    - path: "HIKMAH__knowledge_index/index/__init__.py"
      provides: "Module package initialization"
      min_lines: 5
  key_links:
    - from: "HIKMAH__knowledge_index/index/schema.py"
      to: "schema validation tests (Wave 4)"
      via: "validate_index_schema() exported for test imports"
      pattern: "from.*schema import validate_index_schema"
---

<objective>
Define the complete JSON schema for persona knowledge indices with full TypedDict type hints, validation logic, and documentation.

Purpose: Establish the schema contract that all downstream phases (15-20) will rely on for index reads/writes. Schema must prevent PII leakage via context_tags whitelist and support future versioning.

Output: schema.py module with validated TypedDicts and validation function; INDEX-01 requirement fully satisfied.
</objective>

<execution_context>
@D:\Users\PC\.claude\get-shit-done\workflows\execute-plan.md
@D:\Users\PC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-knowledge-index-schema-storage/14-RESEARCH.md

# Research findings confirm stdlib-only approach (json, pathlib, uuid, hashlib, datetime, typing)
# 11 personas: AMMAR, HIKMAH, TARIQ, MUNAWARA, MAL, BADAN, NAQD, SHURA, TAFRIGH, MARSAD, NIZAM
# Schema must use context_tags whitelist: technical, health, financial, strategic, personal
# Version field supports semantic versioning with MAKHZAN snapshot pattern on breaking changes
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create HIKMAH__knowledge_index package structure</name>
  <files>HIKMAH__knowledge_index/index/__init__.py</files>
  <action>
    Create Python package structure for the knowledge index module:
    
    1. Create directory: HIKMAH__knowledge_index/index/
    2. Create __init__.py with module docstring and imports that will expose schema.py exports
    3. Docstring should explain this is the core indexing logic (schema + validation)
    
    Use stdlib only. No dependencies beyond what NIZAM already has.
    </action>
  <verify>
    <automated>test -f "HIKMAH__knowledge_index/index/__init__.py" && grep -q "schema" "HIKMAH__knowledge_index/index/__init__.py"</automated>
  </verify>
  <done>Package __init__.py created with appropriate docstring and module structure</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define index schema with TypedDict and validation</name>
  <files>HIKMAH__knowledge_index/index/schema.py</files>
  <behavior>
    Test 1: validate_index_schema() accepts valid index with all required fields (version, persona, topics, completions, activity_history, stalled_work, context_snapshots, metadata) and returns (True, None)
    Test 2: validate_index_schema() rejects index missing "version" field and returns (False, error_string)
    Test 3: validate_index_schema() rejects invalid version format (e.g., "2.0" when only "1.x" valid) and returns (False, error_string)
    Test 4: validate_index_schema() rejects unknown persona (not in 11-persona list) and returns (False, error_string)
    Test 5: validate_index_schema() rejects context_tags with values not in whitelist (must be from: technical, health, financial, strategic, personal) and returns (False, error_string)
    Test 6: validate_index_schema() accepts valid schema with empty topics/completions/activity_history arrays
    Test 7: Schema TypedDicts support full nested structure: TopicDict with id, name, status, created_at, last_activity, context_tags, confidence, key_accomplishments, blockers, notes
    Test 8: Completions have id, name, completed_at, duration_days, context_tags, final_note
    Test 9: activity_history entries have ts, event_type, optional topic_id, description
    Test 10: stalled_work entries have topic_id, topic_name, blocker_count, stalled_since, days_stalled, last_activity, recovery_notes
    Test 11: context_snapshots have ts and snapshot with metrics (open_topic_count, active_blocker_count, recent_accomplishments_count, completion_rate_7d, engagement_level)
    Test 12: metadata field has source, locale, language
  </behavior>
  <action>
    Create HIKMAH__knowledge_index/index/schema.py with complete TypedDict definitions:
    
    1. **TypedDict Definitions:**
       - AccomplishmentDict: {text: str, timestamp: str}
       - BlockerDict: {text: str, since: str, severity: str}
       - TopicDict: {id, name, status, created_at, last_activity, context_tags, confidence, key_accomplishments, blockers, notes}
       - CompletionDict: {id, name, completed_at, duration_days, context_tags, final_note}
       - ActivityEventDict: {ts, event_type, topic_id (optional), description}
       - StalledWorkDict: {topic_id, topic_name, blocker_count, stalled_since, days_stalled, last_activity, recovery_notes}
       - SnapshotMetricsDict: {open_topic_count, active_blocker_count, recent_accomplishments_count, completion_rate_7d, engagement_level}
       - ContextSnapshotDict: {ts, snapshot}
       - MetadataDict: {source, locale, language}
       - PersonaIndexDict: {version, persona, initialized_at, last_updated, topics, completions, activity_history, stalled_work, context_snapshots, metadata}
    
    2. **Validation Function:**
       ```python
       def validate_index_schema(data: dict) -> tuple[bool, Optional[str]]:
           """Validate index structure against schema. Return (valid, error_msg)."""
       ```
       
       - Check required fields: version, persona, initialized_at, last_updated, topics, completions, activity_history, stalled_work, context_snapshots, metadata
       - Validate version format: must be "1.x" (e.g., "1.0", "1.1", not "2.0")
       - Validate persona: must be in ["AMMAR", "HIKMAH", "TARIQ", "MUNAWARA", "MAL", "BADAN", "NAQD", "SHURA", "TAFRIGH", "MARSAD", "NIZAM"]
       - Validate timestamps: all *_at fields must be ISO 8601 format
       - Validate context_tags: all tags must be in whitelist ["technical", "health", "financial", "strategic", "personal"]
       - Validate arrays: topics, completions, activity_history, stalled_work must be lists
       - Validate context_snapshots: must be list of snapshot objects
       - Return (True, None) on success; (False, error_string) on failure
    
    3. **Constants:**
       - VALID_PERSONAS = ["AMMAR", "HIKMAH", "TARIQ", "MUNAWARA", "MAL", "BADAN", "NAQD", "SHURA", "TAFRIGH", "MARSAD", "NIZAM"]
       - CONTEXT_TAGS_WHITELIST = {"technical", "health", "financial", "strategic", "personal"}
       - VALID_VERSION_PATTERN = r"^1\.[0-9]+$"
       - VALID_TOPIC_STATUS = ["active", "paused", "completed"]
       - VALID_EVENT_TYPES = ["topic_created", "accomplishment_logged", "blocker_flagged", "topic_completed", "context_snapshot", "index_initialized"]
       - VALID_BLOCKER_SEVERITY = ["low", "medium", "high"]
    
    4. **Use stdlib only:** json, pathlib, uuid, hashlib, datetime, typing. No new dependencies.
    
    5. **Comment rationale:** Explain whitelist enforcement, version constraints, and how privacy is preserved via context_tags.
    
    Reference NIZAM pattern: Mirror structure and validation style from existing ledger schemas (EVENT_LEDGER, LEARNING_LEDGER).
    </action>
  <verify>
    <automated>python -m pytest HIKMAH__knowledge_index/tests/test_schema_validation.py -xvs 2>/dev/null || echo "Tests will be created in Wave 4"</automated>
  </verify>
  <done>schema.py created with complete TypedDict definitions, validation function, and constants; all required fields documented; context_tags whitelist enforced</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. HIKMAH__knowledge_index/index/ directory exists as a Python package
2. schema.py imports work: `from HIKMAH__knowledge_index.index.schema import validate_index_schema, PersonaIndexDict`
3. Schema defines all 12 required fields (version, persona, initialized_at, last_updated, topics, completions, activity_history, stalled_work, context_snapshots, metadata, plus nested types)
4. Validation function accepts valid sample index and rejects invalid ones
5. Context tags whitelist prevents PII (only: technical, health, financial, strategic, personal)
6. Version validation enforces "1.x" format (supports 1.0, 1.1, 1.2 but not 2.0 without migration)
</verification>

<success_criteria>
- Schema module compiles without errors (python -c "import HIKMAH__knowledge_index.index.schema")
- Validation function exported and callable
- TypedDict definitions match Phase 15-20 requirements
- All 11 personas hardcoded in VALID_PERSONAS constant
- Context tags whitelist prevents raw PII (names, health conditions, financial amounts)
- Version field supports future semantic versioning with MAKHZAN pattern
</success_criteria>

<output>
After completion, create `.planning/phases/14-knowledge-index-schema-storage/14-01-SUMMARY.md`
</output>
