---
phase: 14-knowledge-index-schema-storage
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - HIKMAH__knowledge_index/README.md
  - HIKMAH__knowledge_index/_index.json
  - NIZAM__system/NIZAM_TEMPLE.json
  - NIZAM__system/policies/PRIVACY_CLASSIFICATION.json
autonomous: true
requirements:
  - INDEX-02
must_haves:
  truths:
    - "HIKMAH__knowledge_index is registered as a NIZAM module in NIZAM_TEMPLE.json"
    - "PERSONA_KNOWLEDGE_INDEX ledger is registered in NIZAM_TEMPLE.json with privacy=strict_local"
    - "PRIVACY_CLASSIFICATION.json includes rules for indices/ directory (strict_local, never synced)"
    - "PRIVACY_CLASSIFICATION.json includes rules for PERSONA_KNOWLEDGE_INDEX.jsonl (strict_local)"
    - ".gitignore prevents indices/*.json files from being committed to GitHub"
    - "Module _index.json includes phase reference (14) and purpose description"
    - "README.md documents module purpose, usage, privacy constraints, and integration points"
  artifacts:
    - path: "HIKMAH__knowledge_index/README.md"
      provides: "Module documentation, quick start, privacy warning"
      min_lines: 50
    - path: "HIKMAH__knowledge_index/_index.json"
      provides: "Module self-registration, phase reference, purpose"
      min_lines: 10
    - path: "NIZAM__system/NIZAM_TEMPLE.json"
      provides: "Module + ledger registration"
      contains: "HIKMAH__knowledge_index"
    - path: "NIZAM__system/policies/PRIVACY_CLASSIFICATION.json"
      provides: "strict_local rules for indices/ and ledger"
      contains: "HIKMAH__knowledge_index/indices"
  key_links:
    - from: "HIKMAH__knowledge_index/_index.json"
      to: "NIZAM_TEMPLE.json"
      via: "Module registry lookup"
      pattern: "HIKMAH__knowledge_index"
    - from: "NIZAM__system/policies/PRIVACY_CLASSIFICATION.json"
      to: "HIMAYAH gate"
      via: "Classification rules prevent egress"
      pattern: "strict_local.*PERSONA_KNOWLEDGE_INDEX"
---

<objective>
Register HIKMAH__knowledge_index as an official NIZAM module and configure privacy enforcement via SYNC_POLICY + PRIVACY_CLASSIFICATION.

Purpose: Ensure indices are treated as strict_local (never egressed to GitHub/Drive/Telegram), ledger is registered in NIZAM governance, and documentation is clear on privacy constraints.

Output: Module fully registered in NIZAM Temple; privacy rules locked in PRIVACY_CLASSIFICATION; README documents module scope and integration points; INDEX-02 requirement satisfied.
</objective>

<execution_context>
@D:\Users\PC\.claude\get-shit-done\workflows\execute-plan.md
@D:\Users\PC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-knowledge-index-schema-storage/14-RESEARCH.md

# Recommended module name: HIKMAH__knowledge_index (HIKMAH = weekly synthesist + pattern promoter)
# Indices stored in HIKMAH__knowledge_index/indices/{PERSONA}_index.json (strict_local, never committed)
# Ledger: PERSONA_KNOWLEDGE_INDEX.jsonl (strict_local, append-only)
# Privacy classification enforced via SYNC_POLICY + HIMAYAH gate (proven pattern in BADAN, MAL, TARIQ)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create module README and _index.json</name>
  <files>
    HIKMAH__knowledge_index/README.md
    HIKMAH__knowledge_index/_index.json
  </files>
  <action>
    Create module documentation and self-registration:
    
    1. **HIKMAH__knowledge_index/README.md:**
       - Title: "HIKMAH Knowledge Index — Persona Knowledge State Tracking"
       - Purpose: "Per-persona knowledge indices tracking topics, activity history, blockers, and context snapshots. Strictly local (never egressed). Initialized once per persona, refreshed by Phase 15 (Data Refresh), read by Phase 16 (Message Generation)."
       - Architecture: Describe directory structure (indices/, data/, tests/), per-persona JSON format
       - Privacy: **BOLD warning:** "Knowledge indices are STRICTLY LOCAL. Never committed to GitHub, never synced to Drive, never exposed to Telegram. All data remains on encrypted laptop/volume only."
       - Versioning: Explain version field, MAKHZAN snapshot pattern for schema evolution
       - Integration: Phases 15 (refresh), 16 (message gen), 17 (response tracking), 18 (adaptation), 20 (validation)
       - Usage: Quick start code snippet showing how to load an index
       - Testing: Reference test files for validation patterns
    
    2. **HIKMAH__knowledge_index/_index.json:**
       - Schema: Follow NIZAM module registration pattern (module, purpose, phase, version)
       - Content:
         ```json
         {
           "module": "HIKMAH__knowledge_index",
           "codename": "Persona Knowledge Index",
           "purpose": "Per-persona knowledge state tracking with topics, activity history, blockers, and context snapshots. Foundation for adaptive messaging (Phases 15-20).",
           "phase": 14,
           "phase_gate": "HIMAYAH (privacy enforcement)",
           "privacy": "strict_local",
           "indices_location": "HIKMAH__knowledge_index/indices/{PERSONA}_index.json",
           "ledger": "PERSONA_KNOWLEDGE_INDEX.jsonl",
           "personas": ["AMMAR", "HIKMAH", "TARIQ", "MUNAWARA", "MAL", "BADAN", "NAQD", "SHURA", "TAFRIGH", "MARSAD", "NIZAM"],
           "schema_version": "1.0",
           "downstream_phases": ["15-data-refresh", "16-message-generation", "17-delivery-tracking", "18-adaptation", "20-privacy-validation"],
           "created": "2026-06-20",
           "version": "1.0"
         }
         ```
    
    Use Markdown for README and JSON5 (but JSON-valid) for _index.json. No dependencies beyond stdlib.
    </action>
  <verify>
    <automated>test -f "HIKMAH__knowledge_index/README.md" && test -f "HIKMAH__knowledge_index/_index.json" && grep -q "strict_local" "HIKMAH__knowledge_index/README.md" && python -m json.tool "HIKMAH__knowledge_index/_index.json" > /dev/null</automated>
  </verify>
  <done>README.md created with comprehensive module documentation; _index.json created with self-registration metadata</done>
</task>

<task type="auto">
  <name>Task 2: Register module and ledger in NIZAM_TEMPLE.json</name>
  <files>NIZAM__system/NIZAM_TEMPLE.json</files>
  <action>
    Update NIZAM_TEMPLE.json to register the HIKMAH__knowledge_index module and PERSONA_KNOWLEDGE_INDEX ledger:
    
    1. **Read existing NIZAM_TEMPLE.json** to understand current module/ledger structure
    
    2. **Add module entry** (in "modules" section or appropriate location):
       ```json
       {
         "module": "HIKMAH__knowledge_index",
         "codename": "Persona Knowledge Index",
         "purpose": "Per-persona knowledge state tracking with topics, activity history, blockers, and context snapshots. Foundation for v1.1 adaptive messaging.",
         "phase": 14,
         "privacy": "strict_local",
         "indices_location": "HIKMAH__knowledge_index/indices/{PERSONA}_index.json",
         "schema_version": "1.0",
         "downstream_consumers": ["Phase 15: Data Refresh", "Phase 16: Message Generation", "Phase 17: Delivery Tracking", "Phase 18: Adaptation", "Phase 20: Validation"],
         "status": "active"
       }
       ```
    
    3. **Add ledger entry** (in "ledgers" section):
       ```json
       {
         "name": "PERSONA_KNOWLEDGE_INDEX",
         "privacy": "strict_local",
         "phase": 14,
         "purpose": "Append-only ledger tracking all knowledge index mutations per persona (topic creates, accomplishments, blockers, completions, context snapshots)",
         "writer": "HIKMAH__knowledge_index/index/writer.py",
         "format": "JSONL (append-only, hash-chained)",
         "retention": "permanent",
         "note": "Critical audit trail; must be preserved across schema migrations via MAKHZAN snapshots"
       }
       ```
    
    4. **Preserve existing entries** — only add new entries, do not modify other modules/ledgers
    
    5. **Use json.tool to validate** the resulting JSON before committing
    </action>
  <verify>
    <automated>python -m json.tool "NIZAM__system/NIZAM_TEMPLE.json" > /dev/null && grep -q "HIKMAH__knowledge_index" "NIZAM__system/NIZAM_TEMPLE.json" && grep -q "PERSONA_KNOWLEDGE_INDEX" "NIZAM__system/NIZAM_TEMPLE.json"</automated>
  </verify>
  <done>NIZAM_TEMPLE.json updated with module and ledger registrations; JSON valid</done>
</task>

<task type="auto">
  <name>Task 3: Add privacy classification rules to PRIVACY_CLASSIFICATION.json</name>
  <files>NIZAM__system/policies/PRIVACY_CLASSIFICATION.json</files>
  <action>
    Update PRIVACY_CLASSIFICATION.json to enforce strict_local classification on knowledge indices and ledger:
    
    1. **Read existing PRIVACY_CLASSIFICATION.json** to understand rule structure
    
    2. **Add classification rules** (in "rules" array or appropriate location):
       
       Rule 1 — Per-persona indices:
       ```json
       {
         "path_glob": "HIKMAH__knowledge_index/indices/*.json",
         "classification": "strict_local",
         "reason": "Per-persona knowledge state with sensitive context tracking; never egressed to GitHub, Drive, or Telegram",
         "enforcement": "HIMAYAH gate blocks sync; .gitignore prevents commit",
         "phase": 14,
         "note": "Owner: Seif only; encrypted volume protection assumed"
       }
       ```
       
       Rule 2 — PERSONA_KNOWLEDGE_INDEX ledger:
       ```json
       {
         "path_glob": "NIZAM__system/ledgers/PERSONA_KNOWLEDGE_INDEX.jsonl",
         "classification": "strict_local",
         "reason": "Audit trail of all knowledge index mutations; contains sensitive context and activity tracking",
         "enforcement": "HIMAYAH gate blocks sync; .gitignore prevents commit",
         "phase": 14,
         "note": "Permanent ledger; archived via MAKHZAN snapshots on schema migration"
       }
       ```
    
    3. **Preserve existing rules** — only add new rules, do not modify other classifications
    
    4. **Validate JSON** before committing
    </action>
  <verify>
    <automated>python -m json.tool "NIZAM__system/policies/PRIVACY_CLASSIFICATION.json" > /dev/null && grep -q "HIKMAH__knowledge_index/indices" "NIZAM__system/policies/PRIVACY_CLASSIFICATION.json" && grep -q "PERSONA_KNOWLEDGE_INDEX" "NIZAM__system/policies/PRIVACY_CLASSIFICATION.json"</automated>
  </verify>
  <done>PRIVACY_CLASSIFICATION.json updated with strict_local rules; enforcement via HIMAYAH gate confirmed</done>
</task>

<task type="auto">
  <name>Task 4: Update .gitignore to prevent index commits</name>
  <files>.gitignore</files>
  <action>
    Update project .gitignore to prevent strict_local index files from being accidentally committed:
    
    1. **Read existing .gitignore** to understand current patterns
    
    2. **Add exclusion rules:**
       ```
       # HIKMAH Knowledge Index — strict_local (never commit)
       HIKMAH__knowledge_index/indices/
       HIKMAH__knowledge_index/indices/*.json
       NIZAM__system/ledgers/PERSONA_KNOWLEDGE_INDEX.jsonl
       ```
    
    3. **Preserve existing rules** — only add new patterns for knowledge index
    
    4. **Verify no test files are excluded** — test_*.py files should still be included
    
    Pattern explanation:
    - HIKMAH__knowledge_index/indices/ — blocks entire per-persona index directory
    - NIZAM__system/ledgers/PERSONA_KNOWLEDGE_INDEX.jsonl — blocks the audit ledger
    - Allows: README.md, _index.json, Python code (schema.py, writer.py, main.py)
    </action>
  <verify>
    <automated>grep -q "HIKMAH__knowledge_index/indices" ".gitignore" && grep -q "PERSONA_KNOWLEDGE_INDEX" ".gitignore"</automated>
  </verify>
  <done>.gitignore updated to prevent strict_local files from being committed</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. NIZAM_TEMPLE.json includes HIKMAH__knowledge_index module entry
2. NIZAM_TEMPLE.json includes PERSONA_KNOWLEDGE_INDEX ledger entry
3. PRIVACY_CLASSIFICATION.json includes rules for indices/ directory (strict_local)
4. PRIVACY_CLASSIFICATION.json includes rules for ledger (strict_local)
5. .gitignore prevents indices/ and PERSONA_KNOWLEDGE_INDEX.jsonl from being committed
6. All JSON files remain valid (no syntax errors)
7. Module documentation explains strict_local constraints and Phase 15-20 integration points
</verification>

<success_criteria>
- Module fully registered in NIZAM Temple with privacy=strict_local
- Ledger fully registered in NIZAM Temple
- PRIVACY_CLASSIFICATION rules enforce strict_local on both indices and ledger
- .gitignore prevents accidental commits
- README documents privacy constraints prominently
- All downstream phases (15-20) are documented as consumers
- INDEX-02 requirement satisfied: "Index stored locally per persona in strict_local directory (not egressed)"
</success_criteria>

<output>
After completion, create `.planning/phases/14-knowledge-index-schema-storage/14-02-SUMMARY.md`
</output>
