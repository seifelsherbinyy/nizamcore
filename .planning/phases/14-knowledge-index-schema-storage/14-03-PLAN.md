---
phase: 14-knowledge-index-schema-storage
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - HIKMAH__knowledge_index/index/main.py
  - HIKMAH__knowledge_index/index/writer.py
  - HIKMAH__knowledge_index/data/init_manifest.json
autonomous: true
requirements:
  - INDEX-02
  - INDEX-04
must_haves:
  truths:
    - "initialize_persona_index(persona, target_dir) creates a valid index file for a single persona"
    - "initialize_all_personas(indices_dir) creates valid index files for all 11 personas in parallel"
    - "All created index files pass validate_index_schema() with (True, None)"
    - "Index files are created in correct location: HIKMAH__knowledge_index/indices/{PERSONA}_index.json"
    - "Each index has initialized_at and last_updated timestamps in ISO 8601 UTC format"
    - "Each index includes one activity_history entry: index_initialized event"
    - "Each index includes empty arrays: topics, completions, stalled_work"
    - "Each index includes initial context_snapshots with zero metrics"
    - "init_manifest.json records initialization timestamp, count of personas initialized, file paths"
  artifacts:
    - path: "HIKMAH__knowledge_index/index/main.py"
      provides: "initialize_persona_index() + initialize_all_personas() functions"
      min_lines: 80
    - path: "HIKMAH__knowledge_index/index/writer.py"
      provides: "write_index_to_file() and related I/O functions"
      min_lines: 50
    - path: "HIKMAH__knowledge_index/data/init_manifest.json"
      provides: "Initialization record (timestamp, personas, paths)"
      min_lines: 10
  key_links:
    - from: "HIKMAH__knowledge_index/index/main.py"
      to: "HIKMAH__knowledge_index/index/schema.py"
      via: "import validate_index_schema from schema module"
      pattern: "from.*schema import.*validate_index_schema"
    - from: "HIKMAH__knowledge_index/index/main.py"
      to: "HIKMAH__knowledge_index/indices/"
      via: "create JSON files on disk"
      pattern: "indices_dir.*PERSONA_index.json"
    - from: "HIKMAH__knowledge_index/index/writer.py"
      to: "NIZAM__system/ledgers/PERSONA_KNOWLEDGE_INDEX.jsonl"
      via: "append initialization records to ledger"
      pattern: "PERSONA_KNOWLEDGE_INDEX"
---

<objective>
Implement initialization functions to create valid, empty knowledge index files for all 11 personas in the strict_local indices/ directory.

Purpose: Provide the entry point for Phase 14 initialization. These functions will be called once during setup to create the per-persona JSON scaffolds that Phase 15+ will populate and modify.

Output: main.py with initialize_persona_index() and initialize_all_personas() functions; writer.py with I/O helpers; init_manifest.json recording the initialization event; INDEX-02 and INDEX-04 requirements satisfied.
</objective>

<execution_context>
@D:\Users\PC\.claude\get-shit-done\workflows\execute-plan.md
@D:\Users\PC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-knowledge-index-schema-storage/14-RESEARCH.md

# Requires schema.py from Wave 1 (Plan 01)
# 11 personas: AMMAR, HIKMAH, TARIQ, MUNAWARA, MAL, BADAN, NAQD, SHURA, TAFRIGH, MARSAD, NIZAM
# Indices directory: HIKMAH__knowledge_index/indices/ (strict_local)
# Each index initialized with empty topics/completions/activity_history arrays
# Use stdlib only: json, pathlib, uuid, datetime, typing
# Validation: all created indices must pass validate_index_schema()
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement index initialization logic in main.py</name>
  <files>HIKMAH__knowledge_index/index/main.py</files>
  <behavior>
    Test 1: initialize_persona_index("AMMAR", temp_dir) creates file at temp_dir/AMMAR_index.json
    Test 2: Created index file is valid JSON and passes validate_index_schema()
    Test 3: Created index has version="1.0" and persona="AMMAR"
    Test 4: Created index has initialized_at and last_updated in ISO 8601 format (2026-06-XX...Z)
    Test 5: Created index has empty topics[], completions[], stalled_work[] arrays
    Test 6: Created index has activity_history with one entry of event_type="index_initialized"
    Test 7: Created index has context_snapshots with one snapshot having all zero metrics
    Test 8: initialize_persona_index() with invalid persona raises ValueError with clear message
    Test 9: initialize_persona_index() creates directory if it doesn't exist (parents=True)
    Test 10: initialize_all_personas(temp_dir) creates indices for all 11 personas
    Test 11: initialize_all_personas() returns dict mapping {"AMMAR": path, "HIKMAH": path, ...}
    Test 12: All 11 created indices pass validate_index_schema()
    Test 13: All 11 persona files are named correctly ({PERSONA}_index.json)
  </behavior>
  <action>
    Create HIKMAH__knowledge_index/index/main.py with initialization functions:
    
    1. **Function: initialize_persona_index(persona: str, target_dir: Path) -> Path**
       - Validate persona is in VALID_PERSONAS list; raise ValueError if not
       - Create template index with fields:
         - version: "1.0"
         - persona: {persona_name}
         - initialized_at: datetime.now(timezone.utc).isoformat()
         - last_updated: same as initialized_at
         - topics: []
         - completions: []
         - activity_history: [{ts: now, event_type: "index_initialized", persona: {persona}, description: "Knowledge index initialized for {persona}"}]
         - stalled_work: []
         - context_snapshots: [{ts: now, snapshot: {open_topic_count: 0, active_blocker_count: 0, recent_accomplishments_count: 0, completion_rate_7d: 0.0, engagement_level: "unknown"}}]
         - metadata: {source: "v1.1-knowledge-index", locale: "Egypt/Cairo", language: "en"}
       - Validate schema using validate_index_schema(index); raise ValueError if invalid
       - Create target_dir if missing (parents=True, exist_ok=True)
       - Write index to {target_dir}/{persona}_index.json using json.dumps(indent=2, ensure_ascii=False)
       - Return Path to created file
    
    2. **Function: initialize_all_personas(indices_dir: Path) -> dict[str, Path]**
       - Define PERSONAS = ["AMMAR", "HIKMAH", "TARIQ", "MUNAWARA", "MAL", "BADAN", "NAQD", "SHURA", "TAFRIGH", "MARSAD", "NIZAM"]
       - Loop over each persona; call initialize_persona_index(persona, indices_dir)
       - Return mapping {persona: path_to_file}
       - Raise error if any persona initialization fails (abort early, not silent)
    
    3. **Imports:**
       - from HIKMAH__knowledge_index.index.schema import validate_index_schema, VALID_PERSONAS
       - from datetime import datetime, timezone
       - from pathlib import Path
       - import json
       - from typing import Optional, Dict
    
    4. **Error handling:**
       - Raise ValueError with descriptive messages (e.g., "Unknown persona: XYZ")
       - Raise FileNotFoundError if target_dir cannot be created
       - Log to console (print) if initialization succeeds for each persona
    
    5. **Documentation:**
       - Module docstring: "Index initialization logic for all personas"
       - Function docstrings with args, return, raises
       - Example: initialize_all_personas(Path("HIKMAH__knowledge_index/indices"))
    </action>
  <verify>
    <automated>python -m pytest HIKMAH__knowledge_index/tests/test_index_initialization.py::test_initialize_persona_index -xvs 2>/dev/null || echo "Tests in Wave 4"</automated>
  </verify>
  <done>main.py created with initialize_persona_index() and initialize_all_personas() functions; all indices pass schema validation</done>
</task>

<task type="auto">
  <name>Task 2: Implement ledger writer for initialization events</name>
  <files>HIKMAH__knowledge_index/index/writer.py</files>
  <action>
    Create HIKMAH__knowledge_index/index/writer.py with ledger I/O functions:
    
    1. **Function: write_index_to_file(index: dict, target_path: Path) -> Path**
       - Serialize index to JSON with indent=2, ensure_ascii=False
       - Write to target_path
       - Return target_path
       - Raise IOError on write failure
    
    2. **Function: write_initialization_event_to_ledger(persona: str, file_path: Path, ledger_path: Path) -> str**
       - Create ledger entry (JSONL row) with:
         - ts: datetime.now(timezone.utc).isoformat()
         - ledger: "PERSONA_KNOWLEDGE_INDEX"
         - row_id: str(uuid.uuid4())
         - trace_id: str(uuid.uuid4())
         - actor: "auto_system"
         - action: "index_initialized"
         - persona: {persona}
         - module: "HIKMAH__knowledge_index"
         - privacy_class: "strict_local"
         - prev_hash: "genesis" (first row) or sha256 of prior row
         - row_hash: sha256(row_json) excluding the row_hash field itself
         - payload: {file_path: str(file_path), event: "initialization"}
       - Append to ledger_path as JSONL (one JSON object per line)
       - Create ledger_path directory if missing
       - Return row_id
    
    3. **Helper: compute_row_hash(row_dict: dict) -> str**
       - Serialize row to JSON (sort keys, no indent)
       - Compute sha256 of serialized JSON
       - Return hex digest
    
    4. **Imports:**
       - from datetime import datetime, timezone
       - from pathlib import Path
       - import json, uuid, hashlib
       - from typing import Optional
    
    5. **Error handling:**
       - Raise IOError if file write fails
       - Raise ValueError if row validation fails
    
    6. **Note:**
       - Ledger writes are deferred until after index files are created (called from main.py after success)
       - Ledger format mirrors existing NIZAM ledger pattern (EVENT_LEDGER, LEARNING_LEDGER)
    </action>
  <verify>
    <automated>python -c "from HIKMAH__knowledge_index.index.writer import write_index_to_file, write_initialization_event_to_ledger; print('writer.py imported successfully')"</automated>
  </verify>
  <done>writer.py created with write_index_to_file() and write_initialization_event_to_ledger() functions</done>
</task>

<task type="auto">
  <name>Task 3: Create initialization manifest record</name>
  <files>HIKMAH__knowledge_index/data/init_manifest.json</files>
  <action>
    Create HIKMAH__knowledge_index/data/init_manifest.json as a record of the phase initialization:
    
    1. **Manifest structure:**
       ```json
       {
         "phase": 14,
         "phase_name": "Knowledge Index Schema & Storage",
         "initialization_at": "2026-06-20T...",
         "operator": "auto_system",
         "personas_initialized": 11,
         "persona_list": ["AMMAR", "HIKMAH", "TARIQ", "MUNAWARA", "MAL", "BADAN", "NAQD", "SHURA", "TAFRIGH", "MARSAD", "NIZAM"],
         "indices_location": "HIKMAH__knowledge_index/indices/",
         "schema_version": "1.0",
         "status": "pending_execution",
         "note": "Created during Phase 14 planning; status updated after initialization completes"
       }
       ```
    
    2. **Initial status:** "pending_execution" (will be updated to "complete" after execution)
    
    3. **Use current timestamp in ISO 8601 UTC format**
    
    4. **This is a git-tracked file** (published_github classification; documents initialization state)
    </action>
  <verify>
    <automated>test -f "HIKMAH__knowledge_index/data/init_manifest.json" && python -m json.tool "HIKMAH__knowledge_index/data/init_manifest.json" > /dev/null</automated>
  </verify>
  <done>init_manifest.json created as initialization record; JSON valid</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. main.py imports successfully and exports initialize_persona_index, initialize_all_personas
2. writer.py imports successfully and exports write_index_to_file, write_initialization_event_to_ledger
3. initialize_all_personas(test_dir) creates 11 JSON files in test_dir
4. All 11 created files are valid JSON
5. All 11 files pass validate_index_schema()
6. All files have correct structure: version, persona, topics[], completions[], activity_history, stalled_work, context_snapshots, metadata
7. init_manifest.json exists and is valid JSON
8. No file I/O errors occur when creating indices directory or writing files
</verification>

<success_criteria>
- initialize_persona_index() creates valid index for single persona
- initialize_all_personas() creates valid indices for all 11 personas
- All indices stored in HIKMAH__knowledge_index/indices/{PERSONA}_index.json (correct path)
- All created indices pass validate_index_schema() with (True, None)
- Each index has initialized_at timestamp (ISO 8601 UTC)
- Each index has one activity_history entry (index_initialized event)
- write_initialization_event_to_ledger() appends JSONL records to PERSONA_KNOWLEDGE_INDEX.jsonl
- init_manifest.json records initialization metadata
- INDEX-02 requirement satisfied: "Index initialized per persona and stored locally in strict_local directory"
- INDEX-04 requirement satisfied: "Empty test run creates valid, readable index file with correct structure"
</success_criteria>

<output>
After completion, create `.planning/phases/14-knowledge-index-schema-storage/14-03-SUMMARY.md`
</output>
