---
phase: 14-knowledge-index-schema-storage
plan: 04
type: execute
wave: 3
depends_on: [14-01, 14-03]
files_modified:
  - HIKMAH__knowledge_index/index/versioning.py
  - NIZAM__system/docs/CONTINUITY_PROTOCOL.md
autonomous: true
requirements:
  - INDEX-03
must_haves:
  truths:
    - "Schema version field supports semantic versioning (1.0, 1.1, 1.2, etc.)"
    - "increment_schema_version() atomically updates all 11 persona indices to new version"
    - "Before version increment, MAKHZAN snapshot is created preserving old indices"
    - "Snapshot includes MANIFEST.json with change description, old version, new version, timestamp"
    - "Snapshot path follows pattern: MAKHZAN__archive/{ISO_TIMESTAMP}/HIKMAH__knowledge_index/indices/"
    - "Schema evolution is backward-compatible for 1.x versions (v1.1 adds optional fields, not removes)"
    - "Version mismatch detection catches indices at different schema versions and raises error"
    - "MAKHZAN snapshot can be used to rollback indices to prior version if needed"
  artifacts:
    - path: "HIKMAH__knowledge_index/index/versioning.py"
      provides: "increment_schema_version(), snapshot_indices_to_makhzan(), validate_schema_versions()"
      min_lines: 120
    - path: "NIZAM__system/docs/CONTINUITY_PROTOCOL.md"
      provides: "Updated with HIKMAH__knowledge_index versioning pattern"
      contains: "HIKMAH__knowledge_index"
  key_links:
    - from: "HIKMAH__knowledge_index/index/versioning.py"
      to: "MAKHZAN__archive/"
      via: "snapshot_indices_to_makhzan() creates snapshot directories"
      pattern: "MAKHZAN__archive.*ISO_TIMESTAMP"
    - from: "HIKMAH__knowledge_index/index/versioning.py"
      to: "HIKMAH__knowledge_index/indices/"
      via: "Reads all persona indices before version bump"
      pattern: "indices_dir.*PERSONA_index.json"
---

<objective>
Implement schema versioning support using MAKHZAN snapshot pattern for backward-compatible evolution.

Purpose: Enable future schema changes (e.g., adding new optional fields in 1.1) without breaking existing indices. MAKHZAN snapshots preserve old indices before version bumps, enabling rollback if needed.

Output: versioning.py with increment_schema_version(), snapshot functions, and validation; CONTINUITY_PROTOCOL.md updated with HIKMAH__knowledge_index pattern; INDEX-03 requirement satisfied.
</objective>

<execution_context>
@D:\Users\PC\.claude\get-shit-done\workflows\execute-plan.md
@D:\Users\PC\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/14-knowledge-index-schema-storage/14-RESEARCH.md
@NIZAM__system/docs/CONTINUITY_PROTOCOL.md

# MAKHZAN snapshot pattern proven in existing CONTINUITY_PROTOCOL
# Versioning strategy: 1.x = backward-compatible (optional new fields); 2.x = breaking changes
# Current schema version: 1.0 (initialized in Phase 14)
# Future versions: 1.1 (optional engagement_patterns array), 1.2 (future)
# All 11 persona indices must be upgraded atomically to prevent version mismatch
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement versioning functions in versioning.py</name>
  <files>HIKMAH__knowledge_index/index/versioning.py</files>
  <behavior>
    Test 1: validate_schema_versions(indices_dir) with all indices at v1.0 returns (True, None)
    Test 2: validate_schema_versions(indices_dir) with mixed versions (some v1.0, some v1.1) returns (False, error_msg)
    Test 3: snapshot_indices_to_makhzan(indices_dir, from_version, to_version, change_description) creates MAKHZAN__archive/{ISO_TIMESTAMP}/ directory
    Test 4: snapshot preserves all 11 index files in snapshot directory with original content
    Test 5: snapshot creates MANIFEST.json with from_version, to_version, change_description, timestamp, indices_backed_up count
    Test 6: increment_schema_version(indices_dir, old_version, new_version, change_description) calls snapshot first
    Test 7: increment_schema_version() updates all 11 indices to new version field value
    Test 8: increment_schema_version() updates last_updated field on all indices
    Test 9: After increment_schema_version(), all indices pass validate_schema_versions() with new version
    Test 10: increment_schema_version() with invalid old_version raises ValueError
    Test 11: increment_schema_version() with version="2.0" (major change) allows breaking changes
    Test 12: increment_schema_version() atomicity: if any index write fails, prior changes are rolled back (or all fail together)
  </behavior>
  <action>
    Create HIKMAH__knowledge_index/index/versioning.py with schema evolution support:
    
    1. **Function: validate_schema_versions(indices_dir: Path) -> tuple[bool, Optional[str]]**
       - List all {PERSONA}_index.json files in indices_dir
       - Read each file and extract version field
       - Confirm all versions are identical
       - Return (True, None) if all match; (False, error_msg) if mismatch
       - Raise error if any file is missing/unreadable
    
    2. **Function: snapshot_indices_to_makhzan(indices_dir: Path, from_version: str, to_version: str, change_description: str) -> Path**
       - Create MAKHZAN__archive/ directory if missing
       - Create timestamp-based subdirectory: MAKHZAN__archive/{ISO_TIMESTAMP}/
       - Create subdirectory: MAKHZAN__archive/{ISO_TIMESTAMP}/HIKMAH__knowledge_index/indices/
       - Copy all {PERSONA}_index.json files to snapshot location (preserving content exactly)
       - Create MANIFEST.json in MAKHZAN__archive/{ISO_TIMESTAMP}/:
         ```json
         {
           "trigger": "schema_version_increment",
           "from_version": "{from_version}",
           "to_version": "{to_version}",
           "change": "{change_description}",
           "snapshot_at": "{ISO_TIMESTAMP}",
           "indices_backed_up": 11,
           "operator": "auto_system",
           "recovery_note": "If rollback needed, restore indices from this snapshot and revert schema_version field"
         }
         ```
       - Return Path to snapshot directory
       - Raise IOError if any copy fails
    
    3. **Function: increment_schema_version(indices_dir: Path, old_version: str, new_version: str, change_description: str) -> dict**
       - Validate old_version matches all indices using validate_schema_versions(indices_dir)
       - Raise ValueError if version mismatch detected
       - Call snapshot_indices_to_makhzan(indices_dir, old_version, new_version, change_description)
       - For each {PERSONA}_index.json in indices_dir:
         - Read file to dict
         - Update version field to new_version
         - Update last_updated field to datetime.now(timezone.utc).isoformat()
         - Validate updated dict using validate_index_schema(); raise error if invalid
         - Write updated dict back to file
       - Return dict: {personas_updated: 11, snapshot_location: Path, manifest_location: Path}
       - Raise error if any write fails (abort early)
    
    4. **Function: validate_version_format(version: str) -> bool**
       - Version must match pattern: "^[1-9]+\.[0-9]+$" (e.g., 1.0, 1.1, 2.0, but not 0.5 or v1.0)
       - Return True if valid; False otherwise
    
    5. **Imports:**
       - from pathlib import Path
       - from datetime import datetime, timezone
       - import json, shutil
       - from typing import Optional, Dict
       - from HIKMAH__knowledge_index.index.schema import validate_index_schema
    
    6. **Error handling:**
       - Raise ValueError for version mismatches, invalid formats
       - Raise IOError for file I/O failures
       - Raise FileNotFoundError if indices_dir doesn't exist
    
    7. **Documentation:**
       - Module docstring: "Schema versioning via MAKHZAN snapshot pattern"
       - Function docstrings with examples
       - Example: increment_schema_version(Path("HIKMAH__knowledge_index/indices"), "1.0", "1.1", "Added engagement_patterns array")
    </action>
  <verify>
    <automated>python -m pytest HIKMAH__knowledge_index/tests/test_versioning.py -xvs 2>/dev/null || echo "Tests in Wave 4"</automated>
  </verify>
  <done>versioning.py created with schema versioning and MAKHZAN snapshot support; increment_schema_version() is atomic and validates before/after</done>
</task>

<task type="auto">
  <name>Task 2: Document versioning pattern in CONTINUITY_PROTOCOL.md</name>
  <files>NIZAM__system/docs/CONTINUITY_PROTOCOL.md</files>
  <action>
    Update NIZAM__system/docs/CONTINUITY_PROTOCOL.md to document HIKMAH__knowledge_index versioning pattern:
    
    1. **Read existing CONTINUITY_PROTOCOL.md** to understand current structure and patterns
    
    2. **Add section: "Knowledge Index Schema Evolution (HIKMAH__knowledge_index)"**
       - Title: "Knowledge Index Schema Evolution (HIKMAH__knowledge_index)"
       - Subsection: "Versioning Strategy"
         - Schema version field: semantic versioning (MAJOR.MINOR)
         - v1.x: backward-compatible changes (new optional fields; no removals)
         - v2.0+: breaking changes (field removals, type changes)
       - Subsection: "Snapshot Pattern"
         - Before any version bump: snapshot_indices_to_makhzan(indices_dir, old_version, new_version, change_description)
         - Creates MAKHZAN__archive/{ISO_TIMESTAMP}/HIKMAH__knowledge_index/indices/
         - Creates MANIFEST.json with change description, old/new versions, recovery notes
       - Subsection: "Atomic Updates"
         - increment_schema_version() updates all 11 persona indices atomically
         - Validates schema before/after using validate_index_schema()
         - Raises error if any update fails; no partial updates
       - Subsection: "Rollback Procedure"
         - If version bump causes issues: restore indices from MAKHZAN snapshot
         - Revert version field to old_version
         - Validate all indices post-rollback
       - Subsection: "Future Schema Changes"
         - Example v1.1 change: Add engagement_patterns array (optional, backward-compatible)
         - Example v2.0 change: Remove context_snapshots field (breaking, requires migration guide)
    
    3. **Keep existing CONTINUITY_PROTOCOL sections intact** — only add HIKMAH__knowledge_index section
    
    4. **Use Markdown formatting** consistent with existing protocol document
    
    5. **Cross-reference:** Link to HIKMAH__knowledge_index/index/versioning.py functions
    </action>
  <verify>
    <automated>grep -q "HIKMAH__knowledge_index" "NIZAM__system/docs/CONTINUITY_PROTOCOL.md" && grep -q "snapshot_indices_to_makhzan\|increment_schema_version" "NIZAM__system/docs/CONTINUITY_PROTOCOL.md"</automated>
  </verify>
  <done>CONTINUITY_PROTOCOL.md updated with HIKMAH__knowledge_index versioning pattern and rollback procedure</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. versioning.py imports successfully: `from HIKMAH__knowledge_index.index.versioning import increment_schema_version`
2. validate_schema_versions() detects version mismatches correctly
3. snapshot_indices_to_makhzan() creates MAKHZAN__archive/ directory with correct structure
4. increment_schema_version() atomically updates all 11 persona indices
5. CONTINUITY_PROTOCOL.md documents the versioning pattern with examples
6. All functions include proper error handling and validation
7. Schema evolution is properly sequenced: snapshot → validate old → update all → validate new
</verification>

<success_criteria>
- versioning.py provides increment_schema_version() for atomic schema bumps
- All 11 persona indices must be at same version (validate_schema_versions() enforces)
- Before any version bump, MAKHZAN snapshot preserves old indices with manifest
- Schema evolution supports backward-compatible 1.x versions and breaking 2.x versions
- Rollback procedure documented (restore from MAKHZAN, revert version)
- CONTINUITY_PROTOCOL.md includes HIKMAH__knowledge_index pattern
- INDEX-03 requirement satisfied: "Schema supports versioning and evolution via MAKHZAN snapshot pattern"
</success_criteria>

<output>
After completion, create `.planning/phases/14-knowledge-index-schema-storage/14-04-SUMMARY.md`
</output>
