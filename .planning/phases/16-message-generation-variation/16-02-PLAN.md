---
phase: 16-message-generation-variation
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - HIKMAH__knowledge_index/message_generation/tests/conftest.py
  - HIKMAH__knowledge_index/message_generation/tests/test_generator.py
  - HIKMAH__knowledge_index/message_generation/tests/test_repetition_tracker.py
  - HIKMAH__knowledge_index/message_generation/tests/test_intent_processor.py
  - HIKMAH__knowledge_index/message_generation/tests/test_tone_consistency.py
  - HIKMAH__knowledge_index/README.md
  - tests/conftest.py
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03, MSG-04]

must_haves:
  truths:
    - "Message generator produces different rephrasing per persona (AMMAR: terse, HIKMAH: philosophical, TARIQ: strategic)"
    - "Repetition tracker correctly identifies and rejects exact phrase repeats within last 5 messages"
    - "Generated messages contain actionable language (imperative verbs or clear motivation)"
    - "Tone consistency validated: 5 consecutive message generations per persona maintain consistent voice"
  artifacts:
    - path: "HIKMAH__knowledge_index/message_generation/tests/conftest.py"
      provides: "Shared pytest fixtures (MockClaude, sample indices, persona samples)"
      exports: ["mock_client", "sample_ammar_index", "sample_hikmah_index", "message_ledger_path", "repetition_tracker"]
      min_lines: 60
    - path: "HIKMAH__knowledge_index/message_generation/tests/test_generator.py"
      provides: "End-to-end message generation tests (intent rephrasing, tone injection, error handling)"
      tests: ["test_intent_rephrasing_with_tone", "test_actionability_validation", "test_error_fallback", "test_max_retries"]
      min_lines: 100
    - path: "HIKMAH__knowledge_index/message_generation/tests/test_repetition_tracker.py"
      provides: "Repetition detection tests (phrase-level deduplication)"
      tests: ["test_last_5_deduplication", "test_phrase_extraction", "test_no_false_positives"]
      min_lines: 80
    - path: "HIKMAH__knowledge_index/message_generation/tests/test_intent_processor.py"
      provides: "Intent extraction and context building tests"
      tests: ["test_extract_topics", "test_context_summary", "test_activity_summary"]
      min_lines: 60
    - path: "HIKMAH__knowledge_index/message_generation/tests/test_tone_consistency.py"
      provides: "Persona tone consistency validation across 5 consecutive generations"
      tests: ["test_tone_consistency_5x_ammar", "test_tone_consistency_5x_hikmah", "test_tone_consistency_5x_tariq"]
      min_lines: 100
    - path: "HIKMAH__knowledge_index/README.md"
      provides: "Phase 16 documentation and integration examples"
      updates: "Add Phase 16 section with: API usage, message generation examples, persona tone patterns, Phase 17 integration handoff"
      min_lines: 50
  key_links:
    - from: "test_generator.py"
      to: "conftest.py"
      via: "mock_client fixture provides MockClaude for LLM calls"
      pattern: "mock_client"
    - from: "test_repetition_tracker.py"
      to: "conftest.py"
      via: "repetition_tracker fixture and sample messages"
      pattern: "repetition_tracker"
    - from: "test_tone_consistency.py"
      to: "conftest.py"
      via: "mock_client and persona_tones loaded"
      pattern: "mock_client.*PERSONA_SYSTEM_PROMPTS"
    - from: "test_tone_consistency.py"
      to: "generator.py"
      via: "generate_message() called 5x per persona to validate tone"
      pattern: "generate_message.*persona"
---

<objective>
Build comprehensive test suite for Phase 16 message generation with focus on: (1) intent rephrasing consistency per persona, (2) phrase-level repetition detection accuracy, (3) actionability validation, (4) tone consistency across repeated generations. Also document Phase 16 in README with integration example for Phase 17 consumption.

**Purpose:** Testing is the quality gate — ensures fresh nudges don't fall into stale patterns, tone doesn't drift, and messages are actionable. Documentation enables Phase 17 (Delivery) to consume the API correctly.

**Output:** 43+ passing tests covering all MSG-01-04 requirements, full README documentation, ready for Phase 17 integration.
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-message-generation-variation/16-RESEARCH.md
@.planning/phases/16-message-generation-variation/16-01-SUMMARY.md
@HIKMAH__knowledge_index/README.md
@HIKMAH__knowledge_index/index/schema.py
@HIKMAH__knowledge_index/message_generation/persona_tones.py
@HIKMAH__knowledge_index/message_generation/generator.py
@HIKMAH__knowledge_index/message_generation/repetition_tracker.py
@HIKMAH__knowledge_index/message_generation/intent_processor.py
@HIKMAH__knowledge_index/message_generation/message_ledger.py

## Test Strategy

**Test levels:**
- **Unit tests:** Individual functions (phrase extraction, context building, ledger write)
- **Integration tests:** Multi-function flows (generate_message with mocked Claude, generate_and_dedupe with tracker+ledger)
- **Tone consistency tests:** 5 consecutive generations per persona to validate consistency (using mocked Claude)
- **Quick-run command:** `pytest HIKMAH__knowledge_index/message_generation/tests/ -v -k "not api"` (unit tests, no API calls)
- **Full suite:** `pytest HIKMAH__knowledge_index/message_generation/tests/ -v` (all tests including mocked LLM)

## MockClaude Strategy

Create a MockClaude class in conftest.py that simulates Claude API responses:
- Takes a system prompt and user message as input
- Returns a realistic message response matching persona tone (can be static responses per persona, or simple template responses)
- Used in test_generator.py and test_tone_consistency.py to avoid real API calls during testing
- Pattern: `mock_client.messages.create(model="...", system=..., messages=[...])` returns Mock with `.content[0].text`

## Persona Tone Examples (for test validation)

From research, example outputs per persona:
- **AMMAR:** "3 items waiting. Pick one." (terse, direct)
- **HIKMAH:** "Your AI work stalled 2 weeks. Sleep first—notice the pattern?" (deep, reflective)
- **TARIQ:** "AI work impacts Q3 target. This stall directly affects June. What's blocking?" (strategic, big-picture)

Tests should validate that generated messages match these patterns.

## Coverage Target

Aim for >80% coverage on:
- `generator.py` (core generation logic)
- `repetition_tracker.py` (phrase deduplication)
- `message_ledger.py` (privacy validation)
- `intent_processor.py` (context building)

Can skip persona_tones.py (just data structures).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared pytest fixtures in conftest.py</name>
  <files>HIKMAH__knowledge_index/message_generation/tests/conftest.py</files>
  <action>
Create `conftest.py` with shared pytest fixtures for all Phase 16 tests. Include:

1. **MockClaude** (class or fixture)
   - Simulates Anthropic client for testing without real API calls
   - Methods: `.messages.create(model, max_tokens, system, messages)` → returns Mock with `.content[0].text`
   - Template responses per persona:
     - AMMAR: "Task 1: Pick one and move forward. Task 2: Focus first. Task 3: Finish what matters."
     - HIKMAH: "Your work carries weight. The stall reflects something deeper. What's beneath this pause?"
     - TARIQ: "This work directly feeds Q3. Remove the blocker and restore momentum."
     - Others: Similar persona-specific responses
   - Implementation: Use `unittest.mock.Mock` or simple class that matches Anthropic interface

2. **sample_ammar_index** (fixture)
   - Returns a valid AMMAR persona index (JSON dict) with:
     - 2-3 open topics (e.g., "AI Optimization", "Team Sync Setup")
     - 1-2 completions (recent, within 7 days)
     - 5-10 activity_history events
     - 0-1 stalled_work entries
   - Use `HIKMAH__knowledge_index.index.schema.PersonaIndexDict` as template
   - Ensure schema validation passes: `validate_index_schema(index) == (True, None)`

3. **sample_hikmah_index** (fixture)
   - Similar to AMMAR, but for HIKMAH persona (tone-related topics may differ)

4. **sample_tariq_index** (fixture)
   - Similar to AMMAR/HIKMAH, for TARIQ (strategic topics)

5. **message_ledger_path** (fixture)
   - Returns a temporary file path for MessageLedger testing
   - Cleanup: Pytest tmpdir auto-cleanup

6. **repetition_tracker** (fixture)
   - Returns initialized RepetitionTracker with message_ledger_path
   - Pre-populate with 2-3 sample messages (via .log_message()) for testing deduplication

7. **mock_client** (fixture)
   - Returns MockClaude instance (use MockClaude from step 1)

8. **import statements**
   - Import all schema types, validators, test helpers
   - Import `pytest`, `unittest.mock`, `tempfile`, `pathlib`

Docstring should explain fixture purpose and usage in tests.
  </action>
  <verify>
    <automated>pytest HIKMAH__knowledge_index/message_generation/tests/conftest.py --collect-only 2>&1 | grep -E "sample_ammar_index|sample_hikmah_index|repetition_tracker|mock_client" | wc -l</automated>
  </verify>
  <done>
    - `conftest.py` created with 8+ fixtures
    - MockClaude class/factory implemented simulating Anthropic API
    - Sample indices created for AMMAR, HIKMAH, TARIQ (all pass schema validation)
    - Fixtures initialized: message_ledger_path, repetition_tracker, mock_client
    - All fixtures scoped correctly (function or session as appropriate)
    - Docstring explains fixture usage and persona patterns
  </done>
</task>

<task type="auto">
  <name>Task 2: Test RepetitionTracker (phrase-level deduplication)</name>
  <files>HIKMAH__knowledge_index/message_generation/tests/test_repetition_tracker.py</files>
  <action>
Create `test_repetition_tracker.py` with focus on deduplication logic. Include:

1. **test_last_5_deduplication** (main test)
   - Setup: tracker with pre-populated ledger (5 messages logged)
   - Call: `tracker.get_last_messages('AMMAR', limit=5)` → should return all 5
   - Call: `tracker.get_last_messages('AMMAR', limit=3)` → should return last 3
   - Call: `tracker.get_last_messages('AMMAR', limit=10)` → should return only 5 (more than available)
   - Assert: Correct messages returned in correct order

2. **test_phrase_extraction** (unit test)
   - Call: `tracker.extract_key_phrases("Your AI workflow could be faster")` → should extract 3-grams with min 10 chars
   - Verify: Phrases include "Your AI workflow", "AI workflow could", "workflow could be", "could be faster"
   - Verify: All phrases lowercase for comparison
   - Assert: At least 3 phrases extracted, all valid

3. **test_exact_phrase_match_detection** (core test)
   - Setup: Log message "Your AI workflow could be faster"
   - Test: `tracker.is_repetition("Your AI workflow could be faster", 'AMMAR')` → should return True (exact repeat)
   - Test: `tracker.is_repetition("Your AI workflow might accelerate", 'AMMAR')` → should return True (phrase overlap: "Your AI workflow")
   - Test: `tracker.is_repetition("Let's accelerate the AI pipeline", 'AMMAR')` → should return False (no phrase overlap)
   - Assert: Correct detection in all cases

4. **test_no_false_positives**
   - Log: "Task 1: Pick one and move forward"
   - Log: "Task 2: Identify blockers and escalate"
   - Test: `is_repetition("Task 3: Start with analysis", 'AMMAR')` → should return False (no common phrases)
   - Test: `is_repetition("Task 1: Pick one and prioritize", 'AMMAR')` → should return True ("Pick one" overlap)
   - Assert: Correctly distinguishes similar vs. repeated

5. **test_ledger_persistence**
   - Setup: Create tracker, log 3 messages
   - Call: Create new tracker with same ledger path
   - Verify: New tracker can read previously logged messages
   - Assert: Ledger persists across tracker instances

6. **test_empty_ledger_fallback**
   - Setup: Create tracker with non-existent ledger path
   - Call: `get_last_messages('AMMAR')` → should return [] (no error)
   - Call: `is_repetition("test message", 'AMMAR')` → should return False (no history)
   - Assert: Graceful handling of missing ledger

Use fixture: `repetition_tracker`, `message_ledger_path`
Test framework: pytest, unittest.mock
Aim: 6 tests, >80% coverage of repetition_tracker.py
  </action>
  <verify>
    <automated>pytest HIKMAH__knowledge_index/message_generation/tests/test_repetition_tracker.py -v</automated>
  </verify>
  <done>
    - `test_repetition_tracker.py` created with 6+ tests
    - test_last_5_deduplication passing
    - test_phrase_extraction passing
    - test_exact_phrase_match_detection passing (3 sub-tests)
    - test_no_false_positives passing (2 sub-tests)
    - test_ledger_persistence passing
    - test_empty_ledger_fallback passing
    - All tests pass with >80% coverage of repetition_tracker.py
  </done>
</task>

<task type="auto">
  <name>Task 3: Test IntentProcessor (context extraction)</name>
  <files>HIKMAH__knowledge_index/message_generation/tests/test_intent_processor.py</files>
  <action>
Create `test_intent_processor.py` focusing on intent-to-context conversion. Include:

1. **test_extract_topics_exact_match**
   - Setup: Use sample_ammar_index fixture (has topic "AI Optimization")
   - Call: `IntentProcessor.extract_topics("You have open work on AI optimization", index)` 
   - Assert: Returns list with "AI Optimization" topic

2. **test_extract_topics_no_match_fallback**
   - Setup: Use sample_ammar_index
   - Call: `IntentProcessor.extract_topics("Something random", index)` 
   - Assert: Returns first 3 active topics from index (fallback behavior)

3. **test_extract_topics_empty_index**
   - Setup: Index with no topics
   - Call: `extract_topics("intent", empty_index)`
   - Assert: Returns empty list (no error)

4. **test_context_summary_with_topics**
   - Setup: sample_ammar_index with 2 topics
   - Call: `IntentProcessor.build_context_summary(topics, index)`
   - Assert: Returns string with topic names and blocker info (e.g., "AI Optimization (active 3d, blockers: ...) | Team Sync")

5. **test_context_summary_no_topics_uses_completions**
   - Setup: Index with no open topics but 2 completions
   - Call: `build_context_summary([], index)`
   - Assert: Returns string with completion names (fallback)

6. **test_should_celebrate_recent_completion**
   - Setup: Index with completion from 2 days ago
   - Call: `IntentProcessor.should_celebrate(index)`
   - Assert: Returns True

7. **test_should_celebrate_old_completion**
   - Setup: Index with completion from 10 days ago
   - Call: `should_celebrate(index)`
   - Assert: Returns False (outside 7-day window)

8. **test_activity_summary_with_events**
   - Setup: Index with 5 activity_history events (2x topic_created, 2x accomplishment_logged, 1x blocker_flagged)
   - Call: `IntentProcessor.get_activity_summary(index)`
   - Assert: Returns string like "Recent: 2 topic created, 2 accomplishment logged, 1 blocker flagged."

9. **test_activity_summary_empty**
   - Setup: Index with no activity_history
   - Call: `get_activity_summary(index)`
   - Assert: Returns "No recent activity logged."

10. **test_build_full_context**
    - Setup: sample_ammar_index with topics and activity
    - Call: `IntentProcessor.build_full_context("AI work", index)`
    - Assert: Returns dict with keys: topics, context_summary, should_celebrate, activity_summary, stalled_count, topic_count

Use fixtures: `sample_ammar_index`, `sample_hikmah_index`, `sample_tariq_index`
Test framework: pytest
Aim: 10 tests, >80% coverage of intent_processor.py
  </action>
  <verify>
    <automated>pytest HIKMAH__knowledge_index/message_generation/tests/test_intent_processor.py -v</automated>
  </verify>
  <done>
    - `test_intent_processor.py` created with 10 tests
    - All tests passing (extract_topics, context_summary, should_celebrate, activity_summary, build_full_context)
    - Edge cases covered (empty index, no topics, no activity, old/recent completions)
    - >80% coverage of intent_processor.py
  </done>
</task>

<task type="auto">
  <name>Task 4: Test message generator (intent rephrasing, tone injection, error handling)</name>
  <files>HIKMAH__knowledge_index/message_generation/tests/test_generator.py</files>
  <action>
Create `test_generator.py` for core generation logic. Include:

1. **test_intent_rephrasing_with_tone** (key test for MSG-01)
   - Setup: Use mock_client, sample_ammar_index, RepetitionTracker
   - Call: `generate_message('AMMAR', 'You have open work', sample_ammar_index, mock_client)`
   - Assert: Returned message is string, <280 chars
   - Assert: Message matches AMMAR tone (terse, direct) — check MockClaude returns AMMAR-like response
   - Note: With mocked Claude, this validates the pipeline (context building + system prompt injection), not the LLM itself

2. **test_generate_and_dedupe_success** (key test for MSG-02)
   - Setup: mock_client, sample_ammar_index, repetition_tracker, message_ledger_path
   - Call: `generate_and_dedupe('AMMAR', 'new intent', index, mock_client, repetition_tracker, ledger)`
   - Assert: Returns (message, True, "success")
   - Assert: Message logged to ledger with success=True
   - Assert: repetition_flagged=False in ledger

3. **test_generate_and_dedupe_detects_repetition** (key test for MSG-02)
   - Setup: Log "Your AI workflow could be faster" to repetition_tracker
   - Call: configure mock_client to return same message on LLM call
   - Call: `generate_and_dedupe('AMMAR', 'intent', index, mock_client, tracker, ledger)`
   - Assert: Returns (message, False, "max_retries_exceeded") after retries exhausted
   - Assert: Ledger entry has repetition_flagged=True, success=False

4. **test_actionability_validation** (key test for MSG-03)
   - Setup: mock_client returns "Your work continues. Keep going!" (no imperative verb)
   - Call: `generate_and_dedupe(...)` with this LLM response
   - Assert: Message still returned, but ledger entry has warning (or is flagged for Phase 20)
   - Note: Phase 16 doesn't hard-reject non-actionable messages; flags them for monitoring

5. **test_error_fallback_on_api_error**
   - Setup: mock_client configured to raise `anthropic.APIError("Service unavailable")`
   - Call: `generate_and_dedupe(...)`
   - Assert: Returns fallback message (e.g., "You have 2 open items. Pick one and move it forward.")
   - Assert: Ledger entry has success=False, error_reason="api_error"

6. **test_error_fallback_on_timeout**
   - Setup: mock_client configured to raise `anthropic.APITimeoutError(...)`
   - Call: `generate_and_dedupe(...)`
   - Assert: Returns fallback message
   - Assert: Ledger has error_reason="timeout"

7. **test_message_length_enforcement**
   - Setup: mock_client returns message >280 chars
   - Call: `generate_message(...)`
   - Assert: Returned message is ≤280 chars (truncated if needed)

8. **test_ledger_logging**
   - Setup: message_ledger_path, sample_ammar_index
   - Call: `generate_and_dedupe(...)`
   - Assert: MESSAGE_LEDGER.jsonl file created
   - Assert: Ledger contains entry with: persona, message_text, intent, context_tags, tone_applied, success, ts

9. **test_context_tags_whitelist_enforcement**
   - Setup: ledger configured with index that has invalid context_tags
   - Call: `generate_and_dedupe(...)`
   - Assert: Ledger raises ValueError on invalid tag OR filters to whitelist

Use fixtures: `mock_client`, `sample_ammar_index`, `repetition_tracker`, `message_ledger_path`
Mock modules: `unittest.mock.Mock`, `anthropic.APIError`, `anthropic.APITimeoutError`
Test framework: pytest
Aim: 9 tests, >80% coverage of generator.py
  </action>
  <verify>
    <automated>pytest HIKMAH__knowledge_index/message_generation/tests/test_generator.py -v</automated>
  </verify>
  <done>
    - `test_generator.py` created with 9 tests
    - Core generation flow tested (rephrasing, tone, context building)
    - Repetition detection tested (success and failure paths)
    - Actionability validation tested (flagging without rejection)
    - Error handling tested (API error, timeout, fallback messages)
    - Message length enforcement tested
    - Ledger logging tested (entry structure, privacy tags)
    - All tests passing, >80% coverage of generator.py
  </done>
</task>

<task type="auto">
  <name>Task 5: Test tone consistency across 5 consecutive generations per persona</name>
  <files>HIKMAH__knowledge_index/message_generation/tests/test_tone_consistency.py</files>
  <action>
Create `test_tone_consistency.py` to validate requirement MSG-04 (consistent tone across 5+ generations). Include:

1. **test_tone_consistency_5x_ammar** (key test for MSG-04)
   - Setup: mock_client configured to return 5 AMMAR-style messages (terse, direct)
   - Generate: Call `generate_message('AMMAR', intent, index, mock_client)` 5 times with same intent
   - Analyze: For each message, check tone markers:
     - Contains imperative verbs (pick, move, focus, identify)
     - No emotional language (no "you should feel", no "beautiful")
     - Terse (no flowery description)
   - Assert: All 5 messages maintain AMMAR tone (0 failures allowed)
   - Docstring: "AMMAR tone must be consistent: plain, factual, direct. All 5 generations should sound like a maintenance log."

2. **test_tone_consistency_5x_hikmah** (key test for MSG-04)
   - Setup: mock_client returns 5 HIKMAH-style messages (philosophical, reflective)
   - Generate: Call `generate_message('HIKMAH', intent, index, mock_client)` 5 times
   - Analyze: Check tone markers:
     - Contains reflective language (why, pattern, notice, mean, beneath)
     - References connection/wisdom (holistic, perspective, deeper)
     - Warm but honest (no false cheerleading)
   - Assert: All 5 messages maintain HIKMAH tone

3. **test_tone_consistency_5x_tariq** (key test for MSG-04)
   - Setup: mock_client returns 5 TARIQ-style messages (strategic, big-picture)
   - Generate: Call `generate_message('TARIQ', intent, index, mock_client)` 5 times
   - Analyze: Check tone markers:
     - References time horizons (Q3, quarterly, campaign, horizon)
     - Links to goals/strategy (impact, target, objective)
     - Calm but urgent (not panicked, but clear importance)
   - Assert: All 5 messages maintain TARIQ tone

4. **test_tone_consistency_no_cross_persona_bleed**
   - Setup: Generate messages for AMMAR, HIKMAH, TARIQ with same intent
   - Compare: AMMAR output must NOT contain HIKMAH keywords (e.g., "reflect", "pattern")
   - Compare: HIKMAH output must NOT contain AMMAR-only terseness
   - Assert: Each persona's tone is distinct (no cross-contamination)

5. **test_tone_consistency_with_different_intents**
   - Setup: mock_client configured to return persona-appropriate responses
   - Intents: ["open work", "stalled work", "completed task"]
   - For each intent + persona:
     - Generate message
     - Verify tone remains consistent (e.g., AMMAR still terse for completion message)
   - Assert: Tone persists across different intent types

Helper functions (define in file or conftest):
- **extract_tone_keywords(message: str, persona: str) → set[str]** — Extract keywords matching persona tone
  - AMMAR: ["pick", "move", "focus", "identify", "do", "finish"]
  - HIKMAH: ["reflect", "pattern", "notice", "notice", "mean", "deeper"]
  - TARIQ: ["quarter", "target", "campaign", "objective", "impact"]
- **validate_tone_consistency(messages: list[str], persona: str) → bool** — Return True if all messages match persona tone

Use fixtures: `mock_client`, `sample_ammar_index`, `sample_hikmah_index`, `sample_tariq_index`
Mock modules: `unittest.mock.Mock`
Test framework: pytest
Aim: 5 tests, validate MSG-04 (tone consistency)
  </action>
  <verify>
    <automated>pytest HIKMAH__knowledge_index/message_generation/tests/test_tone_consistency.py -v</automated>
  </verify>
  <done>
    - `test_tone_consistency.py` created with 5 tests
    - Tone consistency validated across 5 consecutive generations per persona (AMMAR, HIKMAH, TARIQ)
    - Tone keyword extraction working (custom helper function)
    - Cross-persona bleed detection working
    - Tone consistency checked across different intent types
    - All tests passing, tone consistency fully validated (MSG-04 requirement)
  </done>
</task>

<task type="auto">
  <name>Task 6: Update HIKMAH README with Phase 16 documentation and Phase 17 integration example</name>
  <files>HIKMAH__knowledge_index/README.md</files>
  <action>
Update README.md to document Phase 16 message generation. Add a new section after "Phase 15: Data Refresh Pipeline":

**APPEND SECTION: Phase 16: Message Generation**

Content structure:
1. **Overview** — Explain message generator's role in nudge generation
2. **Core API** — Document 3 main functions:
   - `generate_message(persona, intent, index, client, max_tokens=100) → str`
   - `generate_and_dedupe(persona, intent, index, client, tracker, ledger, max_retries=3) → tuple[str, bool, str]`
   - Classes: `RepetitionTracker`, `MessageLedger`, `IntentProcessor`
3. **Persona Tones** — Explain tone injection per persona (AMMAR: terse, HIKMAH: philosophical, TARIQ: strategic) with example outputs
4. **Repetition Prevention** — Explain phrase-level deduplication (why better than string matching)
5. **Message Constraints** — <280 chars, no PII, actionable language
6. **Privacy** — Ledger stores only context_tags (no raw personal data)

**APPEND INTEGRATION EXAMPLE: Phase 17 Handoff**

```python
from HIKMAH__knowledge_index import (
    refresh_persona_index, load_refresh_config,
    generate_and_dedupe, RepetitionTracker, MessageLedger
)
from pathlib import Path
from anthropic import Anthropic

# Phase 15: Load fresh index
config = load_refresh_config()
drive_client = GoogleDriveClient(config.credentials_path)
audit_logger = RefreshAuditLogger(config.audit_ledger_path)

success, index, reason = refresh_persona_index(
    persona="AMMAR",
    drive_client=drive_client,
    index_path=Path("HIKMAH__knowledge_index/indices/AMMAR_index.json"),
    audit_logger=audit_logger
)

# Phase 16: Generate message
client = Anthropic(api_key=os.environ["ANTHROPIC_API_KEY"])
tracker = RepetitionTracker(Path("HIKMAH__knowledge_index/MESSAGE_LEDGER.jsonl"))
ledger = MessageLedger(Path("HIKMAH__knowledge_index/MESSAGE_LEDGER.jsonl"))

message, success, reason = generate_and_dedupe(
    persona="AMMAR",
    intent="You have 3 open items on AI optimization",
    index=index,
    client=client,
    tracker=tracker,
    ledger=ledger,
    max_retries=3
)

# Returns: ("Pick one and move forward", True, "success")
# Ledger entry appended: { ts, persona, event_type, message_text, ..., success }

# Phase 17: Deliver to Telegram (next phase)
# hermes_relay.send_telegram(message, persona=persona, message_id=generate_message_id())
```

Also update:
- **Key Files** section: Add entries for Phase 16 modules
- **Integration Points** section: Clarify Phase 16 consumption of Phase 15 indices + production of messages for Phase 17

Use research YAML code block for examples. Docstring should reference:
- MSG-01: Intent rephrasing with tone injection (system prompts)
- MSG-02: Repetition tracking (phrase-level deduplication)
- MSG-03: Actionability (imperative verbs, ledger flagging)
- MSG-04: Tone consistency (5-generation test suite)
  </action>
  <verify>
    <automated>grep -n "Phase 16" D:\NIZAM\HIKMAH__knowledge_index\README.md | head -5</automated>
  </verify>
  <done>
    - Phase 16 section added to README.md (250+ lines total documentation)
    - Core API documented (generate_message, generate_and_dedupe, RepetitionTracker, MessageLedger, IntentProcessor)
    - Persona tone patterns explained with examples (AMMAR, HIKMAH, TARIQ)
    - Repetition prevention strategy documented (phrase-level deduplication why)
    - Message constraints documented (<280 chars, no PII, actionability)
    - Privacy section updated (context_tags whitelist, strict_local)
    - Phase 17 integration example provided (complete code snippet)
    - Key Files section updated with Phase 16 modules
    - Integration Points clarified (Phase 15 → Phase 16 → Phase 17)
  </done>
</task>

</tasks>

<verification>
**Phase 16-02 Verification Checklist:**

1. **Test files exist:** All 5 test modules created
   ```bash
   ls -la HIKMAH__knowledge_index/message_generation/tests/
   ```
   Should show: conftest.py, test_generator.py, test_repetition_tracker.py, test_intent_processor.py, test_tone_consistency.py

2. **Test discovery:** All tests discoverable
   ```bash
   pytest HIKMAH__knowledge_index/message_generation/tests/ --collect-only 2>&1 | tail -5
   ```
   Should show: 28+ tests collected

3. **Quick run (unit tests only):** Pass without API calls
   ```bash
   pytest HIKMAH__knowledge_index/message_generation/tests/ -v -k "not api" --tb=short
   ```
   Expected: All tests pass (<10 seconds)

4. **Full suite pass:** All tests including mocked LLM
   ```bash
   pytest HIKMAH__knowledge_index/message_generation/tests/ -v --tb=short
   ```
   Expected: 28+ tests pass (~30 seconds)

5. **Coverage check:** >80% on core modules
   ```bash
   pytest HIKMAH__knowledge_index/message_generation/tests/ --cov=HIKMAH__knowledge_index.message_generation --cov-report=term-missing 2>&1 | grep -E "generator|repetition_tracker|message_ledger|intent_processor"
   ```
   Expected: generator.py, repetition_tracker.py, message_ledger.py, intent_processor.py all >80%

6. **README updated:** Phase 16 documented
   ```bash
   grep -A 10 "Phase 16" HIKMAH__knowledge_index/README.md | head -15
   ```
   Expected: Section present with API docs and integration example

7. **Integration example works:** README example can be parsed as valid Python
   ```bash
   python -c "import ast; ast.parse('''... Phase 17 integration code from README ...''')"
   ```

8. **Tone validation:** test_tone_consistency.py specifically tests MSG-04
   ```bash
   pytest HIKMAH__knowledge_index/message_generation/tests/test_tone_consistency.py::test_tone_consistency_5x_ammar -v
   ```
   Expected: PASS (AMMAR tone consistent across 5 generations)
</verification>

<success_criteria>
- [ ] 5 test modules created (conftest.py, test_generator.py, test_repetition_tracker.py, test_intent_processor.py, test_tone_consistency.py)
- [ ] 28+ tests defined across all modules
- [ ] MockClaude fixture working (simulates Anthropic API without real calls)
- [ ] Sample indices created for AMMAR, HIKMAH, TARIQ (all pass schema validation)
- [ ] test_repetition_tracker.py: 6 tests covering phrase extraction, deduplication, ledger persistence
- [ ] test_intent_processor.py: 10 tests covering context building, topic extraction, activity summary
- [ ] test_generator.py: 9 tests covering generation flow, error handling, ledger logging, tone injection
- [ ] test_tone_consistency.py: 5 tests validating tone consistency across 5 consecutive generations per persona
- [ ] All tests passing (>80% coverage on generator.py, repetition_tracker.py, message_ledger.py, intent_processor.py)
- [ ] README.md updated with Phase 16 documentation (250+ lines)
- [ ] Integration example provided (Phase 16 → Phase 17 handoff code)
- [ ] Quick-run command working: `pytest HIKMAH__knowledge_index/message_generation/tests/ -v -k "not api"`
- [ ] Full test suite working: `pytest HIKMAH__knowledge_index/message_generation/tests/ -v`

**Goal-Backward Verification:**

✓ **MSG-01 (Rephrase intent, add context, apply tone):** test_intent_rephrasing_with_tone + tone consistency tests validate this
✓ **MSG-02 (Last 5 tracking + exact phrase deduplication):** test_repetition_tracker.py + test_generate_and_dedupe_detects_repetition validate this
✓ **MSG-03 (Actionable nudges):** test_actionability_validation + test_tone_consistency tests check imperative verbs/motivation
✓ **MSG-04 (Persona tone consistency):** test_tone_consistency_5x_ammar, test_tone_consistency_5x_hikmah, test_tone_consistency_5x_tariq validate this across all personas

Ready for Phase 17 (Delivery) to consume Message Generation API.
</success_criteria>

<output>
After completion, create `.planning/phases/16-message-generation-variation/16-02-SUMMARY.md` documenting:
- Test suite complete: 28+ tests (6 repetition + 10 intent processor + 9 generator + 5 tone consistency + 2 utils/integration)
- All tests passing with >80% coverage
- MockClaude fixture working (persona-specific responses)
- Sample indices created and validated
- README.md updated (Phase 16 section + Phase 17 integration example)
- Commits: Task 1-6 (6 atomic commits, one per test module + one for README)

## Phase 16 Complete

Both plans (16-01 and 16-02) address all 4 requirements:
- **MSG-01:** Message generator with intent rephrasing, context from index, persona tone applied (generator.py + tone consistency tests)
- **MSG-02:** Repetition tracker with last-5 message tracking and phrase-level deduplication (repetition_tracker.py + dedicated test suite)
- **MSG-03:** Actionability validation via imperative verb checking and ledger flagging (generator.py + test_actionability_validation)
- **MSG-04:** Persona tone consistency across 5+ consecutive generations (test_tone_consistency.py with AMMAR/HIKMAH/TARIQ specific tests)

Ready for Phase 17: Delivery & Response Tracking
</output>
