---
phase: 17-delivery-response-tracking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - HIKMAH__knowledge_index/delivery/__init__.py
  - HIKMAH__knowledge_index/delivery/message_id_generator.py
  - HIKMAH__knowledge_index/delivery/delivery_ledger.py
  - HIKMAH__knowledge_index/delivery/telegram_relay_client.py
  - HIKMAH__knowledge_index/__init__.py
  - HIKMAH__knowledge_index/README.md
autonomous: true
requirements: [DELIVERY-01, DELIVERY-02, DELIVERY-03, DELIVERY-04, DELIVERY-05]

must_haves:
  truths:
    - "Messages can be sent to Telegram via Hermes relay and receive unique message_id from API"
    - "Unique message IDs are generated for every send attempt, sortable by timestamp, no collisions"
    - "Message delivery events (sent_at, delivered_at, message_id) are recorded in immutable ledger"
    - "System can poll Telegram for user responses matching sent message_id via reply_to_message_id"
    - "Response received within 1-hour window is recorded with timestamp and correlation to sent message"
  artifacts:
    - path: "HIKMAH__knowledge_index/delivery/__init__.py"
      provides: "Public API for delivery module (MessageIDGenerator, DeliveryLedger, TelegramRelayClient)"
      exports: ["MessageIDGenerator", "DeliveryLedger", "TelegramRelayClient"]
    - path: "HIKMAH__knowledge_index/delivery/message_id_generator.py"
      provides: "Globally unique, sortable message ID generation (ULID-style format)"
      contains: "MessageIDGenerator class with generate() and parse() methods"
    - path: "HIKMAH__knowledge_index/delivery/delivery_ledger.py"
      provides: "JSONL append-only ledger for delivery events, response tracking, privacy enforcement"
      contains: "DeliveryLedger class with log_delivery(), log_response(), log_no_response(), query methods"
    - path: "HIKMAH__knowledge_index/delivery/telegram_relay_client.py"
      provides: "Abstraction layer for Hermes relay (send_message, get_updates, reply correlation)"
      contains: "TelegramRelayClient class wrapping relay infrastructure"
    - path: "HIKMAH__knowledge_index/DELIVERY_LEDGER.jsonl"
      provides: "Immutable delivery audit trail (created on first write)"
      format: "JSONL with delivery, response, engagement_window_closed events"
  key_links:
    - from: "MessageIDGenerator.generate()"
      to: "DeliveryLedger.log_delivery()"
      via: "message_id assigned before relay send"
      pattern: "message_id stored in ledger entry ts field"
    - from: "TelegramRelayClient.send_message()"
      to: "DeliveryLedger.log_delivery()"
      via: "delivery status and telegram_message_id captured on response"
      pattern: "response.result.message_id extracted after successful send"
    - from: "TelegramRelayClient.get_updates()"
      to: "DeliveryLedger.log_response()"
      via: "reply_to_message_id matched to sent telegram_message_id"
      pattern: "update.message.reply_to_message.message_id == sent_telegram_message_id"
---

<objective>
Build the foundation for Phase 17: message delivery infrastructure with unique ID generation, delivery ledger for immutable audit trail, and Telegram relay integration. Establish core classes that will be consumed by the delivery orchestrator and response monitor in Wave 2.

**Purpose:** Enable twice-daily scheduled Telegram message delivery with unique message IDs and persistent audit trail. Satisfy requirements DELIVERY-01, DELIVERY-02, DELIVERY-03 (message delivery tracking) and establish infrastructure for DELIVERY-04, DELIVERY-05 (response tracking in Wave 2).

**Output:** 
- MessageIDGenerator: Generates globally unique, sortable message IDs (ULID-style: MSG-YYYYMMDDHHMMSSMMMM-RANDOM)
- DeliveryLedger: JSONL ledger with delivery, response, and engagement window tracking
- TelegramRelayClient: Hermes relay wrapper with send_message() and get_updates() abstractions
- Public API in HIKMAH__knowledge_index.delivery module
- Updated parent README.md and __init__.py with Phase 17 documentation
</objective>

<execution_context>
@execute-plan.md
@summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-delivery-response-tracking/17-RESEARCH.md
@.planning/phases/16-message-generation-variation/16-01-SUMMARY.md
@HIKMAH__knowledge_index/README.md
</context>

<interfaces>
<!-- Key types from Phase 14-16 that Phase 17 depends on -->

From HIKMAH__knowledge_index/index/schema.py (Phase 14):
```python
PersonaIndexDict = TypedDict('PersonaIndexDict', {
    'persona': str,
    'version': str,
    'created_at': str,
    'updated_at': str,
    'topics': List[TopicDict],
    'activity_history': List[ActivityDict],
    'context_snapshot': ContextSnapshotDict,
    'metadata': Dict[str, Any]
}, total=False)
```

From HIKMAH__knowledge_index/message_generation/__init__.py (Phase 16):
```python
# Message generation returns (message_text, success, reason)
def generate_and_dedupe(
    persona: str,
    intent: str,
    index: PersonaIndexDict,
    client: Anthropic,
    tracker: RepetitionTracker,
    ledger: MessageLedger,
    max_retries: int = 3
) -> Tuple[str, bool, str]
```

Phase 17 will consume these and produce:
```python
# Delivery returns metadata with unique IDs and timestamps
@dataclass
class DeliveryResult:
    message_id: str           # Unique message_id (MSG-YYYYMMDDHHMMSSMMMM-RANDOM)
    telegram_message_id: Optional[int]  # Telegram API returned message_id
    sent_at: str              # ISO 8601 UTC timestamp
    delivered_at: Optional[str]  # ISO 8601 UTC timestamp or None
    status: Literal["success", "failure"]
    error: Optional[str]      # Error reason if status="failure"
```

Hermes relay integration (from NIZAM__system.relay.poller):
```python
# Available functions (imported from relay infrastructure)
def tg_send_message(token: str, chat_id: int, text: str, parse_mode: Optional[str] = None) -> dict
def tg_get_updates(token: str, offset: int, timeout: int = 25) -> list[dict]
# Custom exceptions
class GatewayPollingConflict(Exception): pass
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 0: Create Message ID Generator (Globally Unique, Sortable IDs)</name>
  <files>HIKMAH__knowledge_index/delivery/message_id_generator.py</files>
  <action>
Create HIKMAH__knowledge_index/delivery/message_id_generator.py module implementing MessageIDGenerator class:

1. **MessageIDGenerator class** with two class methods:
   - `generate() -> str`: Create unique message ID with format MSG-{YYYYMMDDHHMMSSMMMM}-{8-CHAR-RANDOM-HEX}
     - Timestamp extracted to UTC millisecond precision (use datetime.now(timezone.utc))
     - Random suffix uses uuid4().hex[:8].upper() for collision resistance
     - Example: "MSG-20260621093045123-A7F2E8CD"
   - `parse(msg_id: str) -> dict`: Extract timestamp and metadata from message ID
     - Input validation: check format (3 parts: MSG, timestamp, random)
     - Extract YYYYMMDDHHMMSSMMMM and parse to datetime object
     - Return dict with "message_id" and "timestamp_utc" keys
     - Raise ValueError on invalid format

2. **Module docstring** (50+ lines):
   - Purpose: Generate sortable unique IDs for message tracking
   - Why ULID-style: Sortable by timestamp, universally unique, no distributed coordination needed
   - Usage examples
   - Properties: timezone-aware (UTC), monotonically increasing, deterministic parsing

3. **No external dependencies beyond stdlib** (datetime, uuid)

4. **Test structure prepared** but tests in separate Wave 2 task
   - Create placeholder docstrings noting test coverage expectations

**Privacy note:** Message ID is purely technical (timestamp + random), no PII encoded.

**Verification command location:** Will be `pytest HIKMAH__knowledge_index/delivery/tests/test_message_id_generator.py::test_message_id_uniqueness -v` (Wave 2).
  </action>
  <verify>
File exists: HIKMAH__knowledge_index/delivery/message_id_generator.py
Imports work: `from HIKMAH__knowledge_index.delivery.message_id_generator import MessageIDGenerator`
Methods present: MessageIDGenerator.generate() returns string starting with "MSG-"
Methods present: MessageIDGenerator.parse() accepts string, returns dict with "message_id" and "timestamp_utc"
Parse round-trip: `msg_id = MessageIDGenerator.generate(); parsed = MessageIDGenerator.parse(msg_id); assert parsed["message_id"] == msg_id`
Sortability: Generate 5 IDs in sequence, verify timestamps are monotonically increasing
  </verify>
  <automated>pytest HIKMAH__knowledge_index/delivery/tests/test_message_id_generator.py::test_message_id_uniqueness -v</automated>
  <done>MessageIDGenerator class implemented with generate() and parse() methods, module docstring complete, ready for Wave 2 testing.</done>
</task>

<task type="auto">
  <name>Task 1: Create Delivery Ledger (JSONL Append-Only with Privacy Gates)</name>
  <files>HIKMAH__knowledge_index/delivery/delivery_ledger.py</files>
  <action>
Create HIKMAH__knowledge_index/delivery/delivery_ledger.py module implementing DeliveryLedger class:

1. **DeliveryLedger class** with __init__ and four methods:
   - `__init__(ledger_path: Path)`: Initialize ledger file path
     - Create parent directories (parents=True, exist_ok=True)
     - Store path as instance variable
   
   - `log_delivery(message_id: str, telegram_message_id: Optional[int], persona: str, message_text: str, intent: str, sent_at: str, delivered_at: Optional[str], context_tags: list[str], status: str, error_reason: Optional[str] = None) -> None`
     - Validate context_tags against CONTEXT_TAGS_WHITELIST = ["technical", "health", "financial", "strategic", "personal"]
     - Raise ValueError if invalid tag found (fail-safe privacy gate)
     - Build entry dict with fields: ts (current UTC ISO), message_id, telegram_message_id, persona, event_type="delivery", message_text, intent, sent_at, delivered_at, context_tags, status, error_reason
     - Compute SHA256 hash of entry JSON (16-char truncation) and add to entry as "ledger_hash"
     - Append to ledger as single JSON line (no pretty-printing)
     - Handle file creation on first write
   
   - `log_response(message_id: str, telegram_message_id: int, response_text: str, response_time: str, engagement_latency_seconds: float, persona: str) -> None`
     - Build entry dict with fields: ts (current UTC ISO), message_id, telegram_message_id, persona, event_type="response", response_text (truncate to 500 chars for safety), response_time, engagement_latency_seconds, engagement_status="successful"
     - Compute SHA256 ledger_hash and append
   
   - `log_no_response(message_id: str, telegram_message_id: int, persona: str) -> None`
     - Build entry dict with fields: ts (current UTC ISO), message_id, telegram_message_id, persona, event_type="engagement_window_closed", engagement_status="no_response"
     - Compute SHA256 ledger_hash and append
   
   - `get_deliveries_for_persona(persona: str, limit: int = 10) -> list[dict]`
     - Read ledger file (handle missing file gracefully)
     - Parse JSON lines, filter by persona and event_type="delivery"
     - Return last N entries (most recent first)
   
   - `get_responses_for_message(message_id: str) -> Optional[dict]`
     - Read ledger file (handle missing file gracefully)
     - Find first entry with matching message_id and event_type="response"
     - Return entry or None

2. **Module docstring** (60+ lines):
   - Purpose: Immutable audit trail for message delivery and response tracking
   - Why JSONL: Append-only, atomic writes, supports concurrent reads, natural for event ledgers
   - Privacy enforcement: context_tags whitelist validation
   - Ledger entry format documentation

3. **Constants:**
   - CONTEXT_TAGS_WHITELIST = ["technical", "health", "financial", "strategic", "personal"]

4. **Error handling:**
   - Try/catch on file write operations, propagate errors (fail-safe, don't silently ignore)
   - Malformed JSON lines in queries: skip (don't crash on old ledger corruption)

**Reference:** Follow Phase 14-16 ledger pattern (JSONL append-only, hash chaining, privacy gates)
  </action>
  <verify>
File exists: HIKMAH__knowledge_index/delivery/delivery_ledger.py
Imports work: `from HIKMAH__knowledge_index.delivery.delivery_ledger import DeliveryLedger`
Class methods present: __init__, log_delivery, log_response, log_no_response, get_deliveries_for_persona, get_responses_for_message
Initialize: `ledger = DeliveryLedger(Path("test_ledger.jsonl"))`
Log delivery: `ledger.log_delivery(message_id="MSG-...", telegram_message_id=12345, persona="AMMAR", message_text="test", intent="test", sent_at="2026-06-21T09:00:00Z", delivered_at="2026-06-21T09:00:05Z", context_tags=["technical"], status="success")`
Ledger file created: test_ledger.jsonl exists with JSON line
Context tag validation: `ledger.log_delivery(..., context_tags=["invalid_tag"])` raises ValueError
Query by persona: `ledger.get_deliveries_for_persona("AMMAR")` returns list (empty if file new)
Query by message ID: `ledger.get_responses_for_message("MSG-...")` returns None (no responses yet)
  </verify>
  <automated>pytest HIKMAH__knowledge_index/delivery/tests/test_delivery_ledger.py::test_log_delivery -v</automated>
  <done>DeliveryLedger class implemented with all methods, privacy gates enforced (context_tags whitelist), ledger structure validated.</done>
</task>

<task type="auto">
  <name>Task 2: Create Telegram Relay Client (Hermes Integration Wrapper)</name>
  <files>HIKMAH__knowledge_index/delivery/telegram_relay_client.py</files>
  <action>
Create HIKMAH__knowledge_index/delivery/telegram_relay_client.py module implementing TelegramRelayClient class:

1. **TelegramRelayClient class** with __init__ and methods:
   - `__init__(token: Optional[str] = None)`: Initialize relay client
     - Accept token parameter or read from os.environ["TELEGRAM_BOT_TOKEN"]
     - Raise ValueError if no token provided and not in environment
     - Store token as instance variable
   
   - `send_message(chat_id: int, text: str, parse_mode: Optional[str] = None) -> dict`
     - Call tg_send_message(self.token, chat_id, text, parse_mode)
     - Import: from NIZAM__system.relay.poller import tg_send_message
     - Handle exceptions: log error message (print to stderr or raise)
     - Return full Telegram API response dict (including message_id in result)
     - Docstring notes: Returns {"ok": True, "result": {"message_id": int, ...}} on success
     - Raises RuntimeError on API failure
   
   - `get_updates(offset: int, timeout: int = 25) -> list[dict]`
     - Call tg_get_updates(self.token, offset, timeout)
     - Import: from NIZAM__system.relay.poller import tg_get_updates
     - Handle GatewayPollingConflict exception (another process owns polling)
     - Raise exception to caller (Phase 17 Wave 2 will handle gracefully)
     - Return list of updates (may be empty on timeout)
   
   - `check_reply_to_message_id(update: dict) -> Optional[int]`
     - Extract reply_to_message_id from update
     - Path: update.get("message", {}).get("reply_to_message", {}).get("message_id")
     - Return None if not present or update has no message
     - Helper method for response correlation

2. **Module docstring** (50+ lines):
   - Purpose: Abstraction layer for Hermes relay (no direct Telegram API calls)
   - Why wrapper: Centralizes token management, error handling, future extensibility
   - Usage examples: send_message(), get_updates(), response correlation
   - Note on relay: Hermes relay is battle-tested, deployed, reuse infrastructure

3. **Error handling:**
   - Propagate exceptions to caller (don't swallow errors)
   - Docstring notes: RuntimeError on send failure, GatewayPollingConflict on polling conflict

4. **No additional dependencies** beyond existing relay (NIZAM__system.relay.poller)

**Reference:** Pattern 5 from 17-RESEARCH.md section "Pattern 5: Hermes Relay Integration"
  </action>
  <verify>
File exists: HIKMAH__knowledge_index/delivery/telegram_relay_client.py
Imports work: `from HIKMAH__knowledge_index.delivery.telegram_relay_client import TelegramRelayClient`
Initialize with token: `client = TelegramRelayClient(token="test_token")` stores token
Initialize from env: Set TELEGRAM_BOT_TOKEN env var, `client = TelegramRelayClient()` reads from env
Methods present: send_message, get_updates, check_reply_to_message_id
Send method docstring: Documents return format and exceptions
Get updates method docstring: Documents return format and GatewayPollingConflict exception
Reply extraction: `reply_id = client.check_reply_to_message_id({"message": {"reply_to_message": {"message_id": 123}}})` returns 123
  </verify>
  <automated>pytest HIKMAH__knowledge_index/delivery/tests/test_telegram_relay_client.py::test_send_message -v</automated>
  <done>TelegramRelayClient class implemented, wraps Hermes relay (tg_send_message, tg_get_updates), error handling in place.</done>
</task>

<task type="auto">
  <name>Task 3: Create Delivery Module Public API and Directory Structure</name>
  <files>HIKMAH__knowledge_index/delivery/__init__.py</files>
  <action>
Create HIKMAH__knowledge_index/delivery/__init__.py module with public API exports:

1. **Module docstring** (200+ lines):
   - Purpose: Telegram message delivery with Hermes relay integration, unique message IDs, response tracking
   - Overview of Phase 17 (Delivery & Response Tracking) goals
   - Architecture: 4 core components (message ID generator, delivery ledger, relay client, orchestrator/monitor in Wave 2)
   - Public API section: List all exported functions and classes
   - Usage example: Basic delivery flow (generate ID → send message → log to ledger)
   - Phase 17 integration points (consumes Phase 16 message generator, produces ledger for Phase 18)
   - Error handling patterns
   - Performance characteristics (ID generation <1ms, ledger write <5ms, relay send 1-5s)
   - Privacy and safety notes

2. **Imports and exports:**
   ```python
   from .message_id_generator import MessageIDGenerator
   from .delivery_ledger import DeliveryLedger
   from .telegram_relay_client import TelegramRelayClient
   
   __all__ = [
       "MessageIDGenerator",
       "DeliveryLedger",
       "TelegramRelayClient",
   ]
   ```

3. **No implementation code** in __init__.py (just imports and docstring)

4. **Create tests directory structure** (empty, ready for Wave 2):
   - Create HIKMAH__knowledge_index/delivery/tests/ directory
   - Create HIKMAH__knowledge_index/delivery/tests/__init__.py (empty file)
   - Create placeholder files for Wave 2:
     - tests/conftest.py (comment: "shared fixtures and mocks")
     - tests/test_message_id_generator.py (comment: "ID uniqueness and parse tests")
     - tests/test_delivery_ledger.py (comment: "ledger write and query tests")
     - tests/test_telegram_relay_client.py (comment: "relay integration tests")

**Pattern:** Follow Phase 14-16 module structure (all three phases have public __init__.py with imports)
  </action>
  <verify>
File exists: HIKMAH__knowledge_index/delivery/__init__.py
Imports work: `from HIKMAH__knowledge_index.delivery import MessageIDGenerator, DeliveryLedger, TelegramRelayClient`
Module docstring present: >150 lines, covers purpose/architecture/API/usage
__all__ defined: ["MessageIDGenerator", "DeliveryLedger", "TelegramRelayClient"]
Tests directory created: HIKMAH__knowledge_index/delivery/tests/ exists
Tests __init__.py created: tests/__init__.py exists (empty or with pass)
Test placeholder files exist: conftest.py, test_message_id_generator.py, test_delivery_ledger.py, test_telegram_relay_client.py
  </verify>
  <automated>python -c "from HIKMAH__knowledge_index.delivery import *; assert callable(MessageIDGenerator.generate)"</automated>
  <done>Delivery module public API created with comprehensive docstring, tests directory structure ready for Wave 2.</done>
</task>

<task type="auto">
  <name>Task 4: Update Parent Module Exports and README with Phase 17 Integration</name>
  <files>
    - HIKMAH__knowledge_index/__init__.py
    - HIKMAH__knowledge_index/README.md
  </files>
  <action>
Update parent HIKMAH__knowledge_index module to expose Phase 17 API and document delivery infrastructure:

**File 1: HIKMAH__knowledge_index/__init__.py**
1. Add Phase 17 imports to existing module docstring section:
   - Add new section "Phase 17: Delivery & Response Tracking" (60+ lines)
   - Import MessageIDGenerator, DeliveryLedger, TelegramRelayClient from delivery submodule
   - Update __all__ export list to include Phase 17 classes
   - Maintain existing Phase 14-16 exports (do not remove)

2. Add imports statement (after existing Phase 14-16 imports):
   ```python
   from .delivery import (
       MessageIDGenerator,
       DeliveryLedger,
       TelegramRelayClient,
   )
   ```

3. Verify circular import: delivery module should import NO classes from parent (safe to add)

**File 2: HIKMAH__knowledge_index/README.md**
1. Add new "Phase 17: Delivery & Response Tracking" section (300+ lines) after Phase 16 section
   - Overview: Scheduled delivery, message ID tracking, response monitoring, 1-hour engagement window
   - Architecture: Message ID generator → delivery orchestrator (Wave 2) → response monitor (Wave 2) → delivery ledger
   - Core API documentation:
     - MessageIDGenerator: generate(), parse() methods, example usage
     - DeliveryLedger: log_delivery(), log_response(), log_no_response(), query methods
     - TelegramRelayClient: send_message(), get_updates(), check_reply_to_message_id()
   - Message ID format: MSG-{YYYYMMDDHHMMSSMMMM}-{8-CHAR-HEX}, sortable, no collisions
   - Delivery ledger structure: JSONL, privacy gates (context_tags whitelist), event types (delivery/response/window_closed)
   - Hermes relay integration: Why use relay (no direct API calls, no polling conflicts)
   - Phase 18+ integration example: Query delivery ledger for response rate calculations
   - Common pitfalls section: Timezone handling, response matching (use reply_to_message_id), monitor cleanup
   - Error handling patterns

2. Update "Architecture" section to include Phase 17:
   ```
   ├── delivery/                          # Phase 17 (NEW): Message delivery & response tracking
   │   ├── __init__.py                    # Public API
   │   ├── message_id_generator.py        # Unique ID generation
   │   ├── delivery_ledger.py             # JSONL ledger
   │   ├── telegram_relay_client.py       # Hermes relay wrapper
   │   └── tests/                         # Test suite (Wave 2)
   ```

3. Update "Key Files" table to include Phase 17:
   - message_id_generator.py, delivery_ledger.py, telegram_relay_client.py
   - DELIVERY_LEDGER.jsonl
   - README.md Phase 17 section

**Reference:** Phase 16-01 integration example in existing README

**Do not modify:** Existing Phase 14-16 sections (only append Phase 17)
  </action>
  <verify>
Parent __init__ updated: `from HIKMAH__knowledge_index import MessageIDGenerator, DeliveryLedger, TelegramRelayClient` works
Parent __all__ includes: ["MessageIDGenerator", "DeliveryLedger", "TelegramRelayClient", ...existing exports...]
No circular imports: Parent module imports delivery, delivery imports from relay only (no parent imports)
README Phase 17 section present: >200 lines, covers overview, architecture, API, integration
README Architecture updated: Phase 17 delivery directory documented
README Key Files table updated: Phase 17 files listed
README Phase 17 example: Integration example showing message delivery flow
File syntax valid: Both Python files have valid syntax (no import errors)
  </verify>
  <automated>python -c "from HIKMAH__knowledge_index import delivery; assert hasattr(delivery, 'DeliveryLedger')"</automated>
  <done>Parent module exports Phase 17 API, comprehensive README documentation added for delivery infrastructure, integration points documented.</done>
</task>

</tasks>

<verification>
## Wave 1 Deliverables Verification

**Code Quality:**
- All 4 modules created in HIKMAH__knowledge_index/delivery/ (message_id_generator.py, delivery_ledger.py, telegram_relay_client.py, __init__.py)
- All modules have comprehensive docstrings (50+ lines each)
- No external dependencies beyond stdlib and existing Hermes relay
- All imports validated (no circular dependencies)
- Privacy gates enforced (context_tags whitelist in DeliveryLedger)

**Phase 17 Requirements Coverage (Wave 1):**
- ✓ DELIVERY-01: Foundation for twice-daily delivery via Hermes relay (relay client abstraction ready)
- ✓ DELIVERY-02: Unique message_id generation infrastructure (MessageIDGenerator.generate())
- ✓ DELIVERY-03: Delivery ledger infrastructure (DeliveryLedger.log_delivery() with sent_at, delivered_at)
- ✓ DELIVERY-04: Relay client get_updates() and check_reply_to_message_id() for response monitoring (foundation)
- ✓ DELIVERY-05: Response logging infrastructure (DeliveryLedger.log_response() with timestamps)

**Architecture Compliance:**
- Message ID format: MSG-{YYYYMMDDHHMMSSMMMM}-{RANDOM-HEX} (sortable, collision-free)
- Ledger format: JSONL append-only (following Phase 14-16 pattern)
- Privacy enforcement: Context tags validated against whitelist (fail-safe gate)
- Hermes relay integration: No direct Telegram API calls, reuses battle-tested infrastructure
- Error handling: Exceptions propagated to caller (fail-fast, not silent failures)

**Module Dependencies:**
- delivery module requires: No Phase 14-16 modules (independent)
- Depends on: NIZAM__system.relay.poller (Hermes relay)
- Consumed by: Wave 2 (delivery_orchestrator, response_monitor)

**File Structure:**
```
HIKMAH__knowledge_index/delivery/
├── __init__.py                           # Public API (NEW)
├── message_id_generator.py               # ULID generator (NEW)
├── delivery_ledger.py                    # JSONL ledger (NEW)
├── telegram_relay_client.py              # Relay wrapper (NEW)
└── tests/
    ├── __init__.py
    ├── conftest.py                       # Placeholder for Wave 2
    ├── test_message_id_generator.py      # Placeholder for Wave 2
    ├── test_delivery_ledger.py           # Placeholder for Wave 2
    └── test_telegram_relay_client.py     # Placeholder for Wave 2
```

**Integration Readiness:**
- Parent HIKMAH__knowledge_index/__init__.py updated with Phase 17 exports
- README.md Phase 17 documentation complete
- Wave 2 will implement delivery_orchestrator (coordinates ID gen + relay send + ledger log)
- Wave 2 will implement response_monitor (polls relay + correlates replies + logs responses)

**Tests Prepared for Wave 2:**
- Message ID uniqueness (10k IDs, check no collisions)
- Ledger write/read operations
- Context tag validation (reject invalid tags)
- Relay mocking (no real Telegram calls)
- Response correlation logic (reply_to_message_id matching)
</verification>

<success_criteria>
✓ All 4 core modules created and importable
✓ MessageIDGenerator produces unique, sortable IDs (MSG-YYYYMMDDHHMMSSMMMM-RANDOM format)
✓ DeliveryLedger enforces privacy gates (context_tags whitelist validation)
✓ TelegramRelayClient provides abstraction for Hermes relay (send_message, get_updates, reply correlation)
✓ Delivery module public API exported from parent HIKMAH__knowledge_index
✓ README.md Phase 17 section documents architecture, API, integration points
✓ Test directory structure ready for Wave 2 implementation
✓ All DELIVERY-01 through DELIVERY-05 requirements have infrastructure in place for Wave 2
✓ No external dependencies added (uses stdlib + existing Hermes relay)
✓ Error handling follows fail-fast pattern (exceptions propagated, not silently ignored)
✓ Privacy enforcement locked (context_tags whitelist, no raw PII in ledger)
</success_criteria>

<output>
After completion, create `.planning/phases/17-delivery-response-tracking/17-01-SUMMARY.md` documenting:
- Files created (4 modules + tests structure)
- Public API exported (MessageIDGenerator, DeliveryLedger, TelegramRelayClient)
- Requirements satisfied by Wave 1 infrastructure (DELIVERY-01, DELIVERY-02, DELIVERY-03, DELIVERY-04, DELIVERY-05 all have foundation)
- Integration points ready for Wave 2 (orchestrator, response monitor)
- Test scaffold prepared for Wave 2 (5 test modules, 30+ tests planned)
- README.md Phase 17 section complete
</output>
