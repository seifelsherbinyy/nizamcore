---
phase: 18-adaptation-format-evolution
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - HIKMAH__knowledge_index/adaptation/__init__.py
  - HIKMAH__knowledge_index/adaptation/adaptation_state.py
  - HIKMAH__knowledge_index/adaptation/response_rate_calculator.py
  - HIKMAH__knowledge_index/adaptation/format_rotation_manager.py
  - HIKMAH__knowledge_index/adaptation/adaptation_logger.py
  - HIKMAH__knowledge_index/adaptation/tests/__init__.py
  - HIKMAH__knowledge_index/adaptation/tests/conftest.py
  - HIKMAH__knowledge_index/adaptation/tests/test_response_rate_calculator.py
  - HIKMAH__knowledge_index/adaptation/tests/test_format_rotation_manager.py
  - HIKMAH__knowledge_index/adaptation/tests/test_adaptation_logger.py
autonomous: true
requirements:
  - ADAPT-01
  - ADAPT-02
  - ADAPT-03
  - ADAPT-04

must_haves:
  truths:
    - "WeeklyResponseRateCalculator.calculate(persona, days=7) returns (rate: float, numerator: int, denominator: int) tuple"
    - "When denominator=0 (new persona, no deliveries), calculate() returns (1.0, 0, 0) — skip adaptation, no ZeroDivisionError"
    - "FormatRotationManager.rotate_format(persona, reason) advances format in cycle: standard → short → emoji → direct_question → story → standard"
    - "rotate_format() never returns the same format twice consecutively — previous_format check enforced"
    - "AdaptationLogger.log_rotation() appends JSONL entry with persona, old_format, new_format, response_rate, rationale, timestamp, adaptation_id"
    - "ADAPTATION_STATE.jsonl persists current_format and previous_format per persona across calls (reads from disk, not memory)"
  artifacts:
    - path: "HIKMAH__knowledge_index/adaptation/response_rate_calculator.py"
      provides: "WeeklyResponseRateCalculator class"
      exports: ["WeeklyResponseRateCalculator"]
    - path: "HIKMAH__knowledge_index/adaptation/format_rotation_manager.py"
      provides: "FormatRotationManager class with state machine"
      exports: ["FormatRotationManager", "FORMATS"]
    - path: "HIKMAH__knowledge_index/adaptation/adaptation_logger.py"
      provides: "AdaptationLogger JSONL writer"
      exports: ["AdaptationLogger"]
    - path: "HIKMAH__knowledge_index/adaptation/adaptation_state.py"
      provides: "AdaptationState dataclass + file I/O"
      exports: ["AdaptationState", "load_state", "save_state"]
    - path: "HIKMAH__knowledge_index/adaptation/__init__.py"
      provides: "Public API"
      exports: ["WeeklyResponseRateCalculator", "FormatRotationManager", "AdaptationLogger", "AdaptationState"]
    - path: "HIKMAH__knowledge_index/adaptation/tests/test_response_rate_calculator.py"
      provides: "8 unit tests for rate calculation"
      contains: "test_calculate_basic, test_calculate_no_deliveries, test_calculate_7_day_filter"
    - path: "HIKMAH__knowledge_index/adaptation/tests/test_format_rotation_manager.py"
      provides: "12 unit tests for rotation state machine"
      contains: "test_rotate_advances_format, test_rotate_no_consecutive_repeat, test_ten_rotations_no_repeats"
    - path: "HIKMAH__knowledge_index/adaptation/tests/test_adaptation_logger.py"
      provides: "8 unit tests for JSONL logging"
      contains: "test_log_rotation_writes_entry, test_log_rotation_includes_metadata, test_rationale_format"
  key_links:
    - from: "DeliveryLedger.get_deliveries_for_persona()"
      to: "WeeklyResponseRateCalculator.calculate()"
      via: "ledger_path passed to WeeklyResponseRateCalculator.__init__()"
      pattern: "WeeklyResponseRateCalculator\\(ledger_path="
    - from: "WeeklyResponseRateCalculator.calculate()"
      to: "FormatRotationManager.rotate_format()"
      via: "response_rate < 0.80 threshold check by caller"
      pattern: "response_rate < 0\\.80"
    - from: "FormatRotationManager.rotate_format()"
      to: "AdaptationLogger.log_rotation()"
      via: "called inside rotate_format() BEFORE state update"
      pattern: "self\\.logger\\.log_rotation"
    - from: "AdaptationLogger.log_rotation()"
      to: "ADAPTATION_LEDGER.jsonl"
      via: "append-only JSONL write"
      pattern: "ADAPTATION_LEDGER\\.jsonl"
---

<objective>
Build the three core adaptation modules with full unit test coverage.

Purpose: Establish the complete adaptation state machine — rate calculation from the Phase 17 delivery ledger, format rotation with no-consecutive-repeat enforcement, and JSONL audit logging. These are the foundations that Plan 18-02 will wire into Phase 16 message generation.

Output:
- `adaptation/` package with 4 production modules and public `__init__.py`
- 28+ unit tests in `adaptation/tests/` (8 rate calc + 12 rotation + 8 logger)
- `ADAPTATION_STATE.jsonl` and `ADAPTATION_LEDGER.jsonl` created on first write
</objective>

<execution_context>
@C:/Users/PC/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/PC/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/18-adaptation-format-evolution/18-RESEARCH.md

<interfaces>
<!-- Phase 17 DeliveryLedger API — executor must use these exact method signatures. -->
<!-- Source: HIKMAH__knowledge_index/delivery/delivery_ledger.py lines 466–530 -->

```python
class DeliveryLedger:
    def __init__(self, ledger_path: Path): ...

    def get_deliveries_for_persona(self, persona: str, limit: int = 10) -> List[dict]:
        """Returns delivery events (event_type='delivery') most-recent-first.
        Each dict has: ts, event_type, message_id, persona, sent_at, delivered_at, status, context_tags
        Returns [] if ledger does not exist yet."""

    def get_responses_for_message(self, message_id: str) -> Optional[dict]:
        """Returns the first response event (event_type='response') for message_id, or None."""
```

<!-- AdaptationState file schema — mirrors Phase 14–17 JSONL patterns -->
<!-- ADAPTATION_STATE.jsonl: one JSON object per persona (last state wins on load) -->
```json
{
  "ts": "2026-06-21T09:30:00Z",
  "persona": "AMMAR",
  "current_format": "standard",
  "previous_format": null,
  "rotation_index": 0,
  "last_rotation_at": null,
  "adaptation_id": null
}
```

<!-- ADAPTATION_LEDGER.jsonl: one entry per rotation event -->
```json
{
  "ts": "2026-06-21T09:30:00Z",
  "adaptation_id": "ADAPT-AMMAR-20260621-001",
  "persona": "AMMAR",
  "event_type": "format_rotation",
  "old_format": "standard",
  "new_format": "short",
  "trigger": "engagement_threshold_breach",
  "response_rate": 0.65,
  "response_rate_threshold": 0.80,
  "calculation_window_days": 7,
  "denominator": 20,
  "numerator": 13,
  "rationale": "AMMAR response rate 65% < 80%, switching from 'standard' to 'short' format",
  "ledger_hash": "a1b2c3..."
}
```

<!-- Format rotation cycle — IMMUTABLE ORDER -->
```python
FORMATS = ["standard", "short", "emoji", "direct_question", "story"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AdaptationState dataclass and file I/O</name>
  <files>
    HIKMAH__knowledge_index/adaptation/adaptation_state.py,
    HIKMAH__knowledge_index/adaptation/tests/__init__.py,
    HIKMAH__knowledge_index/adaptation/tests/conftest.py
  </files>
  <behavior>
    - AdaptationState dataclass has: persona, current_format, previous_format (optional), rotation_index (int), last_rotation_at (optional ISO str), adaptation_id (optional str)
    - load_state(persona, state_path) reads ADAPTATION_STATE.jsonl, returns last entry matching persona, or default state (current_format="standard", previous_format=None, rotation_index=0) if no entry
    - save_state(state, state_path) appends new JSONL entry — does NOT overwrite (append-only, latest line wins on load)
    - Test: load_state on missing file returns default state for any persona
    - Test: save_state + load_state round-trip recovers same state
    - Test: multiple save_state calls for same persona — load_state returns last saved
    - conftest.py fixture: tmp_state_path (tmp_path / "ADAPTATION_STATE.jsonl"), tmp_ledger_path, mock_delivery_ledger_with_events(n_deliveries, n_responses, persona)
  </behavior>
  <action>
    Create `HIKMAH__knowledge_index/adaptation/` package directory.

    Create `adaptation_state.py` with:
    - `FORMATS = ["standard", "short", "emoji", "direct_question", "story"]` constant at module level
    - `@dataclass AdaptationState` with fields: persona (str), current_format (str, default "standard"), previous_format (Optional[str], default None), rotation_index (int, default 0), last_rotation_at (Optional[str], default None), adaptation_id (Optional[str], default None)
    - `load_state(persona: str, state_path: Path) -> AdaptationState`: reads JSONL line by line, keeps last entry where entry["persona"] == persona, returns AdaptationState from that dict; if file missing or no matching entry, returns AdaptationState(persona=persona) with all defaults
    - `save_state(state: AdaptationState, state_path: Path) -> None`: serializes to dict, appends JSON line to state_path (creates file if needed); use `state_path.parent.mkdir(parents=True, exist_ok=True)` before open
    - `to_dict(state: AdaptationState) -> dict`: serializes including ts (datetime.utcnow().isoformat() + "Z")

    Create `adaptation/tests/__init__.py` (empty).

    Create `adaptation/tests/conftest.py` with:
    - `tmp_state_path` fixture: returns `tmp_path / "ADAPTATION_STATE.jsonl"` (does not create file)
    - `tmp_ledger_path` fixture: returns `tmp_path / "ADAPTATION_LEDGER.jsonl"` (does not create file)
    - `mock_delivery_ledger` parametrized fixture factory: function `make_mock_ledger(n_deliveries, n_responses, persona, tmp_path)` that writes a DELIVERY_LEDGER.jsonl with n_deliveries "delivery" events and n_responses "response" events (first n_responses deliveries get matching response events); each delivery entry: {"ts": ..., "event_type": "delivery", "message_id": f"MSG-{i}", "persona": persona, "sent_at": ..., "status": "success"}; each response entry: {"ts": ..., "event_type": "response", "message_id": f"MSG-{i}", "persona": persona}
    - `sent_at` timestamps for deliveries: generate timestamps within past 7 days (use datetime.utcnow() - timedelta(days=i % 6)) so they fall within the 7-day window
  </action>
  <verify>
    <automated>pytest HIKMAH__knowledge_index/adaptation/tests/test_adaptation_state.py -x -v 2>/dev/null || pytest HIKMAH__knowledge_index/adaptation/tests/ -x -v -k "state" 2>/dev/null; echo "adaptation package created"</automated>
  </verify>
  <done>
    AdaptationState round-trips correctly. load_state returns defaults for new persona. ADAPTATION_STATE.jsonl appended (not overwritten) on save.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WeeklyResponseRateCalculator with unit tests</name>
  <files>
    HIKMAH__knowledge_index/adaptation/response_rate_calculator.py,
    HIKMAH__knowledge_index/adaptation/tests/test_response_rate_calculator.py
  </files>
  <behavior>
    - calculate(persona, days=7) returns Tuple[float, int, int] — (rate, numerator, denominator)
    - Denominator: count "delivery" events for persona where sent_at within past `days` days AND status="success"
    - Numerator: count "response" events for message_ids in denominator set
    - Edge case: denominator=0 → return (1.0, 0, 0), no ZeroDivisionError
    - Edge case: all deliveries responded → rate=1.0
    - Edge case: no responses → rate=0.0, numerator=0
    - Test: 14 deliveries, 10 responses → rate=0.714..., numerator=10, denominator=14
    - Test: 20 deliveries, 16 responses → rate=0.8 (at threshold, should NOT trigger rotation)
    - Test: 20 deliveries, 13 responses → rate=0.65 (below 80%, should trigger rotation)
    - Test: 0 deliveries → (1.0, 0, 0)
    - Test: deliveries older than 7 days are excluded from denominator
    - Test: delivery with status="failure" is excluded from denominator
    - Test: deliveries for different persona are excluded
    - Test: calculate() works when DELIVERY_LEDGER.jsonl does not exist (returns 1.0, 0, 0)
  </behavior>
  <action>
    Create `response_rate_calculator.py`:

    ```python
    class WeeklyResponseRateCalculator:
        def __init__(self, ledger_path: Path):
            self.ledger_path = ledger_path

        def calculate(self, persona: str, days: int = 7) -> Tuple[float, int, int]:
            # Returns (rate, numerator, denominator)
            # If ledger missing → (1.0, 0, 0)
    ```

    Implementation:
    1. If `self.ledger_path` does not exist → return (1.0, 0, 0)
    2. Read all JSONL lines from ledger_path, parse each as JSON (skip malformed lines)
    3. cutoff = datetime.utcnow() - timedelta(days=days)
    4. Filter "delivery" events: event_type=="delivery", persona matches, sent_at parses to datetime >= cutoff, status=="success"
    5. Build set of qualifying message_ids
    6. denominator = len(qualifying message_ids)
    7. If denominator == 0 → return (1.0, 0, 0)
    8. Filter "response" events: event_type=="response", message_id in qualifying set
    9. numerator = count of matching response events
    10. rate = numerator / denominator
    11. Return (rate, numerator, denominator)

    Parse sent_at: use `datetime.fromisoformat(sent_at.replace("Z", "+00:00"))` then compare with cutoff (timezone-aware). Handle ISO strings with or without 'Z' suffix.

    Create `test_response_rate_calculator.py` using conftest fixtures. Use `make_mock_ledger` factory from conftest to create synthetic DELIVERY_LEDGER.jsonl files. Test all 8 behaviors from the behavior block above.
  </action>
  <verify>
    <automated>pytest HIKMAH__knowledge_index/adaptation/tests/test_response_rate_calculator.py -x -v</automated>
  </verify>
  <done>
    All 8 tests pass. calculate() returns correct (rate, n, d) tuples. Zero-division guard confirmed. 7-day filter confirmed.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: FormatRotationManager, AdaptationLogger, and public __init__.py with unit tests</name>
  <files>
    HIKMAH__knowledge_index/adaptation/adaptation_logger.py,
    HIKMAH__knowledge_index/adaptation/format_rotation_manager.py,
    HIKMAH__knowledge_index/adaptation/__init__.py,
    HIKMAH__knowledge_index/adaptation/tests/test_format_rotation_manager.py,
    HIKMAH__knowledge_index/adaptation/tests/test_adaptation_logger.py
  </files>
  <behavior>
    FormatRotationManager:
    - get_current_format(persona) returns current_format from ADAPTATION_STATE.jsonl (default "standard" for new persona)
    - rotate_format(persona, reason, response_rate, numerator, denominator) → str: advances format index, checks previous_format to prevent consecutive repeat, writes ADAPTATION_LEDGER.jsonl entry via AdaptationLogger, updates ADAPTATION_STATE.jsonl; returns new_format
    - Test: get_current_format on new persona → "standard"
    - Test: rotate_format("AMMAR", ...) from "standard" → "short"
    - Test: rotate_format("AMMAR", ...) from "short" → "emoji" (not "standard")
    - Test: wraps around: after "story" → "standard"
    - Test: no-consecutive-repeat — if previous="short" and next would be "short", skips to "emoji"
    - Test: 10 consecutive rotate_format calls produce 10 different formats (no consecutive pairs identical)
    - Test: state persisted to disk (new FormatRotationManager instance reads correct state)
    - Test: rotate_format logs to ADAPTATION_LEDGER.jsonl before updating state
    - Test: get_current_format returns most recently rotated format after rotate_format call
    - Test: rotate_format with last_rotation_at within 7 days returns current format unchanged (rate-limit: 1 rotation per week per persona)

    AdaptationLogger:
    - log_rotation(persona, old_format, new_format, response_rate, numerator, denominator, reason, threshold=0.80, window_days=7) → adaptation_id (str)
    - Appends JSON line to ADAPTATION_LEDGER.jsonl with all fields from research schema
    - adaptation_id format: "ADAPT-{PERSONA}-{YYYYMMDD}-{NNN}" where NNN is zero-padded count of today's entries for this persona
    - rationale is auto-generated: f"{persona} response rate {response_rate:.0%} < {threshold:.0%}, switching from '{old_format}' to '{new_format}' format"
    - ledger_hash: SHA256 hex of the JSON string (first 16 chars) — mirrors Phase 14-17 pattern
    - Test: log_rotation writes JSONL entry with all required fields
    - Test: adaptation_id increments per-persona per-day (second call for same persona same day gets -002)
    - Test: rationale string matches expected human-readable format
    - Test: log_rotation on missing ledger file creates it
    - Test: log_rotation appends (does not overwrite) — two calls → two lines
    - Test: ledger_hash is a 16-char hex string

    __init__.py exports: WeeklyResponseRateCalculator, FormatRotationManager, AdaptationLogger, AdaptationState, load_state, save_state, FORMATS
  </behavior>
  <action>
    Create `adaptation_logger.py`:
    - `AdaptationLogger(ledger_path: Path)` class
    - `log_rotation(...)` method: builds entry dict per schema, computes adaptation_id by reading existing ledger for today's entries for this persona (count existing "format_rotation" events for persona today, then NNN = count + 1), appends JSONL line
    - Uses `pathlib.Path.open("a")` for append-only write
    - SHA256 ledger_hash: `hashlib.sha256(json_str.encode()).hexdigest()[:16]`

    Create `format_rotation_manager.py`:
    - `FormatRotationManager(state_path: Path, ledger_path: Path)` class
    - Constructor imports: `from .adaptation_state import AdaptationState, load_state, save_state, FORMATS`
    - Constructor imports: `from .adaptation_logger import AdaptationLogger`
    - Constructor imports: `from .response_rate_calculator import WeeklyResponseRateCalculator` (for rate param, NOT calculated internally — caller passes rate)
    - `get_current_format(persona: str) -> str`: calls load_state(), returns state.current_format
    - `rotate_format(persona, reason, response_rate, numerator, denominator) -> str`:
      1. Load current state
      2. Check last_rotation_at: if it's within 7 days of now, return current_format unchanged (1-rotation-per-week guard; log skipped rotation at INFO level)
      3. Find current index: `FORMATS.index(state.current_format)` (default 0 if not found)
      4. Advance: `next_idx = (current_idx + 1) % len(FORMATS)`, `next_format = FORMATS[next_idx]`
      5. No-consecutive-repeat: if `next_format == state.previous_format`: `next_idx = (next_idx + 1) % len(FORMATS)`, `next_format = FORMATS[next_idx]`
      6. Log to ADAPTATION_LEDGER via self.logger.log_rotation() — BEFORE state update
      7. Update state: previous_format=current_format, current_format=next_format, rotation_index=next_idx, last_rotation_at=now ISO, adaptation_id=returned id
      8. Save state via save_state()
      9. Return next_format

    Create `adaptation/__init__.py` with explicit imports and `__all__` list.

    Create test files covering all behaviors listed in the behavior block. Use `tmp_state_path` and `tmp_ledger_path` fixtures from conftest.
  </action>
  <verify>
    <automated>pytest HIKMAH__knowledge_index/adaptation/tests/ -x -v --tb=short</automated>
  </verify>
  <done>
    All 28+ tests pass. FormatRotationManager rotates correctly, no consecutive repeats across 10 calls confirmed. AdaptationLogger writes JSONL with correct schema. Public __init__.py exports all classes. pytest runs clean with 0 failures.
  </done>
</task>

</tasks>

<verification>
Full adaptation module verification:
```
pytest HIKMAH__knowledge_index/adaptation/tests/ -v --cov=HIKMAH__knowledge_index/adaptation --cov-report=term-missing
```
Expected: 28+ tests pass, coverage >= 80% on response_rate_calculator.py, format_rotation_manager.py, adaptation_logger.py, adaptation_state.py.

Import check:
```python
from HIKMAH__knowledge_index.adaptation import (
    WeeklyResponseRateCalculator, FormatRotationManager,
    AdaptationLogger, AdaptationState, FORMATS
)
```
Expected: no ImportError.
</verification>

<success_criteria>
- `adaptation/` package importable with all 4 classes exported
- 28+ unit tests pass with 0 failures
- WeeklyResponseRateCalculator correctly filters past-7-days deliveries, handles denominator=0
- FormatRotationManager advances cycle with no-consecutive-repeat and 1-rotation-per-week guards
- AdaptationLogger produces valid JSONL with adaptation_id, rationale, ledger_hash
- AdaptationState persists to disk (not in-memory only) — verified by new-instance reads in tests
</success_criteria>

<output>
After completion, create `.planning/phases/18-adaptation-format-evolution/18-01-SUMMARY.md`
</output>
