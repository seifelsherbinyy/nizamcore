# Executable Prompt

Inspect the live OVH VPS, NIZAM GitHub repositories, Hermes configuration, and `47_NIZAM` Google Drive before changing anything. Implement NIZAM Health Intelligence v0.2 as cloud-first: VPS owns runtime databases, ingestion, deterministic analytics, caches, cron and secrets; Drive owns organized durable knowledge, journals, ledgers, reports, indexes and retrieval manifests; GitHub owns code, schemas and tests. Reconcile existing structures instead of duplicating them. Build fast index-first Hermes retrieval with provenance and freshness resolution. Implement WHOOP read-only ingestion, rolling metrics, journal/workout/calendar joins, and the validated 11:00 Africa/Cairo workflow. Never expose credentials, invent paths, weaken tests, or write calendar events without approval. Verify, tamper-test, record, sync, and read back every landed artifact.
