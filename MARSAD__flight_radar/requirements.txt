# MARSAD — NIZAM Flight Radar dependencies
# Versions current as of 2026-06-24 — verify against PyPI before install
# SWAPPABLE_DEFAULT labels indicate replaceable components

# Core
python-dotenv>=1.0.0
pydantic>=2.0.0
requests>=2.31.0

# Primary data source: SerpApi Google Flights — uses requests directly (no SDK needed)
# The serpapi_source.py calls https://serpapi.com/search via requests.get
# No additional package required beyond requests>=2.31.0
# SERPAPI_KEY environment variable required — sign up at serpapi.com

# Amadeus for Developers API — optional alternative (DATA_SOURCE=amadeus)
# portal status as of 2026: verify at developers.amadeus.com before enabling
# amadeus>=8.0.0

# Secondary / scraping sources — optional, heavy, gated behind ToS review
# playwright>=1.40.0    # Only if DATA_SOURCE=ita_matrix after ToS review
#                       # Install separately: playwright install chromium
# beautifulsoup4>=4.12.0  # Only if using HTML-scraping sources

# Forecasting and analytics (SWAPPABLE_DEFAULT: statsmodels for advanced models)
pandas>=2.1.0
numpy>=1.26.0
# statsmodels>=0.14.0   # Uncomment for Holt-Winters exponential smoothing upgrade

# Scheduling (SWAPPABLE_DEFAULT: GitHub Actions cron — see .github/workflows/marsad_monitor.yml)
APScheduler>=3.10.0

# Console output
rich>=13.0.0

# CSV import for historical seed (seed.py stage)
# csv module is stdlib — no extra package needed

# Development / testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# Optional: Kiwi Tequila secondary source
# KIWI_API_KEY required — sign up at tequila.kiwi.com
# No additional package; uses requests

# Optional: Slack alerts (uncomment if ALERT_DELIVERY=slack)
# slack-sdk>=3.25.0
