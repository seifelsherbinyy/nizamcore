# TARIQ Career Radar — environment variable documentation
# Copy to .env (which is gitignored) and fill in real values before running.
# NEVER commit .env — only .env.example is committed.

TARIQ_RADAR_MODE=standby  # or "live" when approved by operator
TARIQ_MANUAL_SOURCE_PATH=  # absolute path to operator manual import folder
