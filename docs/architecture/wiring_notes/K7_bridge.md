# K7 — Pulsation bridge

- `PulsationMessage.council_*` metadata fields
- `/council-review` via `skills_registry/router.py` + router.config.yaml
- Routine Loop A: no debate text; council skipped via triggers

Graphify path: `pulsation/loops` → `council/triggers` → minimal_pulse_note.
