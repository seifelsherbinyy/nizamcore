# L2 — Context refresh

- Module: `pulsation/context_refresh.py`
- Read-only scanners; freshness windows; no journal bodies in snapshots egress.

Graphify path: `refresh_context` → source scanner helpers → `ContextRefresh`.
